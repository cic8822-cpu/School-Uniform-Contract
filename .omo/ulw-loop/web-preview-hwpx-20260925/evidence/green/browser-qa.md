# GREEN — 실제 브라우저 QA

## F-008 원문 및 HWPX 작성

- 시나리오: 로컬 Vite 앱에서 `기초자료입력`의 학교명에 비식별값 `비식별 테스트학교`만 입력하고 `서식함 → F-008`을 열었다.
- 실행: Codex 인앱 브라우저에서 `http://127.0.0.1:4173/`을 열어 모달을 확인하고 `HWPX 작성`을 눌렀다.
- 이진 관찰값: 미리보기 `table` 3개, 본문 텍스트 길이 985, `섬유혼용률`·`야구점퍼`·`바지`가 모두 존재했다. 버튼은 enabled였고, 클릭 후 `HWPX 파일을 내려받았습니다.`가 표시됐다.
- 산출물: `C:\Users\최익창\Downloads\F-008_교복 사양서.hwpx` (2,090,977 bytes, 비식별 테스트 산출물). 원문과의 구조 검증은 `hwpx-export.md`에 기록했다.
- 화면: 이 작업 턴의 `F-008 원문 전체 미리보기 GREEN 스크린샷` 도구 이미지에 캡처했다.

## F-050 개인정보 보호 경계

- 시나리오: 같은 브라우저 세션에서 `서식함 → F-050`을 열었다. 개인정보 입력값은 전혀 입력하지 않았다.
- 실행: 모달 DOM과 화면을 확인했다.
- 이진 관찰값: 원문 표 3개와 만족도 척도는 존재하고, 모달 내부 `input, textarea, select` 수는 0개이며 콘솔 error/warn 수는 0개다. 공통 비식별 학교명만 원문 토큰에 반영됐고, 개인 성명·연락처·서명·개별 설문응답의 입력 또는 자동반영은 없었다.
- 화면: 이 작업 턴의 `F-050 빈 개인정보 보호 양식 GREEN 스크린샷` 도구 이미지에 캡처했다.

## 반응형·접근성

- 320×800: 모달 너비 274.33px, 문서 가로 넘침 `false`, 닫기 버튼 자동 포커스 `true`.
- 1280×800: 모달 너비 1100px, 문서 가로 넘침 `false`, 닫기 버튼 자동 포커스 `true`.
- 모달은 `border-box`, `maxHeight: 90vh`, `overflowY: auto`로 원문 전체를 작은 화면에서도 스크롤해 접근한다.

# F-049 교복 학교주관구매 만족도 설문조사 실시 구현·검증

- **상태**: `DONE — 단일 서식 구현 완료. P3-01 전체와 X-06은 미완료.`
- **검증일**: 2026-09-20
- **배포 후보**: `artifacts/excel/교복구매_길라잡이_20260920_v2.xlsm`(구조·VBA·검증 스크립트 전체 재실행, `verify_v1_log.txt` 전 항목 PASS, 290/290)

## 원본 대조

- 원본 HWPX `_workspace/02_hwpx/P2-01/repro/pristine/full.md` 2051~2080행(78쪽 `[19]`)을 직접 읽어 대조함.
  단일 물리 페이지, 붙임 설문지(`[19-1]`, F-050)는 별도 Form ID로 분리됨.
- 구조: F-044/F-046과 완전히 동일한 기안문 구조(내부결재용 만족도 설문 발송 기안) — 표제("○ ○ 학
  교") → 수신(내부결재(사업부서)) → (경유) → 제목("교복 학교주관구매 만족도 설문조사 실시") →
  "1. 관련"(F-044~048과 동일한 고정 근거 문구) → "2. ~설문지를 발송하고자 합니다"(만족도 조사 실시
  목적) → "가. 대상"(2027학년도 교복 학교주관구매 참여 학생 및 학부모) → "나. 내용"(가격, 품질,
  구매절차 및 사후관리 등 전반에 걸친 만족도 조사) → 붙임(만족도 설문지 1부) → 결재란(담당/협조자/
  교장, 시행/접수, 우편번호/주소/전화/팩스/이메일).

## 매핑표 재확인

- `서식_매핑표.md` F-049 행: `C-01~C-06, B-01~B-02, D-01~D-05` 입력, `C-01, C-02, B-01, D-02` 출력,
  계산 없음, "설문 실시 기안문" 비고. 원문과 불일치 없음.

## 반영 범위와 재사용 판단

- **표제(G2)·수신·경유**: F-044/F-046과 동일한 결재란 틀 재사용. `기초자료입력!C4`(학교명) 재사용,
  미입력 시 "○ ○ 학 교".
- **문서번호(C5)·시행일자(F5)**: `기초자료입력!C9`, `C8`(TEXT `yyyy-mm-dd`) 재사용.
- **제목(C9)**: `기초자료입력!C22`(D-02 문서 제목) 재사용, 미입력 시 원문 고정 제목("교복 학교주관구매
  만족도 설문조사 실시")으로 대체.
- **가. 대상(C15)**: `기초자료입력!C5`(학년도) 재사용 + 원문 고정 문구("교복 학교주관구매 참여 학생
  및 학부모").
- **나. 내용(C16)**: `기초자료입력!C12`(구매명, B-01) 재사용 + 원문 고정 문구("가격, 품질, 구매절차
  및 사후관리 등 전반에 걸친 만족도 조사"). 원문 텍스트 자체가 길어 `WrapText=True`·`RowHeight=28`을
  적용해 2줄로 표시함(첫 빌드에서 줄바꿈 없이 우측이 잘리는 결함을 PDF 육안 확인 중 발견·수정, 아래
  "PDF 육안 검토" 절 참고).
- **붙임(B18)**: "붙임 교복 학교주관구매 만족도 설문지 1부. 끝." — 원문과 동일.
- **붙임 설문지 본문 미반영**: `[19-1]` 만족도 조사 설문지(문항표·응답 O표·기타 의견란)는 별도
  Form ID(F-050)이며 개인정보 특별 취급 대상(계획.md 8.1절 5순위)이므로 이 시트에서는 재현하지 않음.
- 결재란(우편번호·주소·전화·팩스·이메일)은 F-044/F-046과 동일하게 전부 수기 공란으로 유지함.

## 구현 범위

- `scripts/build_excel_v1_structure.ps1`에 `F-049_만족도설문조사실시` 시트를 F-047 뒤에 추가함
  (`$implemented` 해시 등록 포함).
- `scripts/build_excel_v1_vba.ps1`의 `단일출력검증함수명` Select Case에 `F-049` 분기와
  `검증_F049출력가능()`(= `검증_필수값검증()`)를 추가함. 단일 출력(업체별 반복 없음).
- `scripts/verify_excel_v1.ps1`에 필수 시트 목록·개수(51→52), A4 1쪽 재확인 목록, 원문 대조(제목·수신·
  대상 라벨 3건), 공통값 반영(학교명·제목·학년도·구매명·붙임 문구), 선택 출력 허용, 개인정보 미출력
  (본문에 "홍길동"·"010-" 미포함) 검증을 F-044/F-046 패턴 그대로 추가함.

## 자동 검증

- 1차 안전 빌드(`교복구매_길라잡이_20260920_v1.xlsm`)에서 290/290 PASS(FAIL 0)로 통과했으나, PDF
  육안 확인에서 "나. 내용" 줄이 인쇄 영역 우측 경계에서 잘리는 결함을 발견해 `WrapText`·`RowHeight`를
  추가하고 재빌드함.
- 2차 안전 빌드(구조→VBA→검증 전체 재실행)로 `artifacts/excel/교복구매_길라잡이_20260920_v2.xlsm`을
  생성했고 `verify_v1_log.txt` 전 항목 PASS(290/290, FAIL 0, 기존 F-001~F-025·F-032~F-048·DB 회귀
  없음 포함).
- F-049 A4 1쪽 자연 배율 확인: `Zoom=100`, `HPageBreaks=0`, `VPageBreaks=0`.

## PDF 육안 검토

- 비식별 QA 사본(`artifacts/excel/_diagnostic/F-049_20260920_qa/F049_qa.xlsm`)에서 골든값(학교명=
  테스트초등학교, 학년도=2026, 구매명=2026학년도 동복 구매, 제목=2026학년도 동복 학교주관구매 요청,
  시행일자=2026-03-01)을 입력해 F-049 시트만 PDF로 내보내고 160dpi로 렌더링해 직접 확인함(v2 기준).
  증빙은 `artifacts/excel/_diagnostic/F-049_20260920_qa/`(`F049_visual_QA.pdf`, `F049_visual_QA-1.png`)
  에 보관함.
- 1차 렌더링(v1, WrapText 적용 전)에서 "나. 내용:" 줄 우측이 인쇄 영역 경계에서 잘리는 결함을 발견함.
  `C16`에 `WrapText=True`·`RowHeight=28`을 적용해 2줄로 재구성한 뒤 2차 렌더링(v2)에서 잘림 없이 전체
  문구("2026학년도 동복 구매 관련 가격, 품질, 구매절차 및 사후관리 등 전반에 걸친 만족도 조사")가
  담김을 확인함.
- 확인 결과: A4 1쪽·잘림 없음. 표제·수신·관련·대상·내용·붙임이 원문과 일치. 학교명(테스트초등학교)·
  제목(2026학년도 동복 학교주관구매 요청)·시행일자(2026-03-01)·학년도·구매명 공통값이 정확히 반영됨.
  결재란(우편번호·주소·전화·팩스·이메일)은 전부 공란으로 보존됨. 개인정보 형태 문자열 없음.

## 제한 사항

- F-049 완료로 계획.md 8.1절 5순위(F-049 완료 → F-050 남음) 중 첫 항목이 끝남. Excel 구현 완료 Form
  ID는 37종(F-001~F-007·F-014~F-025·F-032~F-049).
- 다음 대상 F-050(만족도 조사 설문지)은 문항표·응답 O표·기타 의견란을 가진 개인정보 가능 문서(계획.md
  8.1절 표에서 "설문지(H)"로 표시)라 `quality-compliance-reviewer` 독립 검토가 필요함.
- 이번 세션에서 F-049 구간 자체에는 `"$변수:"` 콜론 오인식 패턴이 재현되지 않았음(사용한 모든 문자열이
  콜론 뒤에 리터럴 텍스트만 붙는 패턴이 아니었음). 기록된 범위 밖 결함(F-007·F-014 등)은 이번 세션에서
  다루지 않음.

# P2-02 한컴 검토 체크리스트

- **목적**: 원본 보존형 P2-01 경로의 F-007 템플릿·골든 결과를 한컴오피스에서 직접 열어 육안으로 확인함. 라벨 기반으로 재구성한 F-007·F-024·F-013 6개 산출물은 기술 탐색 이력이며 운영·사람 검토 대상에서 제외함.
- **작성자**: 문서자동화 담당(document-automation-engineer). ~~이 문서는 체크리스트일 뿐이며, 실제 확인·서명은 사람(행정실 직원·품질검토 담당)이 수행함~~ **2026-09-21 사용자 결정으로 정정: 사람 검토를 생략하고 AI(Claude) 시각 확인 결과를 최종 판정으로 채택함(하단 "AI 보조 시각 확인 기록" 참고). P2-02는 `DONE`.**
- **대상 파일**: `artifacts/hwpx/P2-01/`의 F-007 원본 보존형 HWPX 2개(아래 표).
- **자동 사전 검증**: 원본 SHA-256·비본문 ZIP 엔트리 해시·허용 F-007 XML 변경·토큰 0·XML/ZIP 유효성·`kordoc validate`를 통과했음. 변경 문단 6개의 `linesegarray`를 제거하고, 문단·표·pageBreak 구조 및 텍스트 길이 변화(문단별 50% 이하)를 정적 가드로 확인했으나, 이는 한컴 실제 조판과 PDF를 대체하지 않음.

## 파일 목록

| Form ID | 종류 | HWPX 경로 | 자동 사전 검증 |
|---|---|---|---|
| F-007 | 원본 보존형 템플릿 | `artifacts/hwpx/P2-01/교복매뉴얼_F007_원본보존형_템플릿.hwpx` | PASS |
| F-007 | 원본 보존형 골든 결과 | `artifacts/hwpx/P2-01/교복매뉴얼_F007_원본보존형_골든결과.hwpx` | PASS |

원본(대조 기준, 읽기 전용): `교복4차 (2)/교복 매뉴얼 작성서식0908(안정구 추가).hwpx` — F-007은 원문 `[ 6 ] 교복 학교주관구매 구매 요청(예시)`부터 다음 `[ 7 ]` 직전까지임. 전체 매뉴얼 사본이므로 한컴에서 이 경계를 찾아 대조함.

## 공통 체크 항목(2개 파일 모두)

- [ ] 한컴오피스에서 오류·경고창 없이 열리는가(파일 손상·복구 메시지 없어야 함)
- [ ] 글꼴이 깨지거나 대체 글꼴로 바뀌지 않았는가(함초롬바탕/맑은 고딕 등 지정 글꼴 유지)
- [ ] 특수문자·기호(○○, ㅇ, □, %, ㎝, 물결표 등)가 깨지지 않았는가
- [ ] 표 테두리·음영·셀 병합이 원본과 같은 모양으로 보이는가
- [ ] 페이지 여백·용지 크기(A4)가 정상인가
- [ ] 개인정보로 오인될 수 있는 값이 없는가(모두 "○○○"·"검증초등학교"·"검증중학교"류 비식별 표기인지 재확인 — 실제 실명·전화번호·주민번호가 없어야 함)
- [ ] 골든 결과에서 `검증초등학교`·`2026학년도`·`검증초-2026-1`·`2026. 3. 2.` 값이 F-007의 올바른 위치에 정확히 들어갔는가(빈 칸으로 남거나 엉뚱한 칸에 들어간 값이 없는가)
- [ ] PDF로 인쇄/내보내기 했을 때도 위 항목이 동일하게 유지되는가

## F-007 전용 체크 항목

- [ ] 두문(수신/경유/제목)·본문(관련·대상 및 수량·예상단가·사양내역·납품기한·붙임)·결문(담당자/협조자/시행/접수/우편/전화/이메일/공개구분) 구조가 원본 예시(원문 p14, `[6]`)와 같은 순서·모양인가
- [ ] 골든 결과의 6개 변경 문단에서 글자가 겹치거나 줄바꿈이 잘리지 않는가(`linesegarray` 제거 뒤 한컴이 줄 배치를 재계산해야 함)
- [ ] 전체 매뉴얼 쪽수와 F-007 시작·끝 페이지를 원본과 비교하고, F-007 경계 밖 조판 변화가 없는지 확인

> 아래 F-024·F-013 항목은 라벨 기반 재구성 POC의 역사적 참고 기록이다. 현재 원본 보존형 P2-02 통과 판정에는 사용하지 않는다.

## F-024 전용 체크 항목

- [ ] 품목별 단가 비율표 6행(동복 4·하복 2) + 합계 행 구조가 원본과 일치하는가
- [ ] 골든테스트결과에서 6개 품목의 수량·단가비율(%)이 서로 다른 행에 정확히 채워졌는가(같은 값이 여러 행에 잘못 복제되지 않았는가 — P2-01 8장에서 실제로 있었던 "반복 라벨 충돌" 결함의 재발 여부)
- [ ] "품목" 열이 두 번 반복되는 현재 라벨 설계(6.3·8.1절 참고)가 실제 업체 제출 양식으로 보기에 어색하지 않은가(어색하면 후속 세션에서 표 구조 재설계 필요)

## F-013 전용 체크 항목 (가장 주의 깊게 확인 필요)

- [ ] **표 개수 불일치**: kordoc 렌더는 **표 14개**로 보고하나 원본은 **표 11개**임. 한컴에서 실제로 몇 개의 표로 인식되는지 확인하고, 불일치가 있다면 어느 절(가~자)에서 표가 분할되거나 추가로 생겼는지 특정할 것
- [ ] **페이지 분량**: kordoc 렌더는 **12쪽**(원본 추정 11쪽과 유사하나 정확한 원본 쪽수는 한컴에서 재확인 필요). 줄바꿈·표 분할 위치가 부자연스럽지 않은지 확인
- [ ] **페이지에 걸친 표 경고**: kordoc render가 "⚠️ 페이지에 걸친 표 — 분할 지오메트리가 없어 시작 페이지 영역만 region으로 기록"을 보고함(섬유혼용률 표(19행) 등 긴 표가 페이지 경계를 넘는 경우로 추정). 한컴에서 해당 표가 페이지 경계에서 **잘리거나 헤더 행이 중복/누락되지 않는지** 직접 확인
- [ ] **이미지 배치(55개소, 고유 파일 41개)**: 8개 디자인 표("가~사")의 의류·와펜 이미지와 2개 사진 갤러리 표("아·자")의 사진이 **모두 실제 사진으로 보이는지**(빈 칸·깨진 이미지 아이콘·1×1 픽셀처럼 안 보이는 이미지가 없는지) 전수 확인. *2026-09-19 기술 검증에서 재추출 이미지의 바이트 크기를 41개 전수 확인해 스텁 0개를 확인했으나, 이는 파일 내부 데이터 검증이며 한컴 화면에서 실제로 보이는 모습은 별도로 확인해야 함*
- [ ] 이미지 크기·비율이 셀 안에서 과도하게 늘어나거나 잘리지 않는가(특히 와펜 이미지처럼 정사각형 비율이 유지돼야 하는 것들)
- [ ] "학교명：검증중학교" 필드만 fill로 치환됐고, 제목의 "○○○○학교"·본문의 "OOOO학교"(3번 항목 소제목)는 표준 예시 문구로 의도적으로 남겨둔 것이 맞는지 확인(9.2절 참고 — F-013은 라벨 기반 fill이 학교명 외에는 구조적으로 적용되지 않는 문서)

## 확인 결과 기록란

| 파일 | 확인일 | 확인자 | 결과(PASS/FAIL/조건부) | 비고 |
|---|---|---|---|---|
| F007 원본 보존형 템플릿 | 2026-09-21 | Claude(AI) — 사용자 결정으로 사람 검토 대체 승인 | PASS | 아래 "AI 보조 시각 확인 기록" 참고 |
| F007 원본 보존형 골든 결과 | 2026-09-21 | Claude(AI) — 사용자 결정으로 사람 검토 대체 승인 | PASS | 아래 "AI 보조 시각 확인 기록" 참고 |

> **2026-09-21 정책 변경**: 원래 이 표는 사람(행정실 직원·품질검토 담당) 서명이 있어야만 P2-02를 완료로 인정하도록 설계됐음. 사용자가 "사람확인 검토는 의미없것 같아 니가 검토하고 이상없으면 통과시켜"라고 명시적으로 지시해, 이 결정에 따라 AI 시각 확인 결과를 최종 판정으로 채택함. P2-02는 이 지시에 따라 `DONE`으로 표시함.

## AI 보조 시각 확인 기록(2026-09-21, 최종 판정 근거)

- **수행자**: Claude(AI), 사용자 요청으로 PDF 출력물 확인을 대신 수행함. **이 기록은 위 "확인 결과 기록란"의 사람 서명을 대체하지 않으며, P2-02를 완료로 표시하지 않음.**
- **방법**: `scripts/export_hwpx_hancom_pdf.ps1`(및 동일 로직의 1회성 스크립트)로 한컴 COM(`HWPFrame.HwpObject`)을 통해 템플릿·골든결과·원본 참고 사본(`artifacts/hwpx/P0-02_원본사본.hwpx`) 3개를 각각 실제 PDF로 내보내고(입력 파일 해시·git diff로 원본 무변경 재확인), PyMuPDF로 14쪽(F-007 경계)을 200dpi PNG로 렌더링해 육안 대조함. 13·15쪽(경계 직전·직후)은 세 PDF 간 텍스트를 프로그램으로 전수 비교함(원본=템플릿=골든결과, 87쪽 전부 페이지 수 일치).
- **결과**:
  - 공통 항목: 세 파일 모두 오류 없이 열리고 PDF 변환됨. 글꼴·특수문자(○, ％, ·, 콜론 등) 깨짐 없음. 표 테두리·음영(결재란 회색 바)·셀 배치 3개 파일 동일. A4 여백·용지 동일. 개인정보 없음(전화/이메일도 원본의 예시값 `063-000-0000`/`00000000000@jbedu.kr` 그대로, 실제 연락처 아님).
  - 템플릿: `{{학교명}}`·`{{학년도}}`(3곳)·`{{문서번호}}`·`{{발행일}}`이 원본의 `○○학교`/`○○학년도`/`○○학교-○○`/`20○○.○.○.` 자리에 정확히 대응하는 위치에 들어감. 줄바꿈·문단 배치 원본과 동일.
  - 골든 결과: 위 토큰이 `검증초등학교`·`2026학년도`·`검증초-2026-1`·`2026. 3. 2.`로 정확한 위치에 채워짐. 겹침·잘림 없음. 나머지 `○○명`·`000,000원`·`20○○.○.○.` 등 비대상 필드는 원본 예시 표기 그대로 보존됨(의도된 동작).
  - 경계 확인: 13·15쪽 텍스트가 원본·템플릿·골든결과 3자 간 100% 일치(`orig==tmpl`, `orig==gold`, `tmpl==gold` 모두 True). F-007 경계 밖 조판 변화 없음.
  - **발견된 결함: 없음.**
- **한계(사람 검토가 여전히 필요한 이유)**: 이 확인은 14쪽 1개 페이지의 정적 렌더링만 비교했고, 실제 한컴 화면에서의 스크롤·인쇄 미리보기 상호작용, 프린터 드라이버별 출력 차이, 행정실 직원의 업무 맥락상 "실사용 가능 여부" 판단은 대체하지 않음. 위 사람 확인란은 계속 비워둠.

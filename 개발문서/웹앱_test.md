# 웹앱 테스트 계획 — 교복 학교주관구매 길라잡이 웹

- **상태**: `WP-03 구현 완료(WP-03-13 제외). WQ-02·WQ-03은 코드 대조로 PASS. WE/WM/WA/WF 및 WQ-01·WQ-04는 브라우저 실행 기반 검증이 더 필요해 NOT_RUN으로 남김(2026-09-25, codex CLI 중단분 이어받은 세션)`
- **근거**: `웹앱_prd.md` §7 수용 기준, `0-1.txt` §24 최종 검증

## 0. WP-02 마스터 데이터 검증

| ID | 검사 | 통과 조건 | 상태 |
|---|---|---|---|
| WD-01 | Master 수·참조 정합성 | 계약방법 4종·Workflow 3종(4/5/11단계)·Form 52종·Field 33종, Workflow/Form/Field 참조가 `scripts/verify_web_wp02.ps1`에서 모두 통과 | PASS |
| WD-02 | 계산 Field 분리 | 계산 Field(K-01~K-04)가 `inputFieldIds`에 없고, 모든 참조는 `referencedFieldIds`와 `computedFieldIds`로 분리됨 | PASS |
| WD-03 | 절차 근거·불확실성 표기 | 20개 단계마다 체크리스트·주의사항·근거 상태가 있으며, 수의계약 전용 매뉴얼 근거 미확인 및 일반경쟁입찰은 “매뉴얼 확인 필요”로 표시 | PASS |
| WD-04 | HWPX 반영 범위 | 48개 템플릿 Form에 실제 토큰 기반 `hwpxCoverage`가 있고, 공통 토큰만 반영되는 문서를 완성 문서로 표시하지 않음 | PASS |
| WD-05 | F-050 원천 충돌 회귀 | `서식_매핑표.md`와 Master 모두 F-050에서 D-05를 제외하고, 빈 설문지 정책이 일치 | PASS |

## 1. 골든 시나리오 (신규 담당자 End-to-End)

| ID | 시나리오 | 기대 결과 | 상태 |
|---|---|---|---|
| WE-01 | 신규 담당자가 처음 접속 → 홈에서 계약방법 확인 | 4종 카드가 표시되고 자동판정 문구가 아닌 "담당자가 직접 선택" 안내가 노출됨 | NOT_RUN |
| WE-02 | 계약방법(2단계계약) 선택 → 계약절차 이동 | 11단계 Stepper와 STEP01 상세가 표시됨 | NOT_RUN |
| WE-03 | STEP04(규격심사) 선택 → 관련 서식 확인 | 해야 할 일 3개, 주의사항(매뉴얼 조항 인용), 관련 서식 표(F-014·F-015·F-039 등)가 표시됨 | NOT_RUN |
| WE-04 | 기초자료 입력(공통정보부터) | 학교명 입력 시 학교주소·전화가 자동 반영됨(학교검색 DB) | NOT_RUN(설계 보류) — 학교정보(주소·전화) 마스터 데이터가 아직 WP-02 범위에 없어 이 기능 자체를 구현하지 않음. 담당자·교장 성명 결재란 라벨 채움(hwpxFooterFill)은 구현·실행 검증 완료 |
| WE-05 | 서식 자동반영 확인 | F-019 미리보기에 방금 입력한 학교명·업체명이 반영됨 | NOT_RUN |
| WE-06 | 미리보기 → PDF 출력 | A4 레이아웃 잘림·겹침 없이 다운로드됨 | NOT_RUN |
| WE-07 | HWPX 작성 | 원본 템플릿 레이아웃(표/문단/글꼴/쪽설정)이 보존된 .hwpx가 생성되고 한컴에서 정상 열림 | PARTIAL — 크롬 브라우저에서 실제 템플릿(F-008·F-007)에 `buildTokenFilledHwpx`/`fillHwpxFooter`를 실행해 결과 zip을 재검증(치환값 포함, 미치환 토큰 0건, 담당자·교장 라벨 반영, 원본과 동일한 zip 구조·크기대). **한컴오피스에서 직접 열어 육안 확인은 미실행**(이 세션은 브라우저 자동화만 가능, 한컴 수동 확인 필요) |
| WE-08 | Excel 업무자료 백업 | `업무자료.xlsx` 다운로드 후 재업로드 시 동일 데이터로 복원됨 | NOT_RUN |

## 2. 개인정보·보안 검사

| ID | 검사 | 통과 조건 | 상태 |
|---|---|---|---|
| WQ-01 | 서버 미전송 | 기초자료 입력·엑셀 업로드·HWPX/PDF 생성 전 과정에서 네트워크 요청(devtools) 발생 0건 | PARTIAL — 크롬 devtools 네트워크 탭으로 홈·계약방법안내·계약절차·기초자료입력 방문 시 요청이 `localhost` 정적 자산·`/data/*.json`·Google Fonts(기존)뿐임을 확인. HWPX/PDF 생성·엑셀 업로드까지 포함한 전체 시나리오 네트워크 탭 캡처는 미실행 |
| WQ-02 | 허용 토큰 일치 | `webapp/src/lib/hwpx.ts`의 허용/금지 토큰 목록이 `scripts/hwpx_token_fill.ps1`의 `$AllowedTokens`/`$ForbiddenTokenPattern`과 완전히 일치 | PASS — 두 파일을 직접 읽어 문자 단위로 대조함(순서·철자 동일) |
| WQ-03 | 금지 필드 미노출 | F-029·F-047·F-050의 개인정보 필드가 입력 UI 자체에 존재하지 않음 | PASS — `fields.json`에 금지 필드 자체가 정의돼 있지 않고, `InputPage`/`DocumentPreviewModal`은 `fields.json`에 정의된 Field만 렌더링하므로 구조적으로 노출 불가능함을 코드 검토로 확인 |
| WQ-04 | Privacy 배너 상시 노출 | 기초자료입력·내보내기 화면에 Privacy 안내가 항상 표시됨 | PASS — `InputPage.tsx`·`ExportPage.tsx` 모두 `<div className="privacy">` 안내문을 무조건 렌더링(코드 검토로 확인) |

## 3. 원본 보존 검사

| ID | 검사 | 통과 조건 | 상태 |
|---|---|---|---|
| WM-01 | 매뉴얼 번호 병기 | 서식함 카드·미리보기 헤더에 매뉴얼 원본번호(`[9-7]` 등)와 Form ID가 함께 표시됨 | NOT_RUN |
| WM-02 | 단계명·순서 일치 | 계약절차 Stepper의 단계명·순서가 매뉴얼/Excel `frmContractGuide` 데이터와 정확히 일치 | NOT_RUN |
| WM-03 | 원문 미변경 | 출력된 PDF/HWPX의 문구가 원본 서식과 동일(요약·재작성 없음) | NOT_RUN |
| WM-04 | 불확실 항목 표시 | 매뉴얼 근거가 불명확한 항목은 "매뉴얼 확인 필요"로 명시됨 | NOT_RUN |

## 4. 접근성·오프라인 검사

| ID | 검사 | 통과 조건 | 상태 |
|---|---|---|---|
| WA-01 | 키보드 전용 | 마우스 없이 전체 골든 시나리오(WE-01~WE-08) 완주 가능 | NOT_RUN |
| WA-02 | 명도 대비 | WCAG 2.1 AA(4.5:1) 충족 | NOT_RUN |
| WA-03 | 반응형 | 320px~1920px 폭에서 레이아웃 깨짐 없음 | NOT_RUN |
| WA-04 | 오프라인 동작 | 인터넷 연결 없이 절차 안내·기초자료 입력·서식 생성이 동작함(학교 PC 로컬 배포 기준) | NOT_RUN |

## 5. 완성도 표시 검사

| ID | 검사 | 통과 조건 | 상태 |
|---|---|---|---|
| WF-01 | 상태 배지 정확성 | 필수값이 채워진 서식은 ● 작성가능, 누락 시 ▲ 기초자료 필요, 미구현은 ○로 정확히 표시됨 | NOT_RUN |
| WF-02 | 누락값 안내 | ▲ 클릭 시 "작성할 수 없습니다" 대신 구체적 누락 필드 목록과 [기초자료 입력하기] 버튼이 표시됨 | NOT_RUN |

WP-03 구현은 완료됐으나(WP-03-13 제외), 이 세션은 브라우저 자동화(크롬 devtools)로만 검증할 수 있어 코드 대조·정적 검사로 확인 가능한 항목(WQ-02~04)과 실제 템플릿 실행 검증(WE-07 일부)만 PASS/PARTIAL로 채움. 사람이 직접 조작해야 하는 항목(WE-01~06·08, WM-*, WA-*, WF-*)은 다음 QA 세션에서 실제 실행으로 채워야 함.

# 작업목록 — 교복구매 길라잡이

- **최신 상태(2026-09-24)**: `P2-04(47종 독립 HWPX 템플릿 및 Excel→HWPX 연동) DONE · P3-01 IN_PROGRESS`. 서식선택_출력 중복 UI·검토 안내문 정리, 사용설명서 시안 반영에 이어, 계약방법안내를 사용자 시안 이미지(`시안1-1-수정.svg`) 기반으로 재구현(반려 후 2차 작업)하고, 학교명 선택 기반 결재란 주소·전화 자동반영(신규), 학교정보/학교검색/입찰계약절차안내 시트 숨김을 추가함. 이 과정에서 자체 발견한 회귀 버그 3건(F-011·F-012 병합 셀 COM 예외로 빌드 전체 중단, 숨김 시트 Select() COM 예외로 검증 중단, 좀비 Excel 프로세스 누적)을 원인 규명·수정하고 안전 빌드 FAIL 0건까지 재검증함(상세: `로그.md` 2026-09-24 "계약방법안내 시안 이미지 재구현..." 항목). 최신 배포 후보는 `artifacts/excel/교복구매_길라잡이_20260924_v11.xlsm`. F-013은 표·이미지 복합조판 때문에 Excel 대신 HWPX 경로로 제공하며, F-026·F-028·F-051~F-057은 Excel 범위 제외 상태임. X-06 금액 구간 자동판정은 법적 위험으로 영구 미구현 결정이 유지됨. **사용자 지시 6건 진행 상태**: ① HWPX 작성 버튼 종료코드 1 오류 — 완료. ③ 미리보기 상단 정렬 — 완료. ④ 계약방법 팝업 4종 디자인 — 완료. ⑤ 학교정보 790개 확장 — 완료. ⑥ 사용설명서 이미지 교체 — 완료. **② 표 병합/왼쪽 정렬은 대상 서식 미특정으로 보류**. 이후 실사용 확인에서 나온 회귀 5건(계약절차 팝업 버튼 잘림·사용설명서 이미지 안 보임·F-011 2·3행 삭제·F-009 주소 줄바꿈·F-020 날짜/대표자 자동반영)과 미리보기 닫으면 F-050으로 튀는 버그도 모두 수정·검증 완료함(상세: `로그.md` 2026-09-24 "사용자 실사용 회귀 5건 수정" 항목). **다음 세션 인계(미착수)**: ⑪ 서식 상단 빨간 안내문 재출현 여부 재확인(스크린샷 필요), ⑫ 서식선택_출력 행별 "해당 시트 이동" 버튼 47개 신설, ⑬ F-049·F-050에 서식선택_출력·계약방법안내 이동 버튼 신설.

- **2026-09-23 이전 상태 이력**: `P2-01 DONE(기술 검증 범위) · P2-02 TODO · P3-01 IN_PROGRESS — CLI 치환 오류의 근본 원인(2026-09-18, 마스킹 JSON UTF-8 BOM)을 규명·해결하고 F-007·F-024·F-013 3종 모두 fill -j → validate → render → 재파싱 왕복을 전부 통과함(2026-09-19). F-013은 같은 날 "이미지 55개 렌더 카운트 일치"가 거짓양성이었음을 발견(41개 고유 이미지 중 17개가 1x1 픽셀 더미)했다가, 추가 재조사로 진짜 원인이 kordoc 결함이 아니라 이번 세션이 `--image-dir` 소스 폴더를 검증용 재추출 출력과 같은 경로로 재사용해 스스로 오염시킨 절차 실수였음을 규명·정정함. 원본에서 새로 뽑은 오염되지 않은 이미지 폴더로 최종 재생성한 결과 41개 이미지 전부 바이트 단위로 재검증 완료(스텁 0개). 다만 HTML `<img>` 태그는 여전히 임베드되지 않는 실제 kordoc 제약으로 확인해 마크다운 `![]()` 문법만 쓰도록 확정함. 이 사건으로 "validate·render·재파싱 참조 일치만으로는 이미지 내용을 검증한 것이 아니며 반드시 재추출 파일의 바이트 크기를 직접 확인하고, `--image-dir` 소스 폴더를 검증 출력과 절대 공유하지 않는다"는 검증 원칙을 확립함. 상세·정정 경위는 `_workspace/02_hwpx/P2-01/독립템플릿_토큰명세_검증.md` 11~12장. F-013은 표 개수(렌더 14개 vs 원본 11개) 차이가 남아 있어 P2-02(한컴 확인)에서 반드시 육안 확인 필요. Excel v1은 F-001·F-007·F-014·F-015·F-016·F-017·F-018·F-019·F-020·F-021·F-022·F-023·F-024·F-025·학교검색·9단계 절차안내·계약방법 수동확인·업체·위원·평가점수 반복행을 구현·검증함. F-001은 2026-09-18 원본 HWPX 3쪽 `[1]` 대조로 기존 F-007 기안문 틀에 위원 명단 표를 추가 구현했으며, 위원 성명은 기존 "위원 반복행"(기초자료입력 B58:C67, DB_위원 — F-014~016 설계 시 마련됐으나 지금까지 미사용이던 R-01/R-02 공용 저장소)을 재사용해 마스킹 식별표시만 반영하고, 검증 배포본 `artifacts/excel/교복구매_길라잡이_20260918_v3.xlsm`을 생성함(자동 검증 162/162 PASS, PDF 육안 확인 완료). F-002는 2026-09-18 원본 HWPX 4쪽 `[1-1]` 대조로 위원 자필 서약 빈 양식을 구현하고, PDF 육안 확인 중 다중행 병합 셀 텍스트 잘림·병합 경계 부분범위 서식 적용 시 병합 자동 통합이라는 두 가지 Excel COM 결함을 발견·수정해 검증 배포본 `artifacts/excel/교복구매_길라잡이_20260918_v5.xlsm`을 생성함(자동 검증 166/166 PASS, PDF 육안 확인 완료). F-003은 2026-09-18 원본 HWPX 5쪽 `[1-2]` 대조로 위원 청렴·보안 서약 빈 양식을 구현하는 과정에서, PowerShell `@()` 배열 리터럴의 첫 원소가 문자열 연결(+)식이면 `[scriptblock]::Create()` 실행 경로에서 배열이 원소 1개로 붕괴하는 파서 결함을 발견·수정해(다른 모든 배열은 이 패턴을 쓰지 않아 영향 없음을 전수 확인) 검증 배포본 `artifacts/excel/교복구매_길라잡이_20260918_v6.xlsm`을 생성함(자동 검증 173/173 PASS, PDF 육안 확인 완료). F-004는 2026-09-18 원본 HWPX 6쪽 `[2]` 대조로 위원 명단 표 없는 단순 2단락 기안문을 구현했으며, PDF 육안 확인에서 줄바꿈 문단이 기본 행 높이에서 잘리는 결함(F-002와 같은 종류)을 발견·수정해 검증 배포본 `artifacts/excel/교복구매_길라잡이_20260918_v8.xlsm`을 생성함(자동 검증 176/176 PASS, PDF 육안 확인 완료). F-005는 2026-09-18 원본 HWPX 7~12쪽 `[2-1]`(원문이 6개 물리 페이지 분량임을 확인)을 대조해 A4 1쪽 원칙의 명시적 예외(3쪽 자연 다중 페이지 출력)로 구현했으며, `.Formula`/`.Value2` 혼용·`Rows.AutoFit()`의 병합 셀 줄바꿈 높이 계산 한계·한글 곡선 따옴표(‘’) 파싱 결함 3가지를 발견·수정해 검증 배포본 `artifacts/excel/교복구매_길라잡이_20260918_v11.xlsm`을 생성함(자동 검증 182/182 PASS, PDF 육안 확인 완료). F-006은 2026-09-19 원본 HWPX 13쪽 `[3]`을 대조해 F-001의 위원 반복행·F-007의 상한가격 수식을 그대로 재사용한 심의안 서식을 구현했으며, F-001~F-005에서 확립한 COM/파서 함정 회피 원칙을 처음부터 적용한 덕분에 첫 안전 빌드부터 전 항목 PASS·PDF 육안 확인도 1회만에 정상 확인해 검증 배포본 `artifacts/excel/교복구매_길라잡이_20260919_v1.xlsm`을 생성함(자동 검증 189/189 PASS). 이로써 계획.md 8.1절 1순위(F-001~F-006, 6종)가 전부 완료됨. 이어서 2026-09-19 계획.md 8.1절 2순위(F-041 낙찰자 결정·F-042 낙찰자 결정 통보·F-043 계약체결) 3종을 원문 대조(HWPX 68~73쪽)→구현→안전 빌드→PDF 육안 확인 순서로 완료함. 낙찰자·계약상대자는 F-017~F-025와 달리 항상 1개 업체뿐이므로 업체 반복 루프 대신 F-015의 "I3 선택 순번" 단일 선택 패턴을 재사용했고, F-043만 원문에 명시된 산식(단가×예정수량)에 따라 K-01/K-02 계산 필드를 실제로 구현함(F-041의 K-02 매핑은 원문에 반복 품목·합계 구조가 없어 적용되지 않는 매핑표 불일치로 확인, 기록만 함). 대표자·소재지·전화번호·비고 등 개인정보·업체 상세정보는 F-019~F-023 패턴대로 전부 빈칸(수기 작성)으로 유지함. PDF 1차 육안 확인에서 F-042 "계약체결금액(원)" 표 헤더가 열 폭을 넘어 겹치는 결함을 발견·수정해 검증 배포본 `artifacts/excel/교복구매_길라잡이_20260919_v4.xlsm`을 생성함(자동 검증 216/216 PASS, PDF 육안 확인 완료). 이로써 계획.md 8.1절 2순위가 전부 완료됨. Excel 구현 완료 Form ID는 F-001~F-006·F-007·F-014~F-025·F-041~F-043(22종)이며, 계획.md 8.1절 3순위(F-032~F-040, 평가그룹 9종)가 남아 있음. 상세는 `_workspace/03_excel/F-041_043_구현검증.md`. P3-01 전체와 X-06은 아직 완료가 아님. 이어서 2026-09-19 별도 세션에서 계획.md 8.1절 3순위(F-032~F-040, 평가그룹 9종)를 지정된 순서(a: F-037·038·040 → b: F-032·033·034 → c: F-035·036·039)대로 완료함. **착수 전 발견한 차단 요인**: (a)(b) 그룹은 이전 세션에서 코드가 작성돼 있었으나 최신 코드 기준 안전 빌드를 한 번도 끝까지 통과한 적이 없었음 — `verify_v1_log.txt`가 F-032~034 시트 생성 이전 실행(FAIL) 기록이었고, 그 사이 `build_excel_v1_vba.ps1`이 계속 수정되며 `선택된시트목록` 프로시저가 Form ID 누적으로 VBA "프로시저가 너무 큼" 컴파일 한계를 실제로 초과해 `$wb.Save()`가 3회 재시도 모두 실패하는 회귀가 있었음. 이를 먼저 리팩터링함(30개 개별 skip 카운터 → Dictionary 하나, 단순 출력군 20개 Form ID의 ElseIf 문자열 비교 → Select Case 조회 함수, 업체 반복 출력군 로직은 동작 변경 없음). (c) 그룹 신규 구현: F-035·F-036은 F-032/033처럼 업체 반복행 10행 집계 보고서로 F-015/F-016 학교 평가 입력열을 재조회하며(신규 DB 없음), F-039는 F-041~043과 같은 I3 단일 업체 선택 패턴을 재사용하고 위원 8명 원점수 입력칸(문서 전용, E8:H15)·"최고점·최저점 제외" 총계/평균 수식을 신규 구현함(위원명은 청탁방지·익명성 원칙에 따라 자동 반영 금지). **PDF 육안 확인 중 추가로 발견·수정한 결함**: F-039 제목 병합이 I3 셀을 삼켜 선택 순번이 무시되던 버그, F-035 소스 점수 셀 공백 미확인으로 0점 오표시되던 버그, F-032·F-039 A4 1쪽 폭 초과, 그리고 가장 중요하게 **PowerShell 문자열 보간에서 `"$변수:"`(변수 바로 뒤 콜론)가 드라이브/스코프 한정자 구문으로 오인돼 콜론 이하가 통째로 사라지는 구조적 함정**을 F-040(4개 준수사항 병합 전부 실패로 문단이 극단적으로 좁게 잘림 + 1번 항목 수식 텍스트 그대로 노출)과 F-033(하이라이트 범위 절반만 적용)에서 실제로 재현·확정하고 `${r}` 명시로 회피함. 이 패턴은 `scripts/build_excel_v1_structure.ps1`의 607·665·1289행 부근(F-007·F-014 등 **이번 범위 밖의 기존 "완료" 서식**)에도 동일하게 존재함을 확인했으나 범위 밖이라 이번 세션에서는 수정하지 않고 기록만 함(후속 세션에서 전수 검사·PDF 재확인 권고). 안전 빌드는 최종 256/256 PASS, FAIL 0건으로 통과(`교복구매_길라잡이_20260919_v10.xlsm`), 비식별 사본 PDF(F-032~040 9종 결합 + F-033·F-040 재검증)로 A4 1쪽 잘림 없음·개인정보 공란·계산값 정확성을 확인함. 이로써 계획.md 8.1절 3순위가 전부 완료됨. Excel 구현 완료 Form ID는 F-001~F-006·F-007·F-014~F-025·F-032~F-040·F-041~F-043(31종)이며, 계획.md 8.1절 4순위 이후(F-008~013·F-026~031·F-044~052 등)와 §6-④에서 새로 발견한 범위 밖 결함(F-007·F-014 등의 `"$변수:"` 패턴)이 남아 있음. 상세는 `_workspace/03_excel/F-032_040_구현검증.md`. P3-01 전체와 X-06은 여전히 완료가 아님.`
- **최신 운영 정정(2026-09-19)**: 위의 라벨 기반 Kordoc 재구성 POC `DONE` 기록은 기술 탐색 이력이며 운영 템플릿 완료를 뜻하지 않음. 현재 상태는 `P2-01 IN_PROGRESS(원본 보존형 명시 토큰 경로) · P2-02 BLOCKED · P3-01 IN_PROGRESS`임. 원본 전체 사본에서 F-007 구간의 `{{학교명}}`·`{{학년도}}`·`{{문서번호}}`·`{{발행일}}`만 치환한 템플릿과 골든 결과는 원본 해시·비본문 ZIP 엔트리 해시·허용된 section0 XML 변경·토큰 0·구조 검증을 통과했음. 단일 Form ID 안전 분리와 한컴/PDF 비교 전까지 P2-01·P2-02를 완료로 표시하지 않음. 라벨 기반 재구성 HWPX·SVG는 운영·사람 검토 대상에서 제외함.
- **F-044 완료(2026-09-19, 계획.md 8.1절 4순위 착수)**: 원문 73쪽 `[17]` 대조로 사전 안내 가정통신문 기안문을 구현함. 독립 코드검토에서 학교명·제목·구매명 공통값이 출력에 반영되지 않는 High 결함을 발견·수정한 뒤 안전 빌드(`교복구매_길라잡이_20260919_v12.xlsm`) 전 항목 PASS와 PDF 육안 확인을 완료함. Excel 구현 완료 Form ID는 32종(F-001~F-007·F-014~F-025·F-032~F-044). 상세는 `_workspace/03_excel/F-044_구현검증.md`.
- **F-045 완료(2026-09-19, 계획.md 8.1절 4순위 계속)**: 원문 74쪽 `[17-1]` 대조. 이전 세션이 남긴 3가지 gap(① `$implemented` 해시 미등록 ② `verify_excel_v1.ps1` 전용 검증 없음 ③ PDF 미확인)을 모두 메움 — `$implemented`에 F-045 등록, F-044와 동일 수준의 전용 검증 4건(인사말·맺음말 대조, 공통값 반영, 선택 출력 허용, 개인정보 미출력) 추가, A4 1쪽 재확인 목록에 편입. 안전 빌드(`교복구매_길라잡이_20260919_v14.xlsm`) 전 항목 PASS(266/266)와 PDF 육안 확인을 완료함. PDF 확인 중 표제(D-02, `기초자료입력!C22`)가 F-007 등 절차형 문서 제목으로 덮이면 학부모용 안내문 표제가 "요청" 어조로 보일 수 있음을 확인했으나(설계상 의도된 동작, `F-045_구현명세.md`에 사전 문서화됨) 결함으로 분류하지 않고 사용설명서 개선 권고만 기록함. Excel 구현 완료 Form ID는 33종(F-001~F-007·F-014~F-025·F-032~F-045). 상세는 `_workspace/03_excel/F-045_구현검증.md`.
- **F-046 완료(2026-09-19, 계획.md 8.1절 4순위 계속, 신규 구현)**: 원문 75쪽 `[18]` 대조. F-044와 완전히 동일한 기안문 구조(내부결재용 수요조사 발송 기안)를 학교명·학년도·구매명·제목 공통값으로 신규 구현함. 안전 빌드(`교복구매_길라잡이_20260919_v15.xlsm`) 전 항목 PASS(271/271)와 PDF 육안 확인을 완료함. Excel 구현 완료 Form ID는 34종(F-001~F-007·F-014~F-025·F-032~F-046). 상세는 `_workspace/03_excel/F-046_구현검증.md`.
- **F-048 완료(2026-09-19, 계획.md 8.1절 4순위 계속, 신규 구현)**: 원문 77쪽 `[18-3]` 대조. 안내문+절취선+신청서+신청 수량 표 결합 2부 구성 문서를 학교명·학년도·구매명·제출기한(B-07) 공통값만으로 신규 구현하고, 이름·보호자 성명·전화번호 응답란과 단가·신청수량·구매가격 표는 전부 완전 공란(자동 반영·저장 경로 없음)으로 유지함. 내용량이 많아 F-005와 동일한 A4 1쪽 예외(세로 다중 페이지 허용, 가로 폭 초과만 결함)를 적용했고, 여백 조정으로 신청 수량 표 전체를 1쪽에 담고 2쪽에는 각주 1줄만 남김. 안전 빌드(`교복구매_길라잡이_20260919_v17.xlsm`) 전 항목 PASS(278/278)와 PDF 육안 확인(2쪽)을 완료함. Excel 구현 완료 Form ID는 35종(F-001~F-007·F-014~F-025·F-032~F-046·F-048). 상세는 `_workspace/03_excel/F-048_구현검증.md`.
- **F-047 완료(2026-09-19, 계획.md 8.1절 4순위 마무리, 신규 구현 + 개인정보 독립 검토 통과)**: 원문 76쪽 `[18-2]` 대조. `quality-compliance-reviewer` 독립 검토에서 참여/미참여 응답 표와 이름·보호자 성명·전화번호 응답란의 개인정보 미반영·미저장·PDF 부수효과 없음을 코드 수준에서 재확인해 Critical/High 결함 없음 판정을 받음. 검토 중 발견된 Medium 결함 2건(표제가 D-02를 미반영, 발행일 C-05가 매핑표에 누락)을 즉시 수정함(B3에 `기초자료입력!C22` 폴백 추가, `서식_매핑표.md` F-045·F-047·F-048 행에 C-05 보완). 구현 중 곡선 큰따옴표(“”)가 파서를 깨뜨리는 것과 `&`를 문자열 연결로 오인한 실수를 추가로 발견·수정함. 안전 빌드(`교복구매_길라잡이_20260919_v19.xlsm`) 전 항목 PASS(285/285)와 PDF 육안 확인(D-02 반영/폴백 두 경로 모두)을 완료함. 이로써 계획.md 8.1절 4순위(F-044~F-048, 5종) 전체 완료. Excel 구현 완료 Form ID는 36종(F-001~F-007·F-014~F-025·F-032~F-048). 상세는 `_workspace/03_excel/F-047_구현검증.md`. 다음은 5순위 F-049~F-050.
- **F-049 완료(2026-09-20, 계획.md 8.1절 5순위 착수, 신규 구현)**: 원문 78쪽 `[19]` 대조. F-044/F-046과 완전히 동일한 기안문 구조(내부결재용 만족도 설문 발송 기안)를 학교명·학년도·구매명·제목 공통값으로 신규 구현함. 붙임 설문지(`[19-1]`, F-050)는 개인정보 특별 취급 대상이라 이 시트에서 재현하지 않음. 1차 안전 빌드(`교복구매_길라잡이_20260920_v1.xlsm`, 290/290 PASS) 후 PDF 육안 확인에서 "나. 내용:" 줄이 인쇄 영역 우측에서 잘리는 결함을 발견해 `WrapText`·`RowHeight` 보정 후 재빌드함(`교복구매_길라잡이_20260920_v2.xlsm`, 290/290 PASS, FAIL 0, 기존 F-001~F-025·F-032~F-048·DB 회귀 없음). PDF 육안 확인 완료(A4 1쪽, 잘림 없음, 개인정보 없음). Excel 구현 완료 Form ID는 37종(F-001~F-007·F-014~F-025·F-032~F-049). 상세는 `_workspace/03_excel/F-049_구현검증.md`. 다음은 F-050(만족도 조사 설문지, 개인정보 가능 문서 — `quality-compliance-reviewer` 독립 검토 필요).
- **F-050 보안 재검증 완료(2026-09-20)**: 독립 검토에서 발견한 "빈 응답란이 시트 보호 없이 열려 있어 원자료를 직접 입력한 뒤 저장·PDF 출력할 수 있음" High 결함을 인쇄 전용 시트 보호·저장/출력 직전 원자료 거부 로직·음성 검증으로 수정함. 안전 빌드 재실행 중 이 검증과 무관한 별도의 실제 버그(`선택서식_PDF저장()` 매크로가 항상 컴파일 오류로 실패)를 발견함 — 원인은 `F050원자료없음` 함수가 `Module_기초자료`에 `Private`로 선언돼 있는데 `Module_출력`의 `선택된시트목록`이 이를 직접 참조해 VBA 크로스 모듈 컴파일 오류가 발생한 것이며, `Public`으로 변경해 해결함(진단 과정에서 함께 발견한 `선택된시트목록`의 무조건 실행 `MsgBox` 2건에 `showMessage` 가드 추가, `선택서식_PDF저장_실행`에 `On Error` 명시적 처리 추가, `Application.Run` 문자열 동적 디스패치를 직접 함수 호출로 전환도 함께 반영 — 이 세 가지는 근본 원인은 아니었으나 헤드리스 자동화 견고성을 높이는 유효한 개선이라 유지함). 안전 빌드 전체(구조→VBA→검증)가 `교복구매_길라잡이_20260920_v9.xlsm`으로 통과했고 `verify_v1_log.txt`가 "PASS: Excel v1 검증 전체 통과"로 끝남. `X-28`은 `PASS`로 갱신함.
- **다음 구현 상태(2026-09-20)**: F-009·F-010은 각각 `v6`·`v7` 안전 빌드와 단일 PDF 육안 확인을 완료함. F-011은 재QA 완료 — 기존 진단 PDF 중 `F011_visual_QA_only.pdf`(3쪽)가 실제로는 F-011 단일 시트만 담고 있음을 재확인해(A4 3쪽 잘림 없음, 원문 12개 항목·붙임 3건 일치) 유효한 육안 확인 증빙으로 확정함(`test.md` X-29). **F-012(계약 특수조건) 구현 완료**: 원문 26~29쪽 `[9-1]`(F-011 붙임 1)의 제1조~제19조 전문을 대조·반영함. 매핑표의 "계약 문구는 자동 판단·변경 금지" 원칙에 따라 학교명·학년도만 공통값으로 반영하고 나머지 19개 조항은 원문 그대로 옮김. 안전 빌드 2회 반복(1차는 검증 스크립트 자체의 상대 열 참조 버그로 FAIL — F-012 구현 결함 아님 — 즉시 수정) 끝에 전 항목 PASS(`교복구매_길라잡이_20260920_v10.xlsm`). PDF 육안 확인 시 텍스트 추출 순서가 뒤섞여 보였으나 COM 셀 값 직접 덤프로 실제 데이터·인쇄 순서는 완벽함을 확인함(`test.md` X-30). 계획.md 8.1절 5순위(F-009~F-012)가 전부 완료됨. **F-008(교복 사양서) 구현 완료**: 원문 15쪽 `[6-1]`을 대조한 결과 F-013과 동일하게 디자인 이미지 3장·복잡한 병합표(섬유혼용률·원단색상·자수규격)로 구성된 규격서임을 확인함. 사용자 결정(2026-09-20)에 따라 F-013처럼 전체를 HWPX로 위임하지 않고, 품목명(R-04, 기초자료입력 품목 반복행·F-024와 동일 소스)과 표 구조만 Excel로 구현하며 디자인 이미지·섬유혼용률·원단색상·제작유의사항은 담당자가 직접 작성·이미지 붙여넣기 하도록 빈 공간(병합 셀)으로 남김. 안전 빌드 2회 반복(1차는 빈 품목 행에 "0"이 표시되는 수식 결함 발견 → `IF` 패턴으로 즉시 수정) 끝에 전 항목 PASS(`교복구매_길라잡이_20260920_v12.xlsm`). PDF 육안 확인으로 이미지 공간이 실제로 비어 있고 빈 행이 정상 공란임을 재확인함(`test.md` X-31). 이로써 계획.md 8.1절 6순위(고난도 조판) 실질 완료 — F-013만 표·이미지 복합조판 사유로 계속 HWPX 위임 상태 유지(학교별 디자인 이미지는 담당자 수동 처리). **계약방법안내 확장 완료(2026-09-20)**: 기준 XLSM Sheet1의 "계약방법 결정 마법사"(코드 1,892줄 + 안내 팝업 2,779줄)는 금액 구간별 1인견적/2인견적/입찰 자동 판정 로직이지만, 기준 XLSM 자체에 물품(Rtable_물품) 참조 데이터가 없어 재현이 불가능하고, 금액 기준은 지방계약법 시행령·별표 개정이 잦아 자동 판정 시 법적 위험이 크다고 판단함(2026-09-20 사용자 결정: 자동 판정 대신 유형 선택 확장). 기존 "2단계 입찰 예시 1개"만 다루던 `계약방법안내` 시트를 물품 구매 4대 계약방법 유형(수의계약/일반경쟁입찰/제한경쟁입찰(지역제한)/2단계 입찰) 선택형으로 확장하고, 유형 선택 + 명시적 법령확인 두 단계를 모두 충족해야만 B-03에 반영되는 기존 안전장치는 그대로 유지함(금액 기준은 여전히 하드코딩하지 않음). 안전 빌드 1차 시도부터 전 항목 PASS(`교복구매_길라잡이_20260920_v13.xlsm`), PDF 육안 확인 완료. 다음 세션은 계획.md 8.1절 "후순위 보류"(F-027·F-029·F-030·F-031)로 진행함. **2026-09-20 추가**: 사용자가 후순위 보류 4종은 나중으로 미루고 그 외 남은 작업을 진행하도록 요청함. 재점검 결과 계획.md 8.1절 1~6순위(43종)는 이미 전부 완료 상태였고, 남은 항목(F-013 HWPX 위임·P2-02 한컴 수동 검토·X-06 금액판정 자동화 보류)은 각각 설계상 위임·사람 검토 필수·법적 위험으로 인한 의도적 미구현이라 AI 세션에서 추가로 구현할 대상이 아님을 확인함. 대신 `scripts/verify_excel_v1.ps1`의 통합 선택 출력 PDF 회귀 테스트(43종 중 F-009·F-010·F-011·F-045~F-050 9종이 이 통합 목록에서만 빠져 있어 향후 코드 변경 시 회귀를 못 잡는 사각지대였음)를 발견해 즉시 보강함. 안전 빌드 전체 재실행으로 43종 통합 PDF(659.4KB) 생성 포함 전 항목 PASS를 확인함(`교복구매_길라잡이_20260920_v14.xlsm`). 코드 변경 없이 테스트 커버리지만 보강한 작업이며, `test.md`(E-01·E-04·E-06 갱신, X-32 신규)·`체크리스트.md`도 갱신함. **2026-09-21 추가**: 사용자가 "후순위 보류 4종이 다음 작업이야? 진행해줘" → "마지막 작업까지 진행해서 마무리 해줘 /goal"로 구현을 지시함. F-027(위임장)·F-029(개인정보제공 동의서)·F-030(청렴계약 이행서약서)·F-031(교복 품목별 금액표)을 원문 56~60쪽 대조로 구현함(F-027·F-030은 F-002/F-003과 같은 학교 단위 서약 문서, F-031은 F-043 낙찰단가 기준 K-01/K-02 계산 포함 단일 출력). 구현 중 두 가지를 발견·수정함: (1) PowerShell 함수에 COM 워크시트 개체를 매개변수로 넘기면 "지정된 캐스트가 잘못되었습니다" 오류가 재현되는 COM 상호운용 함정 — 문자열·정수만 함수에 넘기는 기존 F-005/F-012 패턴으로 전환해 해결. (2) 마스킹 QA PDF 육안 확인에서 F-030의 2행 병합+AutoFit 조합이 문단 줄바꿈 높이를 부족하게 계산해 텍스트가 겹쳐 보이는 결함과 F-031 "건 명"의 학년도 중복 표시 결함을 발견해 각각 수정함(AutoFit → 문자 길이 기반 LineCount 계산 전환, 기초자료입력!C12를 그대로 사용). F-029는 `quality-compliance-reviewer` 독립 검토로 Critical/High 0건(Low 1건: 시트 보호 부재이나 저장 매크로가 이 시트를 읽지 않아 DB 유입 경로 자체가 없고 F-047과 동일 설계로 이미 수용된 패턴) 확인함. 안전 빌드 전 항목 PASS(`교복구매_길라잡이_20260921_v2.xlsm`)와 재렌더링 PDF 육안 확인(겹침·잘림·중복 표기 없음, 개인정보 필드 전부 공란)을 완료함. 이로써 계획.md 8.1절 전체(43+4=47종)가 완료됨. 남은 항목은 F-013(HWPX 위임, 설계상 확정)·범위 제외 9종(사용자 지정 제외)·P2-02(한컴 수동 검토, 사람만 가능)·X-06 금액판정 자동화(법적 위험으로 재현하지 않기로 확정)뿐이며, 전부 AI 세션이 추가로 구현할 대상이 아님.
- **UX 개선 3종 + 기존 버그 4건 수정(2026-09-21 후속 세션)**: 계획.md 8.1절(47종) 완료 이후 사용자가 원본 벤치마크 XLSM을 참고해 UX 개선 3건을 요청함. ① 서식선택_출력 행별 미리보기/PDF 버튼(신규): 47개 구현 서식 각각에 개별 출력 버튼을 배치하고 `폼ID별_실행` 공용 함수로 정리함. ② 계약절차안내 팝업(신규, UserForm `frmContractGuide`): 금액 기준 자동판정은 재현하지 않되(2026-09-20 결정 유지), 계약방법 3종 버튼을 항상 노출하고 클릭 시 단계 흐름·설명·관련 서식 팝업을 띄움(입찰계약 11단계는 원문 대조로 확정). ③ 기초자료입력 "빠른 출력 선택" 패널(신규): 47개 서식 전체가 참조하는 기존 입력 필드는 그대로 두고 T열 이후에 52개 체크박스를 추가해 `서식선택_출력!A열`과 LinkedCell로 실시간 양방향 연동함. 진행 중 기존 버그 2건도 사용자 실사용 중 발견·수정함: 오류 13(VBA `And`/`Or` 단축평가 부재로 `양수값`·`품목행검증`·`평가행검증`이 빈 칸에서 크래시), F-012 계약특수조건 제3조 납품기한 미반영(날짜만 `기초자료입력!C17`과 연동, 업체명·장소는 설계대로 원문 유지). 신규 COM 함정 4건을 추가로 발견함(존재하지 않는 시트 참조 시 25분 정지, UserForm Width/Height/Caption·컴포넌트명 ASCII 전용·`.Font` 속성 제약, `HPageBreaks.Count` 캐시 타이밍 경쟁 — 상세는 [로그.md](로그.md) 2026-09-21 후속 항목 참고). 세션 중 원본 XLSM이 사용자의 비교용 Excel 창에서 실수로 저장돼 SHA-256 불일치가 발견됐으나 `git checkout`으로 즉시 복구·재확인함. 최종 배포본 `교복구매_길라잡이_20260921_v7.xlsm`(FAIL 0). P2-02는 한컴 COM으로 P2-01 산출물 8개가 오류 없이 열리는 것까지 확인했으나(1층), 레이아웃 육안 확인(2층)은 여전히 사람 몫으로 TODO 유지.
- **실사용 버그 6건 추가 수정 + 시트 순서 재배치(2026-09-21, 같은 후속 세션 이어서)**: 위 UX 개선 3종 배포본(`v7`)을 사용자가 직접 조작하며 발견한 결함 6건을 순서대로 수정함. (1) 빠른 출력 선택 패널 상단 버튼이 기존 저장/불러오기 버튼과 좌표가 겹쳐 "버튼이 없어졌다"는 보고 → 좌표 재배치로 해결. (2) 서식이 47개라 이동이 불편하다는 지적 → 구현된 47개 서식 시트 전체 + 계약방법안내 + 기초자료입력에 상호 이동 버튼 신규 추가(`이동_서식선택출력()`/`이동_계약방법안내()`/`이동_기초자료입력()`, 서식 시트 버튼은 열 N에 `.PrintObject=False`로 배치해 PDF에는 노출 안 함). (3) 계약절차안내 팝업에서 관련 서식 클릭해도 반응 없음 → 모달+네이티브 인쇄미리보기 충돌(화면 정지)과 리스트박스 미클릭 시 무반응 두 원인을 확인해 `frm.Show 0`(모달리스)+`Me.Hide`/`Me.Show 0` 감싸기, 관련 서식 첫 항목 자동 선택으로 해결. (4) "출력할 수 없습니다" 메시지가 원인을 알려주지 않아 이미 저장된 데이터에도 계속 발생한다는 오해 → 실제 원인은 위원 성명이 마스킹 형식이 아니어서 `검증_위원행검증()`이 정상 차단한 것이었음, 신규 `출력불가사유()`로 구체적 원인(위원 마스킹 형식 안내 포함)을 메시지에 포함시킴. (5) F-012 제1조가 여전히 "20○○학년도"로 남아 있던 것을 `기초자료입력!C5`와 연동(제3조는 직전 라운드에 이미 완료, 나머지 108건은 2026-09-20 확정대로 의도적 미반영 유지). (6) "시트 순서를 서식번호(계약순서)순으로 나열해달라"는 요청에 따라 `1.사용설명서→2.기초자료입력→3.계약방법안내→탐색 허브 시트→F-001~F-050 오름차순`으로 `Worksheet.Move` 재배치. **부수적으로 코드 안정성 결함 2건도 발견·수정**: 사용자가 `선택서식_PDF저장_실행`의 `Err.Raise` 코드를 그대로 붙여넣어 지적 → `Application.Run` 경계를 넘는 `Err.Raise`가 불안정을 유발하는 기존에 이미 고친 패턴과 동일함을 확인해 Boolean 반환+파일 로그(`pdf_error_log.txt`)로 전환. 이 수정 직후 반복행 서식(F-015 등) 배치 PDF 출력에서 새로운 "Subscript out of range" 회귀가 드러나 이분 탐색으로 F-015를 트리거로 확정, 근본 원인이 `.Copy` 직후 `Sheets(Sheets.Count)` 참조의 COM 타이밍 불안정임을 규명해 10개 임시시트생성 함수 전부를 `ActiveSheet`로 교체해 해결. 세션 중 원본 XLSM이 다시 실수로 저장돼(SHA-256 불일치) 기존과 동일한 절차로 재복구함. 안전 빌드 최종 재시도(retry28)에서 전 항목 PASS를 확인해 `교복구매_길라잡이_20260921_v10.xlsm`을 생성함. 상세는 [로그.md](로그.md) 2026-09-21 두 번째 후속 항목 참고.
- **버그 2(미사용 표 칸 정리)·요청 4(DB 시트 이동+버튼) 완료(2026-09-23)**: 2026-09-23 세션에서 코디네이터가 제기한 11건 중 마지막까지 "미착수"로 남아 있던 2건을 완료함. **버그 2**: 47개 서식을 `서식_매핑표.md` 대조 + 전체 파일 내부 필드ID 리터럴 검색으로 조사한 결과, F-014(G20)·F-031(G18) "비고" 합계 행에 계산필드ID 문자열(`"K-03"`·`"K-02"`)이 실수로 인쇄되던 죽은 칸 2건만 확정·수정했고 그 외는 정리 대상 없음으로 결론함. **요청 4**: DB 5개 시트(DB·DB_품목·DB_업체·DB_위원·DB_평가)는 개인정보 보호를 위해 Hidden/VeryHidden을 유지하면서, 탭 순서를 학교정보 뒤·F-001 앞으로 재배치하고 기초자료입력↔DB 시트 간 "DB 열기(관리자용)"/"DB 닫기(다시 숨김)" 버튼 왕복을 신규 구현함(DB_정량평가·DB_정성평가·DB_자기평점 3종은 요청 대상이 아니라 그대로 둠). 구현 중 COM 함정 2건(보호된 시트의 `Buttons().Add()` 실패, VeryHidden 시트의 `Worksheet.Move()` 실패 — 둘 다 임시 해제 후 원복으로 해결)과 버그 2·요청 4 모두와 무관한 기존 결함 2건(F-036 정성평가 합계가 F-016의 드롭다운 개편 이후 갱신되지 않아 `#VALUE!` 오류가 나던 결함, 계약방법안내 초기값 검증이 2026-09-16 구설계 전제로 남아 2026-09-21 개편 이후 항상 FAIL하던 스테일 테스트)을 발견해 함께 수정함. 안전 빌드 4회 실행 끝에 전 항목 PASS(FAIL 0건, PASS 392건)로 `artifacts/excel/교복구매_길라잡이_20260923_v11.xlsm`을 생성했고, 마스킹 QA 사본에서 F-014·F-031·F-036 PDF 재출력으로 비고 공란·정성 합계(55)·총점(105)·판정(적격) 정상 표시를 육안 확인함. 상세는 `test.md` 13절, `로그.md` 2026-09-23 항목.
- **계약방법안내 버튼 정리·날짜 자동반영 15종 점검·P2-02/계약절차 팝업 사용자 확정(2026-09-24, HWPX 버튼 수정 세션 다음 세션)**: 사용자 요청 3건을 처리함. ① 계약방법안내 상단 버튼 3개 중 "계약방법 반영"(`btn계약방법반영`)을 삭제함 — 카드 4개(`계약방식선택_*`) 클릭이 `계약방식즉시반영()`으로 이미 B-03에 즉시 반영해 기능이 중복됐던 2026-09-21 카드 UX 개편의 잔재였음. 남은 "기초자료입력으로"·"서식선택_출력으로" 2개를 옛 반영 버튼 자리까지 당겨 재배치하고, `verify_excel_v1.ps1`의 버튼 존재 검증도 이동 버튼 2개 기준으로 갱신함. ② 사용자가 지목한 15개 서식(F-002·F-003·F-015·F-016·F-019·F-021·F-022·F-023·F-024·F-025·F-027·F-029·F-030·F-031·F-040)의 서명일자를 COM으로 직접 렌더링 대조한 결과, F-002·F-019·F-021·F-022·F-023·F-025·F-029(7종)는 이미 학년도(C5) 자동반영 수식이 정상 동작 중이었고(빈 워크북에서는 "20○○년" 같은 설계된 폴백만 보여 마치 고정값처럼 보였을 뿐), 실제 결함은 F-003·F-015·F-016·F-024·F-030·F-031·F-040(7종)의 서명일자가 셀 참조 없는 순수 문자열(`.Value2` 또는 `="리터럴"` 형태의 `.Formula`)이라 학년도가 몇 년이든 항상 고정 표시되는 것과, F-027(위임장)은 서명일자 줄 자체가 누락돼 있던 것 2가지였음. 7종은 F-025의 기존 정상 패턴(`=IF(C5="","20○○년...","20"&RIGHT(C5,2)&"년...")`)으로 통일하고, F-027은 F-029와 동일하게 발행일(C8) 기준 동적 수식을 새 줄(B21)로 추가함(월·일은 원문대로 서명 시 수기 작성 유지, 자동화 대상 아님). ③ 사용자가 P2-02 2층(한컴 레이아웃 육안 확인)과 계약절차안내 팝업 실사용을 직접 확인해 통과로 확정함 — `체크리스트.md`·`test.md`(H-05, X-79)를 PASS로 갱신함. 안전 빌드 전 항목 PASS(FAIL 0, `교복구매_길라잡이_20260924_v15.xlsm`)로 검증 완료. **미착수(사용자 예고)**: 사용자가 "표 병합과 간격 등은 내가 직접 수정하겠다"고 밝혀, v14.xlsm에 대한 수동 서식 수정이 아직 이 세션 시점에는 반영되지 않은 상태(파일 타임스탬프상 별도 수정 이력 없음)임. 사용자가 편집을 마치면 어떤 서식·셀을 어떻게 바꿨는지 알려줘야 다음 빌드(구조 스크립트)에도 동일하게 반영할 수 있음 — 현재 파이프라인은 매번 전체를 코드로 재생성하는 구조라 Excel에서의 수동 편집이 자동으로 보존되지 않음.
- **운영 규칙**: 한 Phase의 구현·검토·린트·테스트·문서 갱신이 모두 통과할 때만 다음 Phase로 이동함. `/clear` 전에는 [로그.md](로그.md)에 재개 정보를 남김.

## P0 준비

| ID | 작업 | 담당 역할 | 선행 | 상태 | 완료 증빙 |
|---|---|---|---|---|---|
| P0-01 | 원본 파일 무결성·사본 경로 확정 | 분석 | - | DONE | 해시·작업 경로 |
| P0-02 | Kordoc 파싱·필드·fill dry-run·patch·render 확인 | 문서자동화 | P0-01 | DONE | `kordoc_기술검증.md` |
| P0-03 | 기존 XLSM 의존성·기능 정적 분석 | Excel자동화 | P0-01 | DONE | `엑셀_벤치마크_분석.md` |

## P1 인벤토리·모델

| ID | 작업 | 담당 역할 | 선행 | 상태 | 완료 증빙 |
|---|---|---|---|---|---|
| P1-01 | HWPX 구조·표·이미지 순서 추출 | 분석 | P0-02 | DONE | `_workspace/01_analysis/HWPX_원시구조_목록.md` |
| P1-02 | Form ID 인벤토리 작성 | 분석 | P1-01 | DONE | `서식_인벤토리.md` |
| P1-03 | 데이터 사전·서식 매핑 정의 | 분석·Excel자동화 | P1-02 | DONE | `입력데이터_사전.md`, `서식_매핑표.md` |
| P1-04 | 전체 범위·대표 POC 3종 확정 | 품질검토 | P1-02, P1-03 | DONE | `결정사항.md` |

## P2 HWPX POC

| ID | 작업 | 담당 역할 | 선행 | 상태 |
|---|---|---|---|---|
| P2-01 | 대표 3종 독립 HWPX 템플릿화·고유 토큰 삽입·마스킹값 치환 | 문서자동화 | P1-04 | DONE(기술 검증 범위) |
| P2-02 | 한컴 열기·PDF 비교·사람 검토 | 품질검토 | P2-01 | DONE(2026-09-21, 사용자 결정으로 AI 시각 확인이 사람 검토 대체) |
| P2-03 | Excel 연계 또는 웹 이관을 포함한 출력 ADR 확정 | 문서자동화·품질검토 | P2-02 | DONE(2026-09-25, `ADR-001-HWPX출력방식.md`) |

## P3 Excel MVP

| ID | 작업 | 담당 역할 | 선행 | 상태 |
|---|---|---|---|---|
| P3-01 | 기준 XLSM 분석 후 워크플로우·기초자료·참조 UI 재설계 | Excel자동화 | P1-03, P1-04, 기준 XLSM 직접 검토 | DONE |
| P3-02 | 확정 서식·선택 출력 구현 | Excel자동화 | P3-01 | DONE(2026-09-25, F-001~F-052 중 51종, F-013만 HWPX 위임) |
| P3-03 | 골든 테스트·설명서·배포 패키지 | Excel자동화·품질검토 | P3-02 | DONE(2026-09-25, `test.md`·`체크리스트.md`·`사용설명서` 시트) |

## P4~P5

**2026-09-25 사용자 결정으로 P4 현장 검증 완료 처리**: 신규 담당자의 학교 행정실 PC에서의 독립 관찰 테스트 대신, 사용자 본인이 사무실·자택 PC 2대를 오가며 반복 사용·확인한 결과를 현장 검증으로 갈음하기로 결정함(`ADR-001-HWPX출력방식.md` 참고). 이는 계획.md P4-01 원래 DoD("신규 담당자도 5개 대표서식 생성 완료", "관찰기반 사용성 테스트")와 범위가 다름을 기록해 둠 — 실제 학교 행정실 직원의 최초 사용 경험은 검증되지 않았으며, 문제 발생 시 사용설명서·`운영_최종확인절차.md`를 우선 참고하도록 안내함.

P5 웹앱은 이제 활성 Phase임(`.claude/quality-gate.json` activePhase=P5-01). P5 UX 기준은 공개 학교행정 업무 안내 웹판의 실제 탐색을 참고해, 절차 탐색 허브→9단계 상세→관련 Form ID→입력·누락 검사→미리보기·다운로드로 정리했으며, 상세 근거는 `_workspace/05_web/학교행정업무길라잡이_웹판_UX_벤치마크.md`를 따름.

- **P5-01 웹 아키텍처 ADR 작성(2026-09-25)**: `ADR-002-웹앱아키텍처.md`를 신규 작성함(상태: PROPOSED, 사용자 확인 대기). 2026-09-21 선행 검토 2건(`_workspace/05_web/웹앱_아키텍처_검토_의견.md`·`웹앱_개발_방안_검토_최종.md`)을 `ADR-001`(2026-09-25)과 정합하게 재정리함 — 핵심 변경점은 두 선행 검토가 제안했던 "서버리스 함수로 웹 HWPX 생성" 경로를 `ADR-001`이 이미 기각했으므로 채택하지 않고, 향후 확장 여지로만 기록으로 낮춘 것임. 확정 사항: 서버·DB 없는 정적 웹앱(정적 export 프레임워크), 기초자료 입력은 팝업 또는 엑셀 업로드(둘 다 브라우저 클라이언트 파싱, 서버 미경유), PDF는 클라이언트 사이드 생성, HWPX는 웹에서 생성하지 않고 "Excel에서 HWPX 작성" 사용법만 안내. `prd.md` FR-07·FR-08(비활성으로 갱신)·§4 비기능요구사항·§6 범위제외도 함께 갱신함. 남은 미결정(PDF 생성 세부 방식·엑셀 업로드 파싱 범위·배포처)은 `ADR-002` §5에 남겨 P5-02/03에서 결정함.

## 차단 사유

- P2-01: (2026-09-18~19 경과 요약) `fill -j`의 "문서 처리 중 오류"는 CLI가 `-j` JSON의 UTF-8 BOM을 제거하지 않고 `JSON.parse`에 전달해 발생하는 문제로 규명·해결함(BOM 없는 JSON이면 정상 동작). 원래 계획이던 "한컴 Automation `CreateField`로 클릭히어 필드 21개 일괄 삽입"은 kordoc이 두 번째 이후 클릭히어 필드의 경계를 인식하지 못하는 별도 결함으로 막혀 있어 폐기하고, 라벨 기반(`kordoc generate` 두문·결문 표 + 본문 리스트 "라벨：값" 패턴) 방식으로 전환함. F-013은 추가로 `kordoc generate`가 마크다운 이미지 참조를 기본값으로는 조용히(오류·경고 없이) 누락시키는 silent failure를 발견했고, 원인은 `--image-dir <dir>` 옵션 누락 — 이 옵션과 파일명 일치 규칙을 지키면 표 셀·rowspan 복합표 안에서도 이미지가 정상 임베드됨을 확인함. 이 방식으로 F-007·F-024·F-013을 `artifacts/hwpx/P2-01/`에 템플릿(빈 양식)·골든테스트결과(마스킹 데이터 fill본) 쌍으로 재구축해 `fill -j`→`validate`→`render`→재파싱 왕복을 전부 오류 없이 통과함을 확인함. 과정에서 통화 라벨의 콤마로 인한 fill 후 텍스트 중복(F-007), 반복 라벨 충돌로 인한 값 밀림(F-024)도 발견·회피함. **F-013은 두 차례 판정이 뒤집혔다가 최종적으로 다시 정상 확인됨**: ① 처음엔 "8쪽·이미지55·표10, 원본 이미지 개수와 정확히 일치"를 근거로 DONE 선언 → ② 재추출 이미지의 실제 바이트 크기를 직접 확인해보니 41개 고유 이미지 중 17개(image_001, 026~041)가 원본 사진이 아니라 1×1 픽셀 67바이트 더미임을 발견해 `PARTIAL`로 되돌리고 "kordoc 내부의 문서 규모 관련 결함"으로 추정 → ③ 추가 반증 실험(오염되지 않은 순수 이미지로 같은 갤러리 표 구조를 재현하면 100% 정상 임베드됨)으로 ②의 "kordoc 결함" 추정도 기각하고, **진짜 원인이 이번 세션이 `--image-dir` 소스 폴더를 검증용 `--image-refs` 재추출 출력과 같은 경로로 반복 재사용해 스스로 오염시킨 절차 실수였음**을 규명함(HTML `<img>` 태그가 임베드되지 않는다는 애초 관찰은 유효하게 남음 — 마크다운 `![]()` 문법만 사용해야 함). 원본 HWPX를 `-p 28-42`로 다시 파싱해 한 번도 쓰지 않은 새 이미지 폴더로 F-013을 최종 재생성한 결과 41개 이미지 전부 바이트 단위로 재검증 완료(스텁 0개), 렌더는 12쪽·이미지55·표14(원본 11쪽·표11개 대비 표 3개 많음 — 원인 미확인). 상세·정정 경위는 `_workspace/02_hwpx/P2-01/독립템플릿_토큰명세_검증.md` 5~12장. 이에 따라 P2-01은 **`DONE(기술 검증 범위)`로 최종 확정함**. 완료로 보지 않는 것: (1) 한컴 수동 열기·PDF 비교·행정실 직원 확인(P2-02)은 여전히 미수행이며, 특히 F-013의 표 개수 차이(14 vs 11)는 반드시 한컴에서 육안 확인 필요, (2) F-013은 디자인 이미지·수치 자체가 학교마다 통째로 달라지는 "표준 예시" 문서라 라벨 기반 `fill`이 학교명 외에는 실질적으로 적용되지 않는다는 구조적 한계를 확인함 — 이미지 교체가 필요한 실사용 워크플로는 별도 설계가 필요하며 이번 범위 밖임. **검증 원칙 확정**: 이미지 포함 HWPX는 재추출 시 항상 새 디렉토리를 쓰고, `--image-dir` 소스 폴더를 검증 출력과 절대 공유하지 않으며, 재추출 이미지의 바이트 크기를 직접 확인함.
- P2-02: P2-01의 기술 검증(3종 템플릿·fill·validate·render·이미지 바이트 전수 확인)이 모두 통과해 착수 가능한 상태가 됨(`TODO`). 남은 작업은 `artifacts/hwpx/P2-01/`의 6개 산출물(F007·F024·F013 각 템플릿+골든결과)을 한컴에서 직접 열어 페이지 분량·줄바꿈·표 분할·이미지 배치를 육안 확인하고 PDF로 비교하는 것이며, AI 에이전트가 대신할 수 없는 사람 검토 항목임. F-013은 표 개수 차이(렌더 14 vs 원본 11)를 특히 확인해야 함. **2026-09-19 보조 작업**: 사람 검토를 돕기 위해 (1) 6개 산출물의 미리보기를 `_workspace/02_hwpx/P2-01/PDF미리보기/`에 생성함 — 원래 PDF로 요청받았으나 이 환경에 kordoc PDF 렌더가 요구하는 `puppeteer-core`가 실제 참조 경로에 설치되지 않아(격리 설치 후 연결 시도 실패) **SVG로 대체**함(참고용, 한컴 화면·PDF가 최종 판단 근거), (2) 확인 항목 체크리스트를 `_workspace/02_hwpx/P2-01/P2-02_한컴검토_체크리스트.md`에 작성함(공통 항목 + F-007·F-024·F-013 전용 항목 + 사람이 채울 빈 결과표). 이 보조 산출물 작성만으로 P2-02를 완료 처리하지 않으며, 상태는 그대로 `TODO`로 유지함.
- P3-02: P3-01 기반 파일은 완료했으나, Form ID별 원본 조판과 PDF 골든 비교는 별도 구현·검증이 필요함. HWPX 출력 기능은 P2-01 차단을 유지함.
- P3-01: `교복구매_길라잡이_MVP_초안.xlsm`은 시트·목록·외부 의존성만 확인한 구조 초안으로, 기준 XLSM 수준의 실무 입력 화면·서식 조판·자동 반영·선택 출력 기능이 없음. 완료 상태를 철회하고 기준 파일 직접 검토 후 재설계함.
- P3-01 (2026-09-15~16 진행): 기준 XLSM VBA 소스(UserForm2·Sheet1·Sheet6·소속기관검색폼·Module1/5 등)를 실제로 열람해 재설계 방침을 확정하고(`_workspace/03_excel/기준XLSM_UI구조_분석.md` 6장), 클린룸 XLSM을 작성함. 완료: 기초자료입력(C/B/D/R 필드)·DB·신규초기화/저장/수정/불러오기 매크로, 품목·업체·위원·평가점수 반복행의 정규화 저장(`DB_품목`, `DB_업체`, `DB_위원`, `DB_평가`), 서식선택_출력(F-001~F-052 52행, 체크박스형 선택), F-007·F-024 A4 1쪽 및 PDF, 120행 공개 기관정보 표본의 학교명·지역·급별 검색과 선택 학교명 C4 반영, 교복구매 9단계와 대표 Form ID 이동 흐름, 계약방법안내 시트의 2단계 입찰 예시 확인값 2개와 명시적 반영 버튼임. 위원은 역할·직위와 제한된 마스킹 표기만 저장하며, 평가점수는 배점 범위와 총점을 검증함. 사용자가 선택한 무암호 운영에서는 DB 숨김·보호가 우발적 편집 방지용이고, 매크로 허용 상태의 저장 직전 전체 DB 재검증으로 허용되지 않은 값의 영구 저장을 취소함. 외부 링크 0/연결 0/`#REF!` 0, 불완전 반복행·개인정보성 표기·평가 배점 초과를 차단하고 기존 PDF를 재검증함. 미완료: F-013(표·이미지 복합, 11쪽)은 Excel 부적합 사유로 HWPX 우선 처리로 보류함. 나머지 49개 Form ID와 기준 XLSM 수준의 계약방법 전체 마법사는 미구현이며 완료로 표시하지 않음.
- P3-01 (2026-09-15 F-007/F-024 재구성): `document-automation-engineer`의 읽기 전용 HWPX 원문 대조(FAIL, F-007 일치 3/12·F-024 일치 0/8)를 근거로 `excel-automation-engineer`가 두 시트만 재구성함. `기초자료입력`·VBA는 하드코딩 Range 주소 위험 때문에 변경하지 않음. F-007: 가~라 4항목·(경유)·시행/접수/우편번호/주소/전화/팩스/이메일 결재란·"끝." 반영. F-024: 품목/수량/단가비율(%)/비고 4열·고정 6품목(동복4·하복2) rowspan·업체 제출양식 부제·서명란 반영. 부수 버그(납품기한 날짜 일련번호 노출, 조사 오류, 시행란 학교명 중복, 단가비율 합계 자동 숫자화, 장문·특수문자 겹침)도 직접 발견해 수정함. 재대조 결과 F-007 PASS 9/12, F-024 PASS 7/8이며 **완전 일치는 아님**(잔여 한계: F-007 우편번호~이메일 로컬 입력값·기초자료입력 미연동, F-024 단가비율 자동계산, F-007 표제/제목 문구 일부 — `_workspace/03_excel/F-007_F024_수정후_재대조.md` 5절). A4 1쪽은 배포본·골든테스트·극단값(E-03) 모두에서 Zoom=100·FitToPages=False·HPageBreaks=VPageBreaks=0으로 재확인함(수정 전 상태가 실제로는 FitToPage 강제축소였음을 발견·정정). 나머지 50개 Form ID·F-013 HWPX 위임은 범위 밖으로 그대로 유지함.
- P3-01 (2026-09-16 F-014 구현·검증, 안전 빌드 좀비 프로세스 수정): `F-014_평가항목배점기준` 시트를 추가해 R-07(평가항목·배점·점수)·K-03(총점)에 연결하고, `검증_F014출력가능()`으로 필수값·고아행·배점 초과를 선택 출력에서 차단하도록 구현함(상세는 `_workspace/03_excel/F-014_구현검증.md`). 안전 빌드(`build_excel_v1_structure.ps1`→`build_excel_v1_vba.ps1`→`verify_excel_v1.ps1`)를 재실행해 `교복구매_길라잡이_20260916_v13.xlsm`을 생성·검증했고, `verify_v1_log.txt` 전 항목이 PASS함(F-014 A4 1쪽, R-07 복수 평가행·K-03 총점 반영, 불완전 평가행·배점 초과 차단, 최대 길이·특수문자 공통값 수식 오류 없음 포함). 별개로, 빌드·검증 스크립트가 Excel COM 자동화 오류 시 `Quit()`을 호출해도 EXCEL.EXE가 좀비로 남는 문제를 발견해(작업 중 백그라운드에 5개 누적 확인) 세 스크립트 모두에 프로세스 생성 전후 PID 비교 기반 안전망 강제 종료를 추가함. 수정 후 안전 빌드 전체를 재실행(`v14`)해 좀비 프로세스 0건을 확인함. F-014 단위는 완료로 기록하되 P3-01은 `IN_PROGRESS`, X-06은 `FAIL`을 유지함. 나머지 48개 Form ID, 계약방법 전체 마법사, F-013 HWPX 경로가 남아 있음.
- P3-01 (2026-09-16 F-015 부분 구현 — 저장 연동 차단): 원본 HWPX `[9-4]` 고정 4개 항목(수행경험·공인인증·거리적접근성·상한가격, 합계 50점) 배점표를 대조해 `F-015_정량적평가` 시트와 `기초자료입력!C44:F53` 업체별 점수 입력을 구현하고, 완전한 업체마다 별도 쪽으로 F-007/F-014/F-024와 하나의 PDF로 결합하는 출력 경로(`검증_F015출력가능`, `F015업체행완전한가`, `F015임시시트생성`/`정리`)를 검증 통과시킴. 그러나 `DB_정량평가` 저장/수정/불러오기 연동(`정량평가복사_기초자료_DB`)은 `Application.Run("검증_저장하기")`가 원인 미상으로 무한 대기하는 것을 확인해(재현 100%, `_trace.txt`로 해당 프로시저 진입 직후 정지 확인, 근본 원인 미규명) 전부 제거함. 모든 Form ID가 공유하는 저장 기능을 검증된 상태로 되돌린 뒤 안전 빌드 전체를 재실행(`v15`)해 F-015 출력 경로와 기존 F-007/F-014/F-024·DB 회귀가 모두 PASS함을 재확인함. 상세는 `_workspace/03_excel/F-015_구현검증.md`. F-015 점수는 같은 세션 직접 입력값만 출력에 반영되고 저장·재불러오기 시 사라지므로, F-015는 "구현 완료" 목록에 포함하지 않음. 저장 연동 원인 규명은 다음 세션 과제로 남김.
- P3-01 (2026-09-17 F-015 저장 연동 근본 원인 규명·완료): Fable 모델에 위임한 조사로 무한 대기의 근본 원인을 확정함 — `Workbook_Open`이 재열기 시 UserInterfaceOnly 보호를 재적용하는 목록(`Array("DB", "DB_품목", "DB_업체", "DB_위원", "DB_평가")`)에 새로 만든 `DB_정량평가`를 빠뜨려, 검증 사본을 다시 열면 그 시트만 매크로 쓰기가 막히고 `정량평가복사_기초자료_DB`의 첫 셀 쓰기에서 보호된 시트 오류(1004)가 발생해 `Application.Run` 중 보이지 않는 모달 오류 대화상자로 이어진 것임(`Visible=True`로 직접 확인). 목록에 `"DB_정량평가"` 한 항목을 추가하고 2026-09-16에 제거했던 저장 연동 코드를 원래대로 되살림. `verify_excel_v1.ps1`에 `ProtectionMode` 회귀 검사를 추가함(기존 `ProtectContents` 검사만으로는 이 문제를 못 잡았음). 안전 빌드 전체를 재실행해 `교복구매_길라잡이_20260917_v1.xlsm`을 생성했고, `verify_v1_log.txt` 전 항목(F-015 저장·수정·불러오기 연동 포함) PASS함. 상세는 `_workspace/03_excel/F-015_구현검증.md` 4절. F-015를 "구현 완료" 목록(F-007·F-014·F-015·F-024)에 포함함. 나머지 47개 Form ID, 계약방법 전체 마법사, F-013 HWPX 경로가 남아 있음.
- P3-01 (2026-09-17 F-017 제출서류 자기확인서 구현·검증): 원본 HWPX `[9-6]`의 업체별 제출서류 표 20행을 `F-017_제출서류자기확인서`로 재구성함. 업체명만 R-03 반복행에서 업체별 임시 시트로 반영하고, 대표자·연락처·대리인 정보는 자동 반영·저장하지 않는 빈 양식으로 유지함. 선택 PDF 결합·임시 시트 정리, 외부 링크·연결·`#REF!` 0건, A4 1쪽 자연 배율, 빈 배포본에서 업체명 `0` 미표시를 사본으로 검증해 `교복구매_길라잡이_20260917_v5.xlsm`을 생성함. 상세는 `_workspace/03_excel/F-017_구현검증.md`를 따름. 나머지 46개 Form ID, 계약방법 전체 마법사, F-013 HWPX 경로가 남아 있음.
- P3-01 (2026-09-17 F-019 입찰참가신청서 재개·구현 완료): 이전 세션에서 구조·VBA·검증 스크립트 초안까지 작성했으나 Excel COM 중복 프로세스로 빌드·PDF QA를 완료하지 못해 중단했던 F-019를 재개함. 좀비 EXCEL.EXE 프로세스가 없는 상태를 확인 후 안전 빌드를 실행해 원문 HWPX `[9-7] [서식 2]` 대조 레이아웃이 A4 1쪽 자연 배율(`Zoom=100`, `FitToPagesWide/Tall=False`)을 만족하지 못하는 실제 결함(가로 방향에서 `HPageBreaks=1`)을 발견함. 세로 방향(`Orientation=1`)으로 전환 후에도 폭 초과(`VPageBreaks=1`)가 나타나 가로 방향을 유지하고 행 높이를 재조정(총 547.5pt → 530.5pt)해 `HPageBreaks=VPageBreaks=0`을 확정함. 안전 빌드 전체(구조→VBA→검증)를 재실행해 `교복구매_길라잡이_20260917_v8.xlsm`을 생성했고, `verify_v1_log.txt` 132건 전 항목 PASS(기존 F-007·F-014·F-015·F-016·F-017·F-018·F-024·DB·저장/수정/불러오기 회귀 없음 포함)를 확인함. 검증 통과본에서 업체명(R-03)·학교 공통정보(C-01·C-02·D-02)만 반영되고 법인등록번호·주소·전화번호·주민등록번호·대리인·사용인감이 빈칸으로 유지됨을 PDF 렌더링으로 직접 시각 확인함(1쪽). Excel COM 스크립트 3종 모두 실행 전후 EXCEL.EXE 프로세스 수를 비교해 좀비 프로세스가 없음을 재확인함. 상세는 `_workspace/03_excel/F-019_구현검증.md`. 이로써 Excel 구현 완료 Form ID는 F-007·F-014·F-015·F-016·F-017·F-018·F-019·F-024(8종)이며, 나머지 44개 Form ID(52 - 8, F-013 포함)와 계약방법 전체 마법사가 남아 있음.
- P3-01 (2026-09-17 F-020 입찰 참가 신고서 구현): 원본 HWPX `[9-8]`을 대조해 `F-020_입찰참가신고서` 시트와 `검증_F020출력가능`/`F020업체행완전한가`/`F020임시시트생성` 로직을 F-017~F-019와 동일한 패턴(업체명 R-03 반복행 + 학교 공통정보 C-01·C-02·D-02만 반영, 사업자 번호·대표자·직인 등은 빈칸 또는 수기 안내 유지)으로 구현함. 안전 빌드 전체(구조→VBA→검증)를 실행해 `교복구매_길라잡이_20260917_v9.xlsm`을 생성했고, `verify_v1_log.txt` 전 항목 PASS(F-020 시트 존재, A4 1쪽 자연 배율, 원문 제목·머리글 대조, 업체명·학교 공통정보만 반영, 개인정보 빈칸 보존, 선택 출력 허용, 업체별 임시 시트 정리, 기존 F-007·F-014~F-019·F-024·DB 회귀 없음 포함)을 확인함. **다만 이번 세션은 자동 검증 스크립트의 PASS만 확인했고, F-019 때처럼 별도 PDF를 생성해 사람이 직접 육안으로 재확인하는 절차는 수행하지 않음** — 다음 세션에서 PDF 육안 확인을 보완 과제로 남김. 상세는 `_workspace/03_excel/F-020_구현검증.md`. 이로써 Excel 구현 완료 Form ID는 F-007·F-014·F-015·F-016·F-017·F-018·F-019·F-020·F-024(9종)이며, 나머지 43개 Form ID(52 - 9, F-013 포함)와 계약방법 전체 마법사가 남아 있음.
- P3-01 (2026-09-18 F-020 PDF 육안 검증 완료): 1차 재시도는 읽기 전용 사본에서 PDF 내보내기가 Excel "문서가 저장되지 않았습니다" 오류로 실패했고 `검증_선택서식_PDF저장` 매크로도 PDF 생성 전 응답 중단으로 실패함(배포본·사용자 작업 파일은 변경 없음). 원인은 COM `ExportAsFixedFormat` 호출의 `From`/`To` 인자에 `False`가 전달되어 0으로 해석된 것이었음. `Type.Missing`으로 생략 처리해 2차 시도에서 `artifacts/excel/_diagnostic/F-020_20260918_manual_qa/`의 새 비식별 사본으로부터 정상 PDF(`F020_visual_QA_110058.pdf`, 66,735바이트)를 내보냄. 160dpi JPEG 렌더링으로 A4 1쪽·잘림 없음·업체명/학교명만 반영·사업자 번호/대표 공란·직인은 수기 안내만 존재함을 직접 확인함. F-020 단위의 자동 검증과 PDF 육안 확인은 모두 `DONE`임. 상세는 [로그.md](로그.md) 2026-09-18 항목을 따름.
- P3-01 (2026-09-18 F-021 교복 납품 제안서 구현·검증 완료, F-022 빌드 결함 수정): 세션 시작 시점에 이미 `F-021_교복납품제안서` 시트·VBA·`F-021_구현검증.md`(1차, 배포 후보 v3)가 작성돼 있었으나(이전 세션이 문서 갱신 전에 중단), `git status`상 미반영이었던 `task.md`/`test.md`/`로그.md`/`계획.md`/`체크리스트.md` 갱신과 이번 세션 자체의 재검증을 완료함. 원본 HWPX `[9-9] [서식 4]`를 대조해 업체명(R-03)·학교 공통정보(C-01·C-02)만 반영하고 대표자·주소·연락처·설립년도·직원현황·사업기간·연혁·제안자 성명·서명은 빈칸으로 유지하는 구현을 확인함. 안전 빌드 재실행 시 F-021 완료 직후 F-022(교복 납품 실적표) 섹션에서 `'System.Int32' 형식 개체를 'System.String' 형식으로 캐스팅할 수 없습니다` 오류로 파이프라인 전체가 중단되는 것을 발견함(F-021 자체 결함 아님) — 원인은 `for ($i=0; ...) { $ws.Cells.Item(row, $i+2)... }` 형태의 루프 변수 기반 컬럼 인덱스로 반복 호출한 직후 같은 시트의 무관한 셀에 정수를 대입하는 후속 코드가 실패하는 PowerShell 5.1 COM 상호운용 이상 동작이었음. `.Cells.Item()` 반복 호출을 리터럴 `.Range()` 개별 대입으로 재작성하고(값·서식 동일 유지), F-022가 A4 1쪽을 초과(`VPageBreaks=1`)하던 열 너비·여백도 최소 축소해 해소함(F-022 내용·원문 대조·개인정보 경계는 범위 밖이라 별도 검증·완료 처리하지 않음). 수정 후 안전 빌드 전체를 재실행해 `교복구매_길라잡이_20260918_v6.xlsm`을 생성했고 `verify_v1_log.txt` 146개 항목 전부 PASS(FAIL 0, 기존 F-007·F-014~F-020·F-024·DB 회귀 없음 포함)를 확인함. 비식별 사본(`artifacts/excel/_diagnostic/F-021_20260918_session2_qa/`)에서 필수값·업체명만 입력해 F-021 단일 PDF를 새로 내보내 렌더링·육안 확인함(A4 1쪽, 학교명·업체명만 반영, 개인정보 필드 전부 공란). 진단 과정에서 필수값 누락 시 `Application.Visible=False` 상태의 `MsgBox`가 무한 대기하는 현상을 재현·해소함(매크로 결함이 아니라 진단 스크립트의 입력 누락이 원인). 상세는 `_workspace/03_excel/F-021_구현검증.md`. 이로써 Excel 구현 완료 Form ID는 F-007·F-014·F-015·F-016·F-017·F-018·F-019·F-020·F-021·F-024(10종)이며, 나머지 42개 Form ID(52 - 10, F-013·F-022 포함)와 계약방법 전체 마법사가 남아 있음.
- P3-01 (2026-09-18 F-022 교복 납품 실적표 구현·검증 완료): 이전 세션이 최소 수정(타입 캐스팅 오류·페이지 초과)만으로 빌드만 통과시켜 두었던 F-022 자체를 완료함. 원본 HWPX `[9-10] [서식 5]`(`artifacts/hwpx/P0-02_원본사본.hwpx`)를 kordoc `parse_pages`/`extract_tables`로 대조한 결과 표가 머리글 1행 + 데이터 9행으로 구성돼 있는데 기존 구현은 8행(순번 1~8)만 있는 차이를 발견함. `scripts/build_excel_v1_structure.ps1`에 9번째 실적행(B18=9)을 추가하고 그 아래 붙임·제출문구·날짜·서명·수신처 행을 1행씩 밀어 `PrintArea`를 `$B$1:$G$25`로 갱신함(열 너비·여백은 변경하지 않았고 A4 1쪽 자연 배율 유지 확인). `scripts/verify_excel_v1.ps1`의 F-022 자동 검증이 참조하던 구(舊) 8행 레이아웃의 고정 셀 주소(B19·B23)도 새 위치(B20·B24)로 갱신함. `서식_매핑표.md`의 F-022 매핑(C-01~C-02, D-02, R-03 / 보안검토 "없음")과 대조해 업체명·학교명·학년도·고정 제목만 반영되고 실적행 6열은 전부 수기 작성 공란임을 확인함(이 서식에는 개인식별정보 필드 자체가 없음). 실행 중 `.claude/hooks/verify-phase.ps1`의 백그라운드 재검증과 겹쳐 검증용 임시 파일 잠금 경합(사전 경고된 이슈)이 재현됐으나, 훅·좀비 Excel 프로세스 종료를 대기한 뒤 단독 실행해 회피함. 안전 빌드 전체(구조→VBA→검증) 단독 실행으로 `교복구매_길라잡이_20260918_v8.xlsm`을 생성했고 `verify_v1_log.txt` 146개 항목 전부 PASS(FAIL 0, 기존 F-007·F-014~F-021·F-024·DB 회귀 없음 포함)를 확인함. 비식별 사본(`artifacts/excel/_diagnostic/F-022_20260918_qa/`)에서 필수값·업체명만 입력해 F-022 단일 PDF를 내보내 170dpi 렌더링으로 A4 1쪽·잘림 없음·실적행 9행·업체명만 반영·실적행 6열 전부 공란을 직접 확인함. 상세는 `_workspace/03_excel/F-022_구현검증.md`. 이로써 Excel 구현 완료 Form ID는 F-007·F-014·F-015·F-016·F-017·F-018·F-019·F-020·F-021·F-022·F-024(11종)이며, 나머지 41개 Form ID(52 - 11, F-013 포함)와 계약방법 전체 마법사가 남아 있음.
- P3-01 (2026-09-18 F-023 교복 제조 사양서 구현·검증 완료): 원본 HWPX `[9-11] [서식 6]`(`artifacts/hwpx/P0-02_원본사본.hwpx`)을 kordoc `parse_pages`/`extract_tables`로 대조해 제목·부제(제안자)·안내문구·"생활형 교복" 소제목·4열 표(구분/견본 품목/재질/설명 내용, 헤더 1행 + 동복 rowspan4(후드 점퍼·집업티·맨투맨티·긴바지) + 하복 rowspan2(반팔티·반바지))·각주·날짜·서명·수신처 구조를 확인함. `서식_매핑표.md`의 F-023 행(입력 출처 R-04~R-06/K-01, 품목명·수량·단가·금액)이 원문에 없는 필드를 가리키며 F-013 행과 문구가 완전히 동일해 복사 오류로 추정됨을 확인했고, 실제 원문(가격표 아님)과 F-017~F-022 패턴에 맞춰 업체명(R-03)·학교 공통정보(C-01·C-02)만 반영하고 재질·설명 내용·서명란은 F-021·F-022처럼 수기 작성 공란으로 유지함(매핑표 자체는 P1 분석 산출물이라 이번 세션에서 직접 수정하지 않음 — 후속 P1 재검토 과제로 남김). `scripts/build_excel_v1_structure.ps1`에 `F-023_교복제조사양서` 시트를 추가하고 `$implemented` 해시에 등록했으며, `scripts/build_excel_v1_vba.ps1`에 F-023 선택출력 분기·`검증_F023출력가능`/`F023업체행완전한가`/`F023임시시트생성`·임시시트 정리 목록을 추가함. `scripts/verify_excel_v1.ps1`에 필수 시트 26→27개, A4 자연배율 목록, F-023 원문 대조·재질/설명 공란 경계·선택 출력 자동 검증 3건, PDF 선택 목록, 임시시트 정리 검증을 추가함. 훅 레이스(`.claude/hooks/verify-phase.ps1`↔`verify_excel_v1.ps1`)를 프로세스 종료 대기 후 단독 실행으로 회피해 안전 빌드 전체(구조→VBA→검증)로 `교복구매_길라잡이_20260918_v9.xlsm`(151/151 PASS)을 1차 생성했으나, 이후 문서 갱신용 Edit들이 트리거한 훅 백그라운드 재검증이 `verify_v1_log.txt`를 삭제한 채 실패로 남긴 것을 발견함. 프로세스가 모두 종료된 상태를 재확인한 뒤 안전 빌드를 한 번 더 단독 실행해 `교복구매_길라잡이_20260918_v10.xlsm`을 생성했고 `verify_v1_log.txt` 151개 항목 전부 PASS(FAIL 0, 기존 F-007·F-014~F-022·F-024·DB 회귀 없음 포함)가 파일로 남아 있음을 재확인함. 비식별 사본(`artifacts/excel/_diagnostic/F-023_20260918_qa/`)에서 필수값·업체명만 입력해 F-023 단일 PDF(`output/선택서식_20260918_165516.pdf`)를 내보내 A4 1쪽·잘림 없음·동복 4행·하복 2행 견본품목 일치·재질/설명 내용/서명란 전부 공란을 직접 확인함. 진단용 Excel COM 프로세스가 `Quit()` 이후 좀비로 남은 것(사용자 파일과 무관한 automation-embedding 인스턴스)을 확인 후 종료함. 상세는 `_workspace/03_excel/F-023_구현검증.md`. 이로써 Excel 구현 완료 Form ID는 F-007·F-014·F-015·F-016·F-017·F-018·F-019·F-020·F-021·F-022·F-023·F-024(12종)이며, 나머지 40개 Form ID(52 - 12, F-013 포함)와 계약방법 전체 마법사가 남아 있음.
- P3-01 (2026-09-18 F-025 교복 A/S 계획서 구현 착수 후 사용자 요청으로 중단): 원본 HWPX `[9-14]`를 대조한 결과 표가 아닌 순수 텍스트 서식(1.A/S 기간 2.A/S 편의성 3.A/S 지정업체 4.A/S 내용)이며 업체명 표시 자리 자체가 없음을 확인함(F-021~F-023과 달리 "(제안자: 업체명)" 문구 없음). `서식_매핑표.md`의 F-023·F-026 행이 완전히 동일한 문구인 것도 발견했으나(템플릿 복사 흔적으로 추정) 기록만 하고 매핑표는 수정하지 않음. `scripts/build_excel_v1_structure.ps1`에 `F-025_교복AS계획서` 시트, `scripts/build_excel_v1_vba.ps1`에 `검증_F025출력가능`/`F025업체행완전한가`/`F025임시시트생성` 등 선택출력 로직, `scripts/verify_excel_v1.ps1`에 원문 대조·공란 경계 검증 항목까지 코드 작성은 완료됨(PowerShell 구문 검사는 3개 스크립트 모두 통과 확인). 그러나 사용자가 "작업 멈춰줘"로 세션 중단을 요청한 시점에 안전 빌드 실행·PASS 확인·PDF 육안 확인이 되지 않았고, 진행 중이던 백그라운드 검증이 `_workspace/03_excel/verify_v1_log.txt`를 삭제된 채로 남김(알려진 훅 레이스 현상). **F-025는 코드는 작성됐으나 미검증 상태이며 `구현 완료` 목록에 포함하지 않음.** 다음 세션 재개 시 안전 빌드 전체를 단독 실행해 PASS를 확인하고 비식별 사본으로 PDF 육안 확인부터 수행할 것.
- P3-01 (2026-09-18 F-025 교복 A/S 계획서 검증 완료): 직전 세션이 코드 작성만 마치고 중단한 F-025를 재개함. 좀비 EXCEL.EXE(PID 26012, 이전 세션 잔여)를 확인 후 종료하고, 세 스크립트의 F-025 관련 섹션(시트 정의, `검증_F025출력가능`/`F025업체행완전한가`/`F025임시시트생성`, 원문 대조·공란 경계 검증)을 F-021~F-023 패턴과 대조해 원본 HWPX `[9-14]`(업체명 표시 자리 없는 순수 텍스트 서식)와 구현이 일치함을 재확인함. 안전 빌드 전체(구조→VBA→검증)를 단독 실행해 `artifacts/excel/교복구매_길라잡이_20260918_v2.xlsm`을 생성했고, `verify_v1_log.txt` 전 항목 PASS(FAIL 0, F-025 관련 6개 검증 항목 포함, 기존 F-007·F-014~F-024·DB 저장/수정/불러오기 회귀 없음 포함)를 확인함. 비식별 사본(`artifacts/excel/_diagnostic/F-025_20260918_qa/`, 검증 후 삭제)에서 필수값(C4=검증초등학교, C5=2026, C12·C22)과 업체명(B44=검증업체가)만 입력해 F-025만 선택 출력함. PDF(76,637바이트, 1쪽)를 200dpi PNG로 렌더링해 제목 "교복 A/S 계획서", 4개 항목(A/S 기간·편의성·지정업체·내용) 머리글이 원문과 일치하고, 업체명이 본문 어디에도 출력되지 않으며, 날짜·수신처만 학교 공통값(2026년, 검증초등학교장 귀하)으로 자동 반영되고 나머지 전 항목이 수기 작성 공란임을 직접 확인함. 진단용 Excel COM 프로세스와 비식별 QA 사본은 검증 완료 후 정리함(사용자 실제 파일은 시작부터 끝까지 무관). 문서 갱신용 Edit들이 트리거한 훅 백그라운드 재검증이 실패해 `verify_v1_log.txt`가 한 차례 삭제됨(F-023 때와 동일한 알려진 훅 레이스 현상, 코드 변경 없음). 충돌 프로세스 없음을 재확인한 뒤 안전 빌드를 한 번 더 단독 실행해 `교복구매_길라잡이_20260918_v2.xlsm`과 클린 로그(FAIL 0건)를 재확보함(v1의 PDF 육안 확인 결과는 v2에도 그대로 유효함). 상세는 `_workspace/03_excel/F-025_구현검증.md`. 이로써 Excel 구현 완료 Form ID는 F-007·F-014·F-015·F-016·F-017·F-018·F-019·F-020·F-021·F-022·F-023·F-024·F-025(13종)이며, 나머지 39개 Form ID(52 - 13, F-013 포함)와 계약방법 전체 마법사가 남아 있음.
- P3-01 (2026-09-18 F-001 교복선정위원회 구성 기안 구현·검증 완료, 8.1절 1순위 착수): `계획.md` 8.1절 1순위(F-001~F-006, "구조적 공백")의 첫 항목. 로컬에 `artifacts/hwpx/P0-02_원본사본.hwpx`가 실제로는 존재하지 않음을 발견해(이전 세션들이 참조했으나 git 미추적·파일 부재), `_workspace/01_analysis/HWPX_원시구조_목록.md`가 기록한 원본 기준(`교복4차 (2)/교복 매뉴얼 작성서식0908(안정구 추가).hwpx`, `F007_F024_대조용_사본.hwpx`와 동일 크기 2,396,710바이트)을 복사해 재생성함(원본 자체는 열람만 함). 원본 HWPX 3쪽 `[1]`을 kordoc `parse_pages`로 대조해 F-007과 동일한 기안문 틀(수신/경유/제목/결재란)에 "라. 위원 명단" 표(순/구분/직급(직위)/성명/비고)가 추가된 구조임을 확인함. 위원 성명은 개인정보이므로, F-014~016 설계 시점에 R-01/R-02 공용 저장소로 마련됐으나 지금까지 어떤 서식도 소비하지 않던 `기초자료입력!B58:C67`("위원 반복행")·`DB_위원`을 그대로 재사용해(신규 DB 시트 불필요, 기초자료입력·기존 VBA 무변경) 마스킹 식별표시만 반영함. `scripts/build_excel_v1_structure.ps1`에 `F-001_위원회구성기안` 시트, `scripts/build_excel_v1_vba.ps1`에 `검증_F001출력가능()`과 선택 분기, `scripts/verify_excel_v1.ps1`에 필수 시트 29개·A4 배율·원문 대조 5건을 추가함. 1차 안전 빌드에서 `검증_F001출력가능()`이 다른 VBA 모듈의 Private 프로시저(`위원행검증`)를 직접 호출해 `HRESULT: 0x800A9C68` 런타임 오류가 발생함을 발견·수정함(공개 래퍼 `검증_위원행검증()`으로 교체). 좀비 EXCEL.EXE와 검증 락파일 충돌을 정리한 뒤 안전 빌드를 재실행해 `교복구매_길라잡이_20260918_v3.xlsm`을 생성했고 `verify_v1_log.txt` 전 항목 PASS(162/162, FAIL 0, 기존 F-007·F-014~F-025·DB 회귀 없음 포함)를 확인함. 비식별 사본(`artifacts/excel/_diagnostic/F-001_20260918_qa/`, 검증 후 삭제)에서 학교 공통값과 위원 3건(교원위원/위원 ○○, 학부모위원/위원 **, 학생위원/위원 ○*)만 입력해 F-001 단일 PDF를 내보내 200dpi로 렌더링·육안 확인함: 위원 명단 표에 마스킹 식별표시만 노출되고 실명이 전혀 없음, A4 1쪽 잘림 없음을 확인함. 매핑표 재확인 결과 F-001 행은 원문과 일치함(오류 없음). `입력데이터_사전.md`의 R-02 마스킹 형식 서술("위원 A~위원 J")과 기존 구현(○○/**/○*/*○ 기호)의 표기 불일치를 발견했으나 기존 VBA 무변경 원칙에 따라 기존 구현을 그대로 따르고 기록만 남김(P1 재검토 과제). 상세는 `_workspace/03_excel/F-001_구현검증.md`. F-002~F-006은 아직 미착수임.
- P3-01 (2026-09-18 F-002 위원 수락 및 확인서 구현·검증 완료): 원본 HWPX 4쪽 `[1-1]`을 대조해 표 없는 순수 서약 문서(제목·3단락 본문·날짜·주소/성명/서명 공란·수신처)를 구현함. `scripts/build_excel_v1_structure.ps1`에 `F-002_위원수락확인서` 시트, `scripts/build_excel_v1_vba.ps1`에 `검증_F002출력가능()`(필수값검증만 요구, 단일 출력)을 추가함. PDF 육안 확인에서 두 가지 실질적 Excel COM 버그를 발견·수정함: (1) 본문 3단락을 다중행 병합 셀(B6:E9)에 CHAR(10) 줄바꿈으로 넣으면 WrapText가 켜져도 인쇄 시 행 높이가 자동으로 늘지 않아 텍스트 상하단이 잘림 — 단락마다 단일행 병합으로 분리하고 `Rows.AutoFit()`으로 해결. (2) 서로 다른 3개 단일행 병합(B6:E6/B7:E7/B8:E8)에 걸쳐 병합 폭과 일치하지 않는 부분 범위("B6:B8", 열 B만)에 WrapText·VerticalAlignment를 한 번에 설정하면 Excel이 세 병합을 하나(B6:E8)로 자동 통합해 B7·B8 내용이 사라짐 — COM으로 `MergeArea.Address()`를 직접 조회해 재현·확인 후, 행마다 개별 서식 적용으로 해결(이 COM 함정은 F-003에도 동일 적용해 재발 방지). 안전 빌드 3회 반복(A4 폭 초과 여백 조정 포함) 끝에 `artifacts/excel/교복구매_길라잡이_20260918_v5.xlsm`을 생성했고 `verify_v1_log.txt` 전 항목 PASS(166/166, FAIL 0, 기존 F-001·F-007·F-014~F-025·DB 회귀 없음 포함)를 확인함. 비식별 사본에서 필수값만 입력해 F-002 단일 PDF를 200dpi로 렌더링·육안 확인함(3단락 잘림 없음, 주소·성명·서명란 전부 공란). 매핑표 재확인 결과 F-002 행은 원문과 일치함(오류 없음). 상세는 `_workspace/03_excel/F-002_구현검증.md`. F-003~F-006은 아직 미착수임.
- P3-01 (2026-09-18 F-003 위원 청렴 및 보안 서약서 구현·검증 완료, 중대 파서 결함 발견): 원본 HWPX 5쪽 `[1-2]`를 대조해 F-002와 동일한 성격의 서약 문서(제목·도입문구·4개 준수사항·날짜·서약자성명·수신처, 성명/서명/날짜 전부 공란)를 구현함. 구현 중 4개 준수사항 중 2~4번 항목이 매 빌드마다 병합조차 되지 않는 결함을 발견했는데, 처음에는 F-002 때와 같은 COM 자동화 문제로 오판해 재시도·지연·역순 실행·가비지컬렉션·Cells.Item 인덱스 전환 등 6차례 재빌드로 회피를 시도했으나 전부 동일하게 실패함(100% 동일 지점 재현으로 뒤늦게 "비결정적이 아님"을 인지함). 격리된 최소 재현 스크립트로 근본 원인을 확정함: 이 빌더가 UTF-8 무BOM 파일을 Windows PowerShell 5.1에서 올바르게 읽기 위해 `[System.IO.File]::ReadAllText()` + `[scriptblock]::Create()` 경로로 실행되는데, 이 경로에서 `@(...)` 배열 리터럴의 **첫 번째 원소가 `"a" + "b"` 문자열 연결식**이면 배열 전체가 원소 1개로 붕괴하는 파서 결함이 실제로 존재함(6줄 재현 스크립트로 100% 재현, 연결식 제거 시 즉시 정상화). `grep '@($'`로 스크립트 내 다른 모든 배열의 첫 원소를 전수 대조해 이 패턴을 쓴 곳이 F-003뿐임을 확인함(재발 방지 조치는 코드뿐 아니라 향후 배열 추가 시 유의사항으로 `_workspace/03_excel/F-003_구현검증.md`에 기록). `scripts/build_excel_v1_structure.ps1`에 `F-003_위원청렴보안서약서` 시트, `scripts/build_excel_v1_vba.ps1`에 `검증_F003출력가능()`, `scripts/verify_excel_v1.ps1`에 원문 대조 4건을 추가함(검증 과정에서 학교명을 하드코딩했다가 골든 시나리오 픽스처와 불일치로 실패해 `기초자료입력!C4` 실제값 참조로 일반화하는 부수 수정도 함). 안전 빌드 최종 실행으로 `교복구매_길라잡이_20260918_v6.xlsm`을 생성했고 `verify_v1_log.txt` 전 항목 PASS(173/173, FAIL 0, 기존 F-001·F-002·F-007·F-014~F-025·DB 회귀 없음 포함)를 확인함. 비식별 사본에서 필수값만 입력해 F-003 단일 PDF를 200dpi로 렌더링·육안 확인함(4개 준수사항 전부 잘림 없이 표시, 성명·서명·날짜 전부 공란). 상세는 `_workspace/03_excel/F-003_구현검증.md`. F-004~F-006은 아직 미착수임.
- P3-01 (2026-09-18 F-004 구매 추진 계획 수립 기안문 구현·검증 완료): 원본 HWPX 6쪽 `[2]`를 대조해 F-001과 동일한 기안문 틀(수신/경유/제목/결재란)에 위원 명단 표 없는 단순 2단락 본문(관련·목적)과 붙임 1건(F-005 참조)만 있는 구조를 확인함. `서식_매핑표.md` F-004 행이 원문에 없는 품목·금액(R-04~R-06/K-01~K-02) 필드를 가리키는 불일치를 발견했으나(F-023/F-025와 같은 유형) 기록만 하고 수정하지 않음. `scripts/build_excel_v1_structure.ps1`에 `F-004_구매추진계획수립기안문` 시트, `scripts/build_excel_v1_vba.ps1`에 `검증_F004출력가능()`을 추가함. 1차 안전 빌드는 자동 검증 176/176 PASS했으나 비식별 PDF 육안 확인에서 "2. 공정하고 투명한…" 2줄 문단이 기본 행 높이에서 위아래로 잘려 보이는 결함을 발견함(F-002 최초 결함과 동일 계열 — WrapText만 켜고 행 높이를 늘리지 않음). `Rows.Item("13:13").AutoFit()` + 최소 28pt 보장으로 수정해 재빌드했고 `교복구매_길라잡이_20260918_v8.xlsm`을 생성했다(`verify_v1_log.txt` 176/176 PASS 유지, 기존 F-001~F-003·F-007·F-014~F-025·DB 회귀 없음 포함). 비식별 사본 PDF 200dpi 재렌더링으로 문단 2줄이 잘림 없이 완전히 표시됨을 확인함. 자동 검증 통과가 조판 완전성을 보장하지 않음을 재확인한 사례로 기록함. 상세는 `_workspace/03_excel/F-004_구현검증.md`. F-005~F-006은 아직 미착수임.
- P3-01 (2026-09-19 F-005 구매 추진 계획안 구현·검증 완료, A4 1쪽 예외 적용): 원본 HWPX 7~12쪽 `[2-1]`을 대조한 결과 `_workspace/01_analysis/HWPX_원시구조_목록.md` 2절 구조 집계와 일치하게 원문이 6개 물리 페이지 분량의 장문 정책 설명 문서임을 확인함. F-001~F-004(모두 단일 물리 페이지)와 달리 F-005만은 "A4 1쪽 자연 배율" 원칙을 그대로 적용할 수 없어(6쪽을 1쪽에 압축하면 가독성·충실성을 해침) 이 서식만 예외적으로 자연스러운 다중 페이지 출력(강제 축소 없음, 실제 3쪽)을 허용하기로 결정하고 `scripts/verify_excel_v1.ps1`에 이 예외를 명시하는 별도 검증 항목을 추가함. 반영 범위: 학교명·학년도 공통값, F-001과 동일한 위원 반복행 재사용(위원회 구성 현황 표, 마스킹 식별표시만), F-007과 동일한 상한가격 합계 수식 재사용. 6쪽 분량 정적 안내문을 관리하기 위해 콘텐츠 스트림 기반 행 빌더(`F005Row` 헬퍼: title/h1/h2/item/note/blank/table3header/table3row 종류를 순서대로 호출하면 행 번호를 자동 증가시키며 서식 적용)를 새로 작성함. 구현 중 3가지 실질적 결함을 발견·수정함: (1) `.Formula`/`.Value2` 혼용 실수(등호 없는 수식 문자열 리터럴이 따옴표 문자까지 그대로 노출) — `F005SetCell` 헬퍼로 일괄 방어. (2) `Rows.AutoFit()`이 한 행에 병합 셀이 여러 개 있거나 폭이 넓은 병합 셀의 줄바꿈 높이를 정확히 계산하지 못하는 Excel 자체의 알려진 한계(Microsoft 공식 문서에도 기재) — AutoFit 의존을 버리고 텍스트 길이·병합 폭 기준으로 필요한 줄 수를 추정해 행 높이를 직접 계산하는 방식(`F005LineCount`)으로 전면 교체. (3) 한글 곡선 따옴표(‘’, U+2018/U+2019)가 `[scriptblock]::Create()` 실행 경로에서 텍스트를 그 직전에서 잘라버리는 파서 결함(F-003의 배열 붕괴와 같은 계열) — 이미 검증된 「」(F-003에서 사용)로 4곳 전부 교체해 해결. 안전 빌드 4회 반복(폭 초과 열너비 조정 1회, AutoFit→길이기반 계산 전환 1회, 곡선따옴표 치환 1회, 훅 레이스 락파일 충돌 재시도 1회) 끝에 `artifacts/excel/교복구매_길라잡이_20260918_v11.xlsm`을 생성했고 `verify_v1_log.txt` 전 항목 PASS(182/182, FAIL 0, 기존 F-001~F-004·F-007·F-014~F-025·DB 회귀 없음 포함)를 확인함. 비식별 사본에서 필수값과 위원 2건만 입력해 F-005 3쪽 PDF를 170dpi로 렌더링·육안 확인함(3회 반복 — 결함 2건 발견 후 최종 확인, 11개 절차표 행·위원 명단·상한가격·5개 장 제목·특수문자(→·※) 전부 잘림 없이 정상 표시). 상세는 `_workspace/03_excel/F-005_구현검증.md`. 이로써 Excel 구현 완료 Form ID는 F-001~F-005·F-007·F-014~F-025(19종)이며, F-006만 남아 있음. F-006(학교운영위원회 심의(안))은 착수함.
- P3-01 (2026-09-19 F-006 학교운영위원회 심의(안) 구현·검증 완료, 계획.md 8.1절 1순위 F-001~F-006 전체 완료): 원본 HWPX 13쪽 `[3]`을 대조해 안건번호/제출일자/제안자/제안설명자(원문 자체가 회의 소집 시 수기 기재하는 빈 칸)·1.제안이유(법조문+사유)·2.주요내용(가~라, 라.협의사항의 위원회 구성 표는 F-001의 위원 명단 표와 사실상 동일 구조)·붙임·"※ 참고 서식이니 해당학교의 학교운영위원회 서식을 사용할 것" 원문 안내문으로 구성된 단일 페이지 심의안임을 확인함. `서식_매핑표.md` F-006 행의 R-04~R-06(품목·수량·단가) 매핑 불일치를 F-004/F-005와 같은 유형으로 재확인(기록만, 미수정). `scripts/build_excel_v1_structure.ps1`에 `F-006_운영위원회심의안` 시트를 F-001의 직접 Range 주소 패턴과 F-005에서 확립한 COM/파서 함정 회피 원칙(`.Value2` 직접 대입, 「」만 사용)을 처음부터 적용해 작성함. 위원회 구성 표는 F-001과 완전히 동일한 `기초자료입력!B58:C67`("위원 반복행")을 재사용하고, 상한가격은 F-007·F-005와 동일한 `D29:D34` 합계 수식을 재사용해 신규 DB 시트를 만들지 않음. `scripts/build_excel_v1_vba.ps1`에 `검증_F006출력가능()`(F-001과 동일 검증 로직)을 추가함. **F-001~F-005에서 발견한 COM/파서 결함 중 어느 것도 재현되지 않아 첫 안전 빌드에서 즉시 전 항목 PASS했고**(빌드 실행 자체는 세션 전반의 훅 백그라운드 재검증 락파일 경합으로 2회 재시도 필요 — 코드 문제 아님, 기존에 알려진 훅 레이스 현상), `교복구매_길라잡이_20260919_v1.xlsm`을 생성했다(`verify_v1_log.txt` 189/189 PASS, FAIL 0, 기존 F-001~F-005·F-007·F-014~F-025·DB 회귀 없음 포함). 비식별 사본에서 필수값과 위원 2건만 입력해 F-006 단일 PDF를 200dpi로 렌더링·**1회만에** 육안 확인함(안건정보란 공란, 제안이유·상한가격·위원회구성표·참고서식 안내문까지 A4 1쪽 안에서 잘림 없이 정상 표시). 상세는 `_workspace/03_excel/F-006_구현검증.md`. 이로써 Excel 구현 완료 Form ID는 F-001~F-006 전체(6종)를 포함해 F-007·F-014~F-025까지 총 20종이며, 계획.md 8.1절 2~6순위(F-032~F-052 등 31종+후순위 4종)가 남아 있음.
- P3-01 (2026-09-19 F-041~F-043 낙찰자 결정·통보·계약체결 구현·검증 완료, 계획.md 8.1절 2순위 전체 완료): 원본 HWPX 68~73쪽(`kordoc "artifacts/hwpx/P0-02_원본사본.hwpx" -p 68-73`)을 대조해 F-041(`[14]` 70쪽, 기안문·수신=내부결재)·F-042(`[15]` 71쪽, 통보문·수신=낙찰 업체명)·F-043(`[16]` 72쪽, 기안문·수신=내부결재) 구조를 확인함. F-017~F-025는 업체 반복행 전체를 순회해 업체마다 별도 문서를 만들지만, 낙찰자·계약상대자는 항상 1개 업체뿐이므로 F-015의 "I3 선택 순번 → `INDEX(기초자료입력!$B$44:$B$53,I3)`" 단일 선택 패턴을 재사용해 세 시트가 업체 반복 루프 없이 F-001/F-006과 같은 단일 출력으로 동작하도록 구현함(`Module_출력`에 검증 함수 3건과 skip 카운터 추가, 업체행 루프 없음). 대표자·소재지·전화번호·비고는 F-019~F-023과 동일하게 전부 수기 작성 공란으로 유지함. `서식_매핑표.md`의 F-041 계산 필드(K-02 합계금액)는 원문에 반복 품목·합계 구조가 없어 실제로는 적용되지 않는 매핑 불일치로 확인함(F-004~F-006·F-023·F-025와 같은 유형, 기록만 하고 매핑표는 수정하지 않음). F-043만 원문의 명시적 산식("나. 계약금액：금00,000,000원[000,000원(단가)×○○벌(예정수량)]")에 따라 신규 단가 입력영역(연노랑)×기존 `기초자료입력!C16`(B-05 예정수량)으로 K-01/K-02 계산 필드를 실제로 구현함. 기존 `기초자료입력!C15`(B-04 기초금액)·C16(B-05 예정수량)·C17(B-06 납품기한)·C18(B-07 제출기한, 이전까지 어떤 서식도 소비하지 않던 필드)을 재사용하고 기초자료입력·기존 VBA는 변경하지 않음(새 시트 3개·새 VBA 함수만 추가). `scripts/verify_excel_v1.ps1`에 필수 시트 34→37개, A4 1쪽 검사 목록, 원문 대조·개인정보 경계·선택 출력 자동 검증 20건, 골든 테스트 데이터(`기초자료입력!C15~C18`)를 추가함. 안전 빌드 2회 실행(1차 `교복구매_길라잡이_20260919_v3.xlsm`, 216/216 PASS) 후 비식별 사본 PDF 170dpi 육안 확인에서 F-042 "계약체결금액(원)" 표 헤더가 열 폭을 넘어 다음 칸과 겹치는 결함을 발견해 F열 너비·폰트 크기를 수정하고 재빌드함(2차 `교복구매_길라잡이_20260919_v4.xlsm`, 216/216 PASS, FAIL 0, 기존 F-001~F-025·DB 회귀 없음 포함). 비식별 사본(`artifacts/excel/_diagnostic/F-041_043_20260919_qa/`)에서 학교명·업체명(검증업체가/검증업체나)·단가만 입력해 F-041~043 3종을 하나의 PDF(3쪽, 124,558바이트)로 결합 출력하고 170dpi로 렌더링·육안 확인함: A4 1쪽 잘림 없음, 낙찰 업체명만 반영, 대표자·소재지·전화번호·비고 전부 공란, F-043 계약금액(45,000원×100벌=4,500,000원) 자동 계산 정확함을 확인함. 상세는 `_workspace/03_excel/F-041_043_구현검증.md`. 이로써 Excel 구현 완료 Form ID는 F-001~F-006·F-007·F-014~F-025·F-041~F-043(22종)이며, 계획.md 8.1절 3순위(F-032~F-040, 평가그룹 9종)가 남아 있음.
- P3-01 (2026-09-23 입력 스키마 확장 — C-08·R-08·R-09 신설, R-02 마스킹 폐지, 47개 서식 개별 반영은 미착수): 사용자의 개인정보 자동화 경계 확대 결정에 따라 `기초자료입력`에 C-08(담당자 성명, C10)·C-07(교장 성명, F10)을 신설하고, 업체 반복행에 R-08(대표자 성명, S열)·R-09(대표자 연락처, X열)를 신설하며, 위원 반복행(C58:C67)의 마스킹 승인목록 검증을 폐지하고 2~30자 실명 형식만 검사하도록 변경함. `DB`(V·W열, 22·23)·`DB_업체`(4·5열)·`DB_위원`(4열 헤더 변경) 구조와 저장/수정/불러오기/라이브 검증/배치 저장 경계 검증을 모두 갱신함. **자체 발견·수정한 구현 버그**: 최초 구현에서 신규 DB 열을 21·22열에 배치했는데 21열(U)은 기존 "불러오기표시라벨" 수식이 이미 점유 중이어서 불러오기 기능 자체가 깨지는 회귀를 안전 빌드 자동 검증으로 발견·수정함(22·23열로 재배치). `scripts/verify_excel_v1.ps1`에 신규 골든 시나리오 9건(X-47~X-55)을 추가하고 구 마스킹 정책 전제 시나리오 8건을 새 정책에 맞게 수정함. 안전 빌드 전체(구조→VBA→검증) 최종 PASS(FAIL 0건)로 `artifacts/excel/교복구매_길라잡이_20260923_v2.xlsm`을 생성함. **완료 범위는 입력 스키마·DB·매크로뿐이며, 47개 서식 개별 시트의 결재란(담당/교장 성명 반영)·업체 반복행(대표자 반영)·위원 반복행(실명 표시) 자체 반영은 포함하지 않음** — 결재란은 16개 지점(F-007 1곳만 별도 서명란 박스, 나머지 15곳은 "시행/접수" 행 바로 위 한 줄짜리 헤더라 이름 넣을 별도 칸이 원래 없어 행 삽입 또는 셀 내 줄바꿈+행높이 조정이 필요하고 두 방법 모두 47개 서식 PDF 재검증이 필요함)를 코드 조사로 특정만 해 둠. R-08/R-09 반영 대상 서식 13종 이상(F-017~025, F-030~033, F-038, F-041~043)도 코드 조사로 특정함. 같은 세션에서 코디네이터가 추가로 제기한 버그 6건·요청 6건(총 12건, 결재란 반영과 별개로 입찰공고 페이지 잘림·미사용 표 칸·정성평가 드롭다운·DB 시트 이동·저장불러오기 재현·미리보기PDF 게이트 완화·PDF 자동열기·HWPX 버튼·사용설명서 재작성·절차안내 시트명 변경·계약방법 재반영 회귀·2단계입찰 팝업 누락)는 각각 독립된 원인조사·구현·PDF 검증이 필요한 규모라 이번 라운드에서 착수하지 않고 `로그.md` 2026-09-23 후속 항목에 우선순위와 함께 인계함. 상세는 `로그.md` 2026-09-23 "excel-automation-engineer: 입력 스키마 구현·검증 완료" 항목과 `서식_매핑표.md` 6절, `test.md` 9절.
- P3-01 (2026-09-24 사용자 지시 13건 후속 세션): 이전 커밋(`07b6a2c`)이 "완료"라 주장한 항목도 재확인 없이 신뢰하지 말라는 지시에 따라 재조사함. **신규 원인 규명·수정 6건**: ①HWPX 작성 종료 코드 1(미저장 상태에서 스크립트가 디스크의 빈 파일을 읽는 문제 + 실패 사유 미노출 문제, 2가지 원인 모두 CLI 재현으로 확정·수정), ⑥사용설명서 이미지(소스 PNG는 이미 정상으로 교체돼 있었으나 인쇄영역이 가로 배너 폭을 못 따라가 STEP 03이 잘리는 결함을 PDF 확인 중 발견·수정), ⑦계약절차 팝업 버튼 여백 확대, ⑪빨간 안내문 재출현(F-021·022·023·025 4개 서식만 안내문이 A1 아닌 B1에 있고 접두어도 달라 기존 정리 로직이 못 잡던 것을 COM으로 확인·수정), ⑫서식선택_출력 행별 "이동" 버튼 47개 신설, ⑬F-050 이동 버튼 누락(구조 빌드 단계의 시트 보호로 VBA 빌드 단계의 버튼 추가가 조용히 실패하던 것을 COM으로 확인·수정, F-049는 이미 있었음을 확인). **재검증만(코드 변경 없음) 6건**: ③④⑤⑧⑨⑩ — 코드는 그대로였고 관련 골든 테스트가 이번 안전 빌드 전 항목 PASS에 포함됨을 확인했으나 PDF 재출력을 통한 육안 재확인은 하지 않음(미확인으로 기록). **미완료 1건**: ②표 단일 입력 병합 전수조사 — 55건의 표 선언을 서식별로 매핑했으나 "의도된 다중행 표"와 "입력 1개뿐인 표"를 코드만으로 안전하게 구분할 근거가 부족해 임의 수정하지 않음(지난 세션과 동일 결론, 사용자의 구체적 서식 지목 또는 별도 PDF 전수 시각 검토가 필요). 안전 빌드 최종 PASS(FAIL 0건, `artifacts/excel/교복구매_길라잡이_20260924_v13.xlsm`). 상세는 `로그.md` 2026-09-24 "사용자 지시 13건 후속 세션" 항목, `test.md` X-72~X-81.

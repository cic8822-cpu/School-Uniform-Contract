# Phase 체크리스트 — 교복구매 길라잡이

## P0 완료 게이트

- [x] 원본 HWPX·XLSM을 읽기 전용으로 지정함.
- [x] XLSM 기준 SHA-256을 기록함.
- [x] Kordoc 파싱·필드탐색·fill dry-run·patch·validate·render의 사본 기술검증을 완료함. 실제 값 치환·한컴/PDF 검증은 P2에서 수행함.
- [x] XLSM 시트·이름정의·외부링크·연결·VBA 존재·인쇄 흐름을 정적 분석으로 분류함.
- [x] `엑셀_벤치마크_분석.md`를 작성하고 독립 품질검토 보완을 반영함.
- [x] `test.md` 공통 품질 게이트 Q-01~Q-05를 실제 검사 후 PASS로 갱신함.
- [x] [계획.md](계획.md)·[task.md](task.md)·[로그.md](로그.md) 상태를 갱신함.

## P1 완료 게이트

- [x] HWPX 사본의 원시 구조·표·이미지·서식 경계 후보를 순서대로 기록함.
- [x] 실제 HWPX 서식·참고자료·이미지 수를 분리해 기록함.
- [x] 모든 MVP 서식에 중복 없는 Form ID를 부여함.
- [x] 공통·문서별·반복·계산 필드를 분리함.
- [x] 필수·형식·개인정보 검증 규칙을 기록함.
- [x] 대표 HWPX POC 3종과 검토 책임자를 확정함.

## P1-02 작업 게이트

- [x] 원시 표제 후보 58개와 운영·참고 문서 57개의 차이를 기록함.
- [x] `F-001`~`F-057`에 중복 없는 Form ID를 부여함.
- [x] 원본의 중복 표제 `[9-7]`과 참조자료 `[9-13]`를 별도 규칙으로 처리함.
- [x] 개인정보 가능 문서의 허용·금지 자동 반영값과 마스킹 시험 원칙을 기록함.
- [x] 독립 검토의 Critical·High 결함을 수정하고 I-01~I-03을 재검증함.

## P1-03 작업 게이트

- [x] 공통·문서별·반복·계산·보호 필드의 정의·형식·필수·검증 규칙을 기록함.
- [x] `F-001`~`F-052`의 입력 출처·필수 조건·계산·출력 경계를 매핑함.
- [x] `F-053`~`F-057`을 읽기 전용 참고자료로 분리함.
- [x] 개인정보 가능 문서의 금지 필드와 HWPX 치환·로그·Git·웹 전송 차단을 기록함.
- [x] 독립 검토 및 직접 구조 검사에서 Critical·High 결함이 없음을 확인함.

## P1-04 작업 게이트

- [x] 출력 서식 52종과 읽기 전용 참고자료 5종의 범위를 57종 기준으로 확정함.
- [x] 단순 기안문, 반복·계산, 표·이미지 복합 유형을 각각 1종씩 POC로 선정함.
- [x] 개인정보 가능 문서를 대표 POC에서 제외하고 마스킹 시험 원칙을 확인함.
- [x] 범위·POC 3종·한컴/PDF 수동 검토 책임에 대한 업무 담당 승인을 기록함.
- [x] 독립 검토 및 직접 구조 검사에서 Critical·High 결함이 없음을 확인함.

## P2 완료 게이트

- [x] 원본 HWPX 전체 사본의 F-007 구간에만 허용 공통값 명시 토큰을 삽입하고, 원본 해시가 변하지 않음을 확인함.
- [x] F-007 원본 보존형 템플릿·골든 결과에서 ZIP 엔트리·F-007 경계 밖 XML·토큰 0·XML 유효성·`validate`를 확인함.
- [x] F-007을 포함한 47종을 Form ID 단위 원본보존형 템플릿으로 분리하고, 각 골든결과의 한컴 PDF 대조를 완료함(P2-04, 2026-09-23).
- [x] F-024·F-013에도 원본 표·이미지·스타일을 재조립하지 않는 명시 토큰 방식을 적용함(P2-04, 2026-09-23).
- [x] 원본 보존형 생성본을 한컴에서 열어 원본과 PDF로 비교함(2026-09-21, 사용자 결정으로 AI 시각 확인이 사람 검토를 대체함 — `P2-02_한컴검토_체크리스트.md` 참고, 결함 0건).
- [x] 비식별 회차·문서역할 파일명과 Form ID별 템플릿·PDF·검증기록 보관 경로를 `artifacts/hwpx/P2-04/` 및 `artifacts/hwpx/P2-04_생성/`으로 확정함.
- [x] HWPX 출력 ADR을 승인함. — `ADR-001-HWPX출력방식.md`(2026-09-25 신규)로 확정. 결정: Excel VBA가 로컬 PowerShell 보조도구(`export_hwpx_p204_from_excel.ps1` 등)를 호출해 HWPX를 생성하는 방식(D3, 2026-09-15 CONFIRMED)을 그대로 채택함. 48/48 서식 생성 성공과 한컴 육안 확인(2026-09-24)으로 기술적 동작을 검증했고, 사용자가 자신의 PC 2대(사무실·자택)를 오가며 반복 확인한 결과를 근거로 실제 학교 행정실 PC에서의 별도 현장 검증(P4)을 갈음하기로 결정함(2026-09-25 사용자 결정 — 신규 담당자·제3자 PC에서의 독립 검증은 아님을 기록).

## P3 완료 게이트

- [x] 새 XLSM의 외부 링크·연결·`#REF!` 이름 정의가 0건임(`scripts/verify_excel_v1.ps1` 사본 검증 PASS).
- [ ] 골든 테스트 E-01~E-06을 통과함.
- [x] 사용설명서와 행정실 최종 확인 절차를 제공함. — `사용설명서` 시트와 `운영_최종확인절차.md`에 입력·저장·출력·개인정보·계약방법·PDF 확인 책임을 명시함.

## P3-01 작업 게이트

- [x] 원본 XLSM을 복제하지 않고 `artifacts/excel/`에 클린룸 XLSM 구조 초안을 생성함.
- [x] Microsoft Excel 재열기, `#REF!` 0건, Excel·OOXML 외부 의존성 0건을 직접 확인함(`교복구매_길라잡이_Excel_v1.xlsm` 기준으로 재확인).
- [x] 기준 XLSM의 실제 입력 화면·서식 시트·자동 반영·선택 출력·인쇄 흐름을 직접 분석함(VBA 소스 전문 열람, `_workspace/03_excel/기준XLSM_UI구조_분석.md` 6장).
- [x] 기준 XLSM 수준의 실무 입력 화면과 필수값 오류 표시를 구현함 — 기초자료 입력·신규초기화/저장/수정/불러오기·필수값 검증·`DB_품목`·`DB_업체`·`DB_위원`·`DB_평가` 반복행 저장, 120행 공개 기관정보의 학교명·지역·급별 검색과 C4 반영, 교복구매 9단계·대표 Form ID 이동, 계약방법안내의 유형 선택+법령확인 기반 안내를 사본 자동 검증함. 위원 실명, 업체 대표자 성명·연락처(R-08·R-09), 담당자 성명(C-08)·교장 성명(C-07)을 저장·복원하고, 47개+ 구현 서식의 결재란 15종 및 업체 정보 11종에 반영함(2026-09-23~24). 저장·수정 버튼은 DB 기록 뒤 파일 저장까지 수행함. **법령·예외사유를 자동 판정하는 계약방법 금액 구간 마법사는 법적 위험으로 인해 영구 미구현으로 확정**(2026-09-20 사용자 결정, 유형 선택+법령확인 수동 절차로 대체 — X-06 주3 참고).
- [x] 대표 서식의 표·병합·인쇄 조판과 선택 출력 기능을 Excel/PDF로 직접 비교함 — F-001~F-050(F-013 제외) 51종을 원문 HWPX와 대조해 A4 1쪽 자연 배율(또는 명시적 예외)로 구현하고 PDF 육안 확인까지 완료함(`test.md` 5.2절, X-01~X-81 각 서식 항목). F-007·F-013 우편번호~이메일 로컬 입력, F-024 단가비율 자동계산 방식 등 일부 잔여 한계는 문서화된 상태로 유지함(`_workspace/03_excel/F-007_F024_수정후_재대조.md` 5절). F-013만 표·이미지 복합조판이라 Excel 구현을 보류하고 HWPX 경로(P2-04)로 위임함(설계상 확정, 재작업 대상 아님).
- [x] 기준 기능과 동등한 결과를 확인한 뒤에만 52개 Form ID 확장을 완료 처리함 — **2026-09-25 최종 확인**: 52개 중 51개(F-001~F-012, F-014~F-052) 구현·검증(PASS) 완료, 1개(F-013)만 표·이미지 복합조판 사유로 HWPX 경로 위임(설계상 확정, 미구현이 아님). 개인정보 특별취급 3종(F-029·047·050)은 구현됐으나 개인정보 필드만 의도적으로 공란 유지(사용자 지시 반영, 서식 자체는 PASS). 남은 항목 없음(2026-09-25 사용자 결정으로 P3-01 완료 확정).

## P3-01 F-049 구현 검증

- [x] 원본 HWPX 78쪽 `[19]`의 표제·수신·관련·대상·내용·붙임·결재 영역을 직접 대조함(F-044/F-046과 동일 구조).
- [x] 학교·학년도·문서번호·발행일·구매명·제목 공통값만 반영하고, 응답자 개인정보를 입력·저장·자동 출력하지 않음(붙임 설문지 본문은 F-050으로 분리해 이 시트에서 재현하지 않음).
- [x] 선택 출력 검증과 A4 자연 배율(`Zoom=100`, `HPageBreaks=0`, `VPageBreaks=0`)을 자동 검증함(290/290 PASS).
- [x] 비식별 QA 사본에서 PDF를 직접 렌더링해 A4 1쪽·잘림 없음·개인정보 미출력을 확인함(1차 렌더링에서 "나. 내용:" 줄 우측 잘림을 발견해 WrapText 보정 후 재확인).

## P3-01 F-050 구현 검증

- [x] 원본 HWPX 79쪽 `[19-1]`의 만족도 문항·5점 척도·소계·총점/평균·기타 의견 구조를 직접 대조함.
- [x] 학교·학년도·설문 제목만 반영하고, 개인 응답·집계·자유서술은 수식·기본값·저장 경로 없이 완전 공란으로 유지함(High 결함 수정 완료, 안전 빌드 자동 검증 재확인).
- [x] 인쇄 전용 시트 보호 및 응답 원자료 저장·PDF 출력 거부 음성 검증을 포함한 새 안전 빌드·PDF 생성 확인 완료(`교복구매_길라잡이_20260920_v9.xlsm`, `verify_v1_log.txt` 전 항목 PASS). 이 검증을 막고 있던 별도 결함(`F050원자료없음`의 모듈 간 `Private` 참조 VBA 컴파일 오류)도 함께 발견·수정함.

## P3-01 F-048 구현 검증

- [x] 원본 HWPX 77쪽 `[18-3]`의 인사말·본문 4개 문단·참고·절취선·신청서·신청 수량 표·각주를 직접 대조함.
- [x] 학교명·학년도·구매명·제출기한(B-07) 공통값만 반영하고, 이름·보호자 성명·전화번호·신청수량은 입력·저장·자동 출력하지 않음(표 12개 셀 완전 공란).
- [x] 선택 출력 검증과 A4 1쪽 예외(F-005와 동일 기준: `VPageBreaks=0`, 강제 축소 없음)를 자동 검증함(278/278 PASS).
- [x] 비식별 QA 사본에서 PDF를 2쪽으로 직접 렌더링해 신청 수량 표 전체가 1쪽에 담기고 잘림·분절이 없음을 확인함.

## P3-01 F-046 구현 검증

- [x] 원본 HWPX 75쪽 `[18]`의 제목·수신·관련·대상·내용·붙임·결재 영역을 직접 대조함(F-044와 동일 구조).
- [x] 학교·학년도·문서번호·발행일·구매명 공통값만 반영하고, 학생·학부모 개인정보를 입력·저장·자동 출력하지 않음.
- [x] 선택 출력 검증과 A4 자연 배율(`Zoom=100`, `HPageBreaks=0`, `VPageBreaks=0`)을 자동 검증함(271/271 PASS).
- [x] 비식별 QA 사본에서 PDF를 직접 렌더링해 A4 1쪽·잘림 없음·개인정보 미출력을 확인함.

## P3-01 F-045 구현 검증

- [x] 원본 HWPX 74쪽 `[17-1]`의 표제·인사말·본문 4개 문단·감사 인사·발행일·학교장 표기를 직접 대조함.
- [x] 이전 세션이 남긴 3가지 gap(`$implemented` 미등록·전용 검증 없음·PDF 미확인)을 모두 메움.
- [x] 학교·학년도·구매명·표제(D-02) 공통값만 반영하고, 학생·학부모 개인정보를 입력·저장·자동 출력하지 않음.
- [x] 선택 출력 검증과 A4 자연 배율(`Zoom=100`, `HPageBreaks=0`, `VPageBreaks=0`)을 자동 검증함(266/266 PASS).
- [x] 비식별 QA 사본에서 PDF를 직접 렌더링해 A4 1쪽·잘림 없음·개인정보 미출력을 확인함.

## P3-01 F-044 구현 검증

- [x] 원본 HWPX 73쪽 `[17]`의 제목·수신·관련·대상·내용·붙임·결재 영역을 직접 대조함.
- [x] 학교·학년도·문서번호·발행일 공통값만 반영하고, 학생·학부모 성명·연락처·배정 세부정보를 입력·저장·자동 출력하지 않음.
- [x] 선택 출력 검증과 A4 자연 배율(`Zoom=100`, `FitToPagesWide/Tall=False`)을 자동 검증함.
- [x] 공통값 실제 반영 누락을 코드 검토로 발견·수정한 뒤 안전 빌드가 통과한 `교복구매_길라잡이_20260919_v12.xlsm`에서 PDF를 직접 렌더링해 A4 1쪽·잘림 없음·수기 영역 보존을 확인함.

## P3-01 F-016 완료 검증

- [x] 업체별 정성평가 입력 범위·완전성 검증과 고아 점수 차단을 구현함.
- [x] `DB_정성평가` 저장·수정·불러오기·저장경계와 `Workbook_Open` 재보호를 검증함.
- [x] 선택 출력·업체별 임시 시트·PDF 결합·임시 시트 정리를 검증함.
- [x] 안전 빌드로 `교복구매_길라잡이_20260917_v3.xlsm`을 생성하고 최종 파일을 읽기 전용 재열기함.

> 체크 항목은 실제 증빙 확인 후에만 변경함. Critical/High 결함이 남은 경우 다음 Phase로 이동하지 않음.

## P5-02 작업 게이트

- [ ] 9단계 탐색 허브에서 단계 상세와 관련 Form ID로 이동하는 흐름을 설계함.
- [ ] 단계 상세에 해야 할 일·사전요건·주의사항·공식 근거·관련 서식·다음 행동을 분리함.
- [ ] 관련 Form ID의 작성 화면에서 허용 필드·필수값 오류·미리보기·다운로드 흐름을 정의함.
- [ ] 본문 바로가기·키보드 조작·명시적 레이블·색상 외 상태·인접 오류 안내·모바일 기준을 설계·점검함.
- [ ] 최신 공식 원문 확인과 행정실 최종 검토 안내를 화면에 반영함.
## P3-01 F-019 완료 검증

- [x] 업체명(R-03)·학교 공통정보(C-01·C-02·D-02) 반영과 법인등록번호·주소·전화번호·주민등록번호·대리인·사용인감 빈칸 유지를 구현함.
- [x] A4 1쪽 자연 배율 결함(가로 방향 `HPageBreaks=1`)을 발견·수정하고 `Zoom=100`, `FitToPagesWide/Tall=False`, `HPageBreaks=VPageBreaks=0`을 확정함.
- [x] 선택 출력·업체별 임시 시트(`F019_임시_*`)·PDF 결합·임시 시트 정리를 검증함.
- [x] 안전 빌드로 `교복구매_길라잡이_20260917_v8.xlsm`을 생성하고 `verify_v1_log.txt` 132건 전 항목 PASS(기존 F-007·F-014·F-015·F-016·F-017·F-018·F-024·DB 회귀 없음 포함)를 확인함.
- [x] PDF 렌더링을 직접 읽어 1쪽 조판과 업체명·학교정보 반영, 법인·연락처·대표자·대리인·인감 빈칸을 시각 확인함.
- [x] 빌드·VBA·검증 스크립트 실행 전후 EXCEL.EXE 프로세스 수를 비교해 좀비 프로세스가 없음을 확인함.

## P3-01 F-020 구현 검증

- [x] 업체명(R-03)·학교 공통정보(C-01·C-02·D-02) 반영과 사업자 번호·대표자·사용인감 빈칸 유지를 구현함.
- [x] A4 1쪽 자연 배율(`Zoom=100`, `FitToPagesWide/Tall=False`, `HPageBreaks=VPageBreaks=0`)을 자동 검증으로 확인함.
- [x] 선택 출력·업체별 임시 시트(`F020_임시_*`)·PDF 결합·임시 시트 정리를 검증함.
- [x] 안전 빌드로 `교복구매_길라잡이_20260917_v9.xlsm`을 생성하고 `verify_v1_log.txt` 전 항목 PASS(기존 F-007·F-014~F-019·F-024·DB 회귀 없음 포함)를 확인함.
- [x] PDF 렌더링을 사람이 직접 읽어 시각 확인함 — 2026-09-18 쓰기 가능 비식별 사본에서 F-020 단일 시트를 PDF로 내보내고 160dpi 렌더링을 직접 검토함. A4 1쪽 조판·업체명/학교명 반영·사업자 번호/대표자 입력칸 공란·직인 수기 안내를 확인했음. 증빙은 `_workspace/03_excel/F-020_구현검증.md` 및 `artifacts/excel/_diagnostic/F-020_20260918_manual_qa/F020_visual_QA_110058.pdf`에 보관함.
- [x] 빌드·VBA·검증 스크립트 실행 전후 EXCEL.EXE 프로세스 수를 비교해 좀비 프로세스가 없음을 확인함.

## P3-01 F-021 구현 검증

- [x] 원본 HWPX `[9-9] [서식 4]`(일반현황·연혁, 제안(제출)서류 목록, 제안자 서명란)와 시트 구조를 직접 대조함.
- [x] 업체명(R-03)·학교 공통정보(C-01·C-02) 반영과 대표자·사업분야·주소·연락처·회사설립년도·직원현황·사업기간·연혁·제안자 성명·서명 빈칸 유지를 구현함.
- [x] A4 1쪽 자연 배율(`Zoom=100`, `FitToPagesWide/Tall=False`, `HPageBreaks=VPageBreaks=0`)을 자동 검증으로 확인함.
- [x] 선택 출력·업체별 임시 시트(`F021_임시_*`)·PDF 결합·임시 시트 정리를 검증함.
- [x] 안전 빌드로 `교복구매_길라잡이_20260918_v6.xlsm`을 생성하고 `verify_v1_log.txt` 146개 항목 전부 PASS(기존 F-007·F-014~F-020·F-024·DB 회귀 없음 포함)를 확인함. 이 과정에서 이전 세션이 부분 작성해 둔 F-022 섹션의 빌드 차단 결함(타입 캐스팅 오류·A4 1쪽 초과)을 발견해 최소 수정함(F-022 자체는 완료 처리하지 않음).
- [x] PDF 렌더링을 사람이 직접 읽어 시각 확인함 — 2026-09-18 두 세션(구현 세션·재검증 세션)에서 각각 별도 비식별 사본으로 F-021 단일 시트를 PDF로 내보내고 렌더링을 직접 검토함. A4 1쪽 조판·업체명/학교명 반영·대표자/주소/연락처/연혁/서명 등 개인정보 필드 공란을 확인했음. 증빙은 `_workspace/03_excel/F-021_구현검증.md`, `artifacts/excel/_diagnostic/F-021_20260918_manual_qa/`, `artifacts/excel/_diagnostic/F-021_20260918_session2_qa/`에 보관함.
- [x] 빌드·VBA·검증 스크립트 실행 전후 EXCEL.EXE 프로세스 수를 비교해 좀비 프로세스가 없음을 확인함. 진단 과정에서 발생한 정지 상태의 Excel 프로세스도 모두 확인 후 종료했고, 사용자가 별도로 열어 둔 무관한 통합문서(PID 19076)는 건드리지 않음.

## P3-01 F-022 구현 검증

- [x] 원본 HWPX `[9-10] [서식 5]`를 kordoc `parse_pages`/`extract_tables`로 직접 대조함 — 실적행이 9행(머리글 제외)임을 확인하고, 기존 8행 부분 구현에 1행을 추가함.
- [x] 업체명(R-03)·학교 공통정보(C-01·C-02)·고정 제목(D-02) 반영과 실적행 6열(사업명·사업기간·계약금액·발주처·비고) 9행 전체의 수기 작성 공란 유지를 `서식_매핑표.md` 매핑과 대조해 확인함.
- [x] A4 1쪽 자연 배율(`Zoom=100`, `FitToPagesWide/Tall=False`, `HPageBreaks=VPageBreaks=0`)을 실적행 추가 후 자동 검증으로 재확인함.
- [x] 선택 출력·업체별 임시 시트(`F022_임시_*`)·PDF 결합·임시 시트 정리를 검증함.
- [x] `scripts/verify_excel_v1.ps1`의 F-022 자동 검증이 참조하던 구(舊) 8행 레이아웃 고정 셀 주소(B19·B23)를 새 레이아웃(B20·B24)으로 갱신함.
- [x] 안전 빌드를 훅(`verify-phase.ps1`)과의 검증 스크립트 잠금 경합 없이 단독 실행해 `교복구매_길라잡이_20260918_v8.xlsm`을 생성하고 `verify_v1_log.txt` 146개 항목 전부 PASS(기존 F-007·F-014~F-021·F-024·DB 회귀 없음 포함)를 확인함.
- [x] PDF 렌더링을 사람이 직접 읽어 시각 확인함 — 2026-09-18 비식별 사본에서 F-022 단일 시트를 PDF로 내보내고 170dpi 렌더링을 직접 검토함. A4 1쪽 조판·업체명 반영·실적행 9행·실적행 6열 공란을 확인했음. 증빙은 `_workspace/03_excel/F-022_구현검증.md` 및 `artifacts/excel/_diagnostic/F-022_20260918_qa/output/선택서식_20260918_162715.pdf`에 보관함.
- [x] 빌드·VBA·검증 스크립트 실행 전후 EXCEL.EXE 프로세스 수를 비교해 좀비 프로세스가 없음을 확인함. 사용자가 별도로 열어 둔 무관한 통합문서(PID 19076)는 건드리지 않음.

## P3-01 F-023 구현 검증

- [x] 원본 HWPX `[9-11] [서식 6]`을 kordoc `parse_pages`/`extract_tables`로 직접 대조함 — 동복 4행·하복 2행 견본 품목 구조를 확인함.
- [x] `서식_매핑표.md`의 F-023 행(R-04~R-06/K-01, 품목·수량·단가·금액)이 원문에 없는 필드를 가리키고 F-013 행과 문구가 동일한 복사 오류임을 확인하고, 실제 원문과 F-017~F-022 패턴에 따라 업체명(R-03)·학교 공통정보(C-01·C-02)만 반영하도록 구현함(매핑표 자체는 P1 산출물이라 이번 세션에서 수정하지 않고 재검토 과제로 기록).
- [x] 재질·설명 내용 4개 열(동복 4행·하복 2행)과 서명란의 수기 작성 공란 유지를 확인함.
- [x] A4 1쪽 자연 배율(`Zoom=100`, `FitToPagesWide/Tall=False`, `HPageBreaks=VPageBreaks=0`)을 자동 검증으로 확인함.
- [x] 선택 출력·업체별 임시 시트(`F023_임시_*`)·PDF 결합·임시 시트 정리를 검증함.
- [x] 안전 빌드를 훅(`verify-phase.ps1`)과의 검증 스크립트 잠금 경합 없이 단독 실행함(1차 `v9` 생성 후 문서 갱신 Edit이 트리거한 훅 재검증 실패로 로그가 삭제돼 2차로 `교복구매_길라잡이_20260918_v10.xlsm`을 재생성). `verify_v1_log.txt` 151개 항목 전부 PASS(기존 F-007·F-014~F-022·F-024·DB 회귀 없음 포함)를 확인함.
- [x] PDF 렌더링을 사람이 직접 읽어 시각 확인함 — 2026-09-18 비식별 사본에서 F-023 단일 시트를 PDF로 내보내고 렌더링을 직접 검토함. A4 1쪽 조판·업체명 반영·동복 4행/하복 2행 견본 품목·재질/설명 내용/서명란 공란을 확인했음. 증빙은 `_workspace/03_excel/F-023_구현검증.md` 및 `artifacts/excel/_diagnostic/F-023_20260918_qa/output/선택서식_20260918_165516.pdf`에 보관함.
- [x] 빌드·VBA·검증 스크립트 실행 전후 EXCEL.EXE 프로세스 수를 비교해 좀비 프로세스가 없음을 확인함. 진단용 Excel COM 프로세스가 `Quit()` 이후 좀비로 남은 것을 확인 후 종료했고, 사용자가 별도로 열어 둔 무관한 통합문서는 이 작업으로 인해 종료된 것이 아님을 확인함.

## P3-01 F-025 구현 검증(2026-09-18 재개 세션에서 완료)

- [x] 원본 HWPX `[9-14]`를 직접 대조함 — 표가 아닌 순수 텍스트 서식이며 업체명 표시 자리가 원문에 없음을 확인함(F-021~F-023과 다른 구조).
- [x] `서식_매핑표.md`의 F-025 행이 F-026 행과 완전히 동일한 문구임을 발견해 기록함(매핑표는 수정하지 않음).
- [x] `scripts/build_excel_v1_structure.ps1`·`build_excel_v1_vba.ps1`·`verify_excel_v1.ps1`에 F-025 시트·선택출력 로직·자동 검증 코드를 작성함. PowerShell 구문 오류 없음을 확인함(`[System.Management.Automation.Language.Parser]::ParseFile`).
- [x] 작업 시작 전 좀비 EXCEL.EXE(PID 26012)를 확인·종료함.
- [x] 안전 빌드 전체 실행·PASS 확인 — `run_build_excel_v1_structure_utf8.ps1` 단독 실행으로 `artifacts/excel/교복구매_길라잡이_20260918_v1.xlsm` 생성, `verify_v1_log.txt` 전 항목 PASS(FAIL 0건, F-025 관련 6건 포함, 기존 F-007·F-014~F-024·DB 회귀 없음 포함). 이후 문서 갱신 Edit이 트리거한 훅 재검증 실패로 로그가 삭제돼(F-023 때와 동일 현상) 2차로 `교복구매_길라잡이_20260918_v2.xlsm`을 재생성함(FAIL 0건 클린 로그 재확보).
- [x] PDF 육안 확인 — 비식별 사본(`artifacts/excel/_diagnostic/F-025_20260918_qa/`, 확인 후 삭제)에서 F-025 단일 시트를 PDF로 내보내 200dpi PNG로 직접 렌더링·확인함. A4 1쪽·잘림 없음, 업체명 미출력, 학교 공통정보(날짜·수신처)만 반영, A/S 기간·편의성·지정업체·내용 전 항목 공란을 확인함.
- [x] `_workspace/03_excel/F-025_구현검증.md` 작성 완료.
- [x] 진단용 Excel COM 프로세스와 비식별 QA 사본을 검증 완료 후 정리함(사용자 실제 파일은 시작부터 끝까지 무관).

## P3-01 F-001~F-006 구현 검증(2026-09-18~19, 계획.md 8.1절 1순위 전체 완료)

- [x] 6종 각각 원본 HWPX(3·4·5·6·7~12·13쪽)를 kordoc `parse_pages`로 직접 대조함.
- [x] 위원 반복행은 신규 설계 대신 F-014~016 설계 시 마련됐으나 미사용이던 기존 저장소(`기초자료입력!B58:C67`, `DB_위원`)를 재사용함(F-001·F-005·F-006 3종이 공유). 위원 성명은 마스킹 식별표시만 반영하고 실명은 자동 처리하지 않음.
- [x] F-005는 원문이 6개 물리 페이지 분량임을 확인해 "A4 1쪽 자연 배율" 원칙의 명시적 예외(3쪽 자연 다중 페이지 출력, 강제 축소 없음)로 처리하고 검증 스크립트에 이 예외를 명시함. 나머지 5종은 A4 1쪽 자연 배율(`Zoom=100`, `FitToPagesWide/Tall=False`, `HPageBreaks=VPageBreaks=0`)을 확인함.
- [x] `scripts/build_excel_v1_structure.ps1`·`build_excel_v1_vba.ps1`·`verify_excel_v1.ps1`에 6종 시트·선택출력 검증 함수·자동 검증 코드를 Form ID 하나씩 순서대로 추가함(`기초자료입력`·기존 VBA 로직 무변경).
- [x] 각 Form ID마다 안전 빌드 실행·전 항목 PASS를 확인함(F-001 162/162, F-002 166/166, F-003 173/173, F-004 176/176, F-005 182/182, F-006 189/189, 누적 회귀 없음).
- [x] 각 Form ID마다 비식별 사본(`artifacts/excel/_diagnostic/F-00N_qa/`, 확인 후 삭제)에서 PDF를 내보내 직접 렌더링·육안 확인함.
- [x] 작업 중 발견한 실질적 결함을 전부 수정하고 기록함: F-002 다중행 병합 셀 텍스트 잘림·병합 자동 통합, F-003 PowerShell `@()` 배열 첫 원소 문자열 연결(+) 파서 결함(이 프로젝트 전체에 적용되는 재발 방지 원칙 확립), F-004 줄바꿈 문단 잘림, F-005 `.Formula`/`.Value2` 혼용·`Rows.AutoFit()` 병합 셀 계산 한계·한글 곡선 따옴표 파싱 결함.
- [x] `_workspace/03_excel/F-001_구현검증.md`~`F-006_구현검증.md` 작성 완료.
- [x] `task.md`·`test.md`·`로그.md`·`계획.md`(8.1절 1순위 완료 표시) 갱신 완료.
- [x] 좀비 EXCEL.EXE 프로세스와 훅 레이스 락파일 충돌을 매 단계 확인·정리함(6종 작업 전반에 걸쳐 재발, 프로세스 종료 대기 후 단독 재실행으로 회피).

## P3-01 F-041~F-043 구현 검증(2026-09-19, 계획.md 8.1절 2순위 전체 완료)

- [x] 원본 HWPX 68~73쪽(`[14]`~`[16]`)을 kordoc으로 직접 대조함 — F-041(낙찰자 결정, 기안문)·F-042(낙찰자 결정 통보, 통보문)·F-043(계약체결, 기안문).
- [x] F-017~F-025의 "업체 반복행 전체 순회"와 다른 "낙찰 업체 1곳 선택" 패턴(F-015의 I3 선택 순번 재사용)으로 설계·구현함. 대표자·소재지·전화번호·비고는 F-019~F-023과 동일하게 수기 작성 공란 유지.
- [x] `서식_매핑표.md`의 F-041 K-02(합계금액) 매핑 불일치를 발견·기록함(수정하지 않음, P1 재검토 과제). F-043은 원문의 명시적 산식(단가×예정수량)에 따라 K-01/K-02를 실제 구현함.
- [x] 기존 `기초자료입력!C15~C18`(기초금액·예정수량·납품기한·제출기한)을 재사용함(기초자료입력·기존 VBA 무변경, 새 시트 3개·새 VBA 함수만 추가).
- [x] 안전 빌드 2회 실행·PASS 확인 — 1차 `교복구매_길라잡이_20260919_v3.xlsm`(216/216 PASS) 후 PDF 육안 확인에서 F-042 표 헤더 겹침 결함 발견·수정, 2차 `교복구매_길라잡이_20260919_v4.xlsm`(216/216 PASS, 기존 F-001~F-025·DB 회귀 없음).
- [x] PDF 육안 확인 — 비식별 사본(`artifacts/excel/_diagnostic/F-041_043_20260919_qa/`)에서 3종을 하나의 PDF(3쪽)로 결합 출력·170dpi 렌더링. A4 1쪽 잘림 없음, 낙찰 업체명만 반영, 개인정보 필드 공란, F-043 계약금액 자동 계산(45,000원×100벌=4,500,000원) 정확함을 확인함.
- [x] `_workspace/03_excel/F-041_043_구현검증.md` 작성 완료.
- [x] `task.md`·`test.md`·`로그.md`·`계획.md`(8.1절 2순위 완료 표시) 갱신 완료.

## P3-01 F-032~F-040 구현 검증(2026-09-19, 계획.md 8.1절 3순위 전체 완료)

- [x] 착수 전 (a)(b) 그룹이 최신 코드 기준 안전 빌드를 통과한 적 없는 상태(VBA `선택된시트목록` 프로시저 컴파일 크기 한계 초과로 `$wb.Save()` 실패)임을 발견·기록함.
- [x] `선택된시트목록`을 리팩터링함 — 30개 개별 skip 카운터 → Dictionary, 단순 출력군 20개 Form ID ElseIf 비교 → Select Case 조회 함수(업체 반복 출력군 로직은 동작 변경 없음).
- [x] F-032·F-033·F-034·F-037·F-038·F-040(이전 세션 작성분)을 최신 코드 기준으로 재검증함.
- [x] F-035(정량평가 결과)·F-036(제안서 평가 결과)을 F-032/033과 같은 업체 반복행 집계 보고서 패턴으로 신규 구현함(F-015/F-016 학교 평가 입력열 재조회, 신규 DB 없음).
- [x] F-039(업체별 제안서 평가표)를 F-041~043과 같은 I3 단일 업체 선택 패턴으로 신규 구현함(위원 8명 원점수 문서 전용 입력칸, "최고점·최저점 제외" 총계/평균, 위원명 자동 반영 금지).
- [x] PDF 육안 확인 중 F-039 제목 병합이 I3 셀을 삼키는 버그, F-035 소스 셀 공백 미확인으로 0점 오표시되는 버그, F-032·F-039 A4 1쪽 폭 초과를 발견·수정함.
- [x] PowerShell 문자열 보간 `"$변수:"`(콜론)이 드라이브/스코프 한정자로 오인돼 범위가 깨지는 구조적 함정을 F-040·F-033에서 재현·확정하고 `${r}` 명시로 수정함.
- [x] 위 콜론 함정이 F-007·F-014 등 범위 밖 기존 "완료" 서식에도 존재함을 발견·기록함(수정하지 않음, 후속 세션 권고).
- [x] 안전 빌드 최종 256/256 PASS, FAIL 0건 확인(`교복구매_길라잡이_20260919_v10.xlsm`).
- [x] PDF 육안 확인 — 비식별 사본(`artifacts/excel/_diagnostic/F-032_035_036_039_20260919_qa/`, `F-032_040_v10_20260919_qa/`)에서 F-032~040 9종 결합·F-033/F-040 재검증. A4 1쪽 잘림 없음, 개인정보 필드 공란, F-035/036/039 계산값 정확함을 확인함.
- [x] `_workspace/03_excel/F-032_040_구현검증.md` 작성 완료.
- [x] `task.md`·`test.md`·`로그.md`·`계획.md`(8.1절 3순위 완료 표시)·`체크리스트.md` 갱신 완료.

## P3-01 F-044~F-050·F-008~F-012 구현 검증 및 계약방법안내 확장(2026-09-19~20, 계획.md 8.1절 4~6순위 전체 완료)

- [x] F-044·F-045·F-046·F-048(가정통신문 4종)을 학교명·학년도·구매명·제출기한(B-07) 공통값만으로 신규 구현·안전 빌드 PASS·PDF 육안 확인함.
- [x] F-047(수요조사 신청 여부, 개인정보 응답란 포함)을 `quality-compliance-reviewer` 독립 검토로 개인정보 미반영·미저장을 확인받고, 검토 중 발견한 표제(D-02) 미반영·발행일(C-05) 매핑 누락 Medium 결함 2건을 즉시 수정함.
- [x] F-049(만족도 설문조사 실시 기안)를 신규 구현하고, F-050(만족도 조사 설문지, 개인정보 특별 취급 문서)의 시트 보호 누락 High 결함(빈 양식 기본값만 검사해 원자료 직접 입력·저장·PDF 출력을 차단하지 못함)을 인쇄 전용 시트 보호·저장/출력 거부 로직으로 수정함.
- [x] F-050 보완 빌드 중 발견한 별도 VBA 컴파일 오류(`F050원자료없음` 함수가 `Private`로 선언돼 다른 모듈에서 참조할 때 컴파일 실패)를 `Public`으로 수정해 `선택서식_PDF저장()` 매크로 전체를 정상화함.
- [x] F-009·F-010·F-011·F-012(사전 규격공개·입찰공고·계약 특수조건 등 고난도 조판 4종)를 원문 대조로 구현·검증하고, F-011은 기존 진단 PDF가 다중 시트 혼입으로 무효였던 것을 재확인해 유효한 단일 서식 조판 증빙(`F011_visual_QA_only.pdf`)으로 확정함.
- [x] F-008(교복 사양서)을 F-013과 같은 이미지·병합표 복합 서식으로 확인했으나, 사용자 결정에 따라 전체 위임 대신 품목명·표 구조만 구현하고 디자인 이미지·섬유혼용률·원단색상은 담당자 작성 공간으로 남김.
- [x] 계약방법안내 시트를 2단계 입찰 예시 1개에서 물품 구매 4대 유형(수의계약·일반경쟁입찰·제한경쟁입찰(지역제한)·2단계 입찰) 선택형으로 확장하고, 유형 선택+법령확인 두 단계 모두 충족해야 B-03에 반영되는 기존 안전장치를 유지함. 금액 구간 자동 판정은 원본 데이터 부재·법령 개정 빈도로 인한 위험 때문에 재현하지 않기로 확정함(2026-09-20 사용자 결정).
- [x] 안전 빌드가 위 각 항목마다 PASS함을 확인함(최종 `교복구매_길라잡이_20260920_v13.xlsm`). 이로써 계획.md 8.1절 1~6순위 전체(43종)가 완료됨.
- [x] `task.md`·`test.md`·`로그.md`·`계획.md` 갱신 완료.

## P3-01 통합 선택 출력 회귀 검사 커버리지 보강(2026-09-20)

- [x] `scripts/verify_excel_v1.ps1`의 통합 선택 출력 PDF 테스트(7절)가 F-009·F-010·F-011·F-045~F-050(9종, 개별 구현·검증은 완료됐으나 이 통합 목록에서만 누락돼 향후 코드 변경 시 회귀를 못 잡는 사각지대였음)을 빠뜨리고 있음을 발견함.
- [x] 선택 목록에 9종을 추가함(빌드/VBA 스크립트 코드 변경 없음, 테스트 스크립트만 수정).
- [x] 안전 빌드 전체(구조→VBA→검증)를 재실행해 43종 통합 PDF(659.4KB) 생성과 F-047·F-050 개인정보 필드 공란 유지를 포함해 전 항목 PASS를 확인함(`교복구매_길라잡이_20260920_v14.xlsm`).
- [x] `task.md`·`test.md`(E-01·E-04·E-06 갱신, X-32 신규 추가)·`로그.md` 갱신 완료.

## P3-01 F-027·F-029·F-030·F-031 구현 검증(2026-09-21, 계획.md 8.1절 후순위 보류 4종 전체 완료)

- [x] 원문 HWPX 56~60쪽(`[9-16]`~`[9-20]`)을 kordoc parse_pages로 대조해 F-027(위임장)·F-029(개인정보제공 동의서)·F-030(청렴계약 이행서약서)·F-031(교복 품목별 금액표)을 구현함.
- [x] F-027·F-030을 F-002/F-003과 같은 학교 단위 서약 문서(업체 반복행 루프 없음)로, F-031을 F-043 낙찰단가(C16) 기준 K-01/K-02 계산이 포함된 단일 출력으로 구현함.
- [x] `scripts/build_excel_v1_structure.ps1`·`build_excel_v1_vba.ps1`·`verify_excel_v1.ps1`에 4개 시트·`검증_F0XX출력가능()` 4종·자동 검증(원문 대조·개인정보 공란 경계·A4 1쪽·출력 게이트)을 추가하고, 통합 선택 출력 PDF 테스트 목록에도 4종을 처음부터 포함함(X-32에서 발견한 사각지대를 재발시키지 않음).
- [x] F-029(개인정보 가능 문서)를 `quality-compliance-reviewer` 독립 검토로 확인함(검토 결과는 로그.md 참고).
- [x] 안전 빌드 1차 시도에서 VBA 함수-매개변수로 COM 워크시트 개체를 넘기면 "지정된 캐스트가 잘못되었습니다" 오류가 재현되는 PowerShell COM 상호운용 함정을 발견해, 문자열·정수만 넘기고 COM 속성 대입은 항상 호출부에서 직접 하는 기존 F-005/F-012 패턴으로 전환함.
- [x] 마스킹 QA PDF 육안 확인에서 F-030의 긴 도입 문단이 2행 병합+AutoFit 조합으로 줄바꿈 높이가 부족해 텍스트가 겹쳐 보이는 결함(및 A4 1쪽 초과)을 발견해 AutoFit을 문자 길이 기반 LineCount 계산으로 교체하고 여백을 축소해 수정함.
- [x] 같은 QA PDF에서 F-031 "건 명" 문구가 학년도를 중복 표시하는 결함을 발견해 기초자료입력!C12를 그대로 사용하도록 수정함(F-041의 "가. 건명"에도 같은 잠재 결함이 있음을 발견했으나 이번 범위 밖이라 기록만 함).
- [x] 재수정 후 안전 빌드 전 항목 PASS(`교복구매_길라잡이_20260921_v2.xlsm`)와 재렌더링한 QA PDF 4쪽 육안 확인(겹침·잘림·중복 표기 없음, 개인정보 필드 전부 공란)을 완료함.
- [x] `계획.md`(8.1절 후순위 보류 완료 표시)·`test.md`(X-33~X-36 신규)·`task.md`·`로그.md` 갱신 완료.

## UX 개선 3종 + 기존 버그 4건 수정(2026-09-21 후속 세션)

- [x] 원격(`git pull`) 동기화 — 로컬 전용 충돌 산출물 14개(원격이 최신) 정리 후 16개 커밋 fast-forward.
- [x] ① 서식선택_출력 행별 미리보기/PDF 버튼 신규 구현(47개 구현 서식 개별 출력, `폼ID별_실행` 공용화).
- [x] 버그 A: PDF 파일명이 초 단위 타임스탬프만 써서 연속 클릭 시 덮어쓰던 문제 — 시트명 포함 + 1/100초 보정으로 수정.
- [x] 버그 B(기존 버그, 실사용자 재현): 오류 13(VBA `And`/`Or` 단축평가 부재) — `양수값`·`품목행검증`·`평가행검증` 3곳을 `If` 분리로 수정.
- [x] 버그 C(사용자 지적): F-012 계약특수조건 제3조 납품기한 미반영 — 날짜만 `기초자료입력!C17`과 연동(업체명·장소는 2026-09-20 결정대로 원문 유지).
- [x] ② 계약절차안내 팝업(`frmContractGuide` UserForm) 신규 구현 — 계약방법 3종 버튼 항상 노출(금액 자동판정 미재현), 입찰계약 11단계는 원문 대조로 확정, 관련 서식 미리보기/PDF는 ①의 `폼ID별_실행` 재사용.
- [x] UserForm 관련 COM 제약 3건 발견·우회: Width/Height/Caption은 `Properties()` 경유 필요, 컴포넌트명은 ASCII 전용, `.Font` 속성 외부 COM 접근 불가.
- [x] `.Show`(모달) 없이 `초기화()`만 호출하는 헤드리스 검증(`검증_계약절차폼`) 추가.
- [x] ③ 기초자료입력 "빠른 출력 선택" 패널 신규 구현 — 기존 입력 필드 배치는 무변경, T열 이후에 52개 체크박스를 `서식선택_출력!A열`과 LinkedCell 양방향 연동.
- [x] 버그 D: 존재하지 않는 시트 수식 참조로 인한 25분 정지 재현·확정 — 참조 시점을 대상 시트 생성 이후로 이동해 해결.
- [x] 버그 E: `HPageBreaks.Count` 캐시 타이밍 경쟁으로 무관한 서식 15개 동시 실패 — 행 Height 읽기로 강제 갱신하는 보정을 검증 스크립트에 추가.
- [x] 테스트 코드 자체의 오류 1건(체크박스 해제 상태를 `0`으로 오인, 실제는 `xlOff=-4146`) 정정.
- [x] 원본 XLSM 실수 저장 발견(사용자 비교용 Excel 창에서 저장) → `git checkout`으로 복구, SHA-256 재확인.
- [x] P2-02 자동 확인(1층 "여는지"만) — 한컴 COM으로 P2-01 산출물 8개 오류 없이 열림 확인, F-013·F-007 페이지 수 이상 지점을 발견해 사람 확인 대상으로 좁힘.
- [x] P2-02 2층(한컴 실제 레이아웃 육안 확인) — 2026-09-24 사용자가 직접 확인해 통과 처리함. P2-02 전체 완료.
- [x] 안전 빌드 총 20회 실행(성공 7회 v1~v7, 나머지는 위 버그 재현·수정 과정) 끝에 `교복구매_길라잡이_20260921_v7.xlsm` 전 항목 PASS 확인.
- [x] `로그.md`·`task.md`·`test.md`(X-37~X-40 신규) 갱신 완료.

## 실사용 버그 6건 추가 수정 + 시트 순서 재배치(2026-09-21, 같은 후속 세션 이어서)

- [x] 버그 F: 빠른 출력 선택 패널 상단 버튼이 기존 저장/불러오기 버튼과 좌표 겹침(사용자 보고: "버튼이 없어졌다") — `$tLeft`/`$tLeft+170`(2행)으로 재배치.
- [x] 네비게이션 버튼 신규: 구현된 47개 서식 시트 각각에 "서식선택_출력으로/계약방법안내로" 버튼 2개(열 N, `.PrintObject=False`로 PDF 미노출), 계약방법안내에 "기초자료입력으로/서식선택_출력으로" 2개, 기초자료입력에 "계약방법안내로" 1개 추가.
- [x] 팝업 관련서식 무반응 수정: 모달(`.Show`)+네이티브 인쇄미리보기 충돌로 인한 화면 정지를 `frm.Show 0`(모달리스)+`Me.Hide`/`Me.Show 0`로 해소, 관련 서식 첫 항목 자동 선택 추가.
- [x] "출력할 수 없습니다" 메시지 원인 불명 해결: 신규 `출력불가사유()`가 `검증_필수값검증()`→`검증_위원행검증()` 순으로 구체적 원인(위원 마스킹 형식 안내 포함)을 반환하도록 `폼ID별_실행` 메시지에 반영.
- [x] F-012 제1조(구매 수량 및 구매 단가) "20○○학년도" 미반영 수정 — `기초자료입력!C5`와 연동(제3조는 직전 라운드 완료, 나머지 108건은 2026-09-20 확정대로 의도적 미반영 유지).
- [x] 시트 순서를 `1.사용설명서→2.기초자료입력→3.계약방법안내→탐색 허브 시트→F-001~F-050 오름차순`으로 `Worksheet.Move` 재배치.
- [x] 버그 G: `선택서식_PDF저장_실행`의 `Err.Raise`가 `Application.Run` COM 경계를 넘으며 불안정 유발(사용자가 코드 직접 붙여넣어 지적) — Boolean 반환 + `pdf_error_log.txt` 파일 로그로 전환.
- [x] 버그 H: G 수정 직후 드러난 반복행 서식(F-015 등) 배치 PDF "Subscript out of range" 회귀 — 이분 탐색으로 F-015까지 좁히고 `.Copy` 직후 `Sheets(Sheets.Count)`의 COM 타이밍 불안정을 근본 원인으로 확정, 임시시트생성 함수 10곳 전부를 `ActiveSheet`로 교체.
- [x] 원본 XLSM 재차 실수 저장 발견(사용자 비교용 Excel 창) → 동일 절차(프로세스 미점유 확인 → `git checkout` → SHA-256 재확인)로 복구.
- [x] 안전 빌드 최종 재시도(retry28)에서 전 항목 PASS 확인, `교복구매_길라잡이_20260921_v10.xlsm` 생성.
- [x] `로그.md`·`task.md`·`test.md`(X-41~X-46 신규) 갱신 완료.

## 버그 2(미사용 표 칸 정리)·요청 4(DB 시트 이동+버튼) 완료 및 무관 기존 결함 2건 발견·수정 (2026-09-23)

- [x] 47개 서식(F-001~F-025·F-032~F-050 중 구현분)을 `서식_매핑표.md` 대조 + 전체 파일 내부 필드ID(`K-0N`/`R-0N`/`C-0N`/`B-0N`/`D-0N`) 리터럴 검색으로 조사함.
- [x] F-014(G20)·F-031(G18) "비고" 합계 행에 내부 계산필드ID 문자열(`"K-03"`·`"K-02"`)이 실수로 인쇄되던 죽은 칸 2건을 확정·수정(빈 칸으로 정리).
- [x] 그 외 표는 죽은 칸 없음으로 결론(F-024·F-031의 비고 열은 정당한 안내문/공란, F-048 신청 수량 표는 학부모 수기 작성용 정당한 빈 양식).
- [x] DB·DB_품목·DB_업체·DB_위원·DB_평가 5개 시트를 Hidden/VeryHidden 보호는 유지한 채 탭 순서를 학교정보 뒤·F-001 앞으로 재배치(`Worksheet.Move`), 기초자료입력에 "DB 열기(관리자용)"·DB 시트에 "DB 닫기(다시 숨김)" 버튼 왕복(`DB시트열기`/`DB시트닫기`, 자동검증용 `검증_` 무메시지 변형 포함)을 신규 구현.
- [x] COM 함정 2건 발견·수정: (1) DrawingObjects=True로 보호된 시트에 `Buttons().Add()` 호출 시 COM 예외 — 배치 직전 Unprotect, 배치 후 재보호로 해결. (2) VeryHidden(2) 시트에 `Worksheet.Move()` 호출 시 COM 예외 — 이동 직전 Visible을 임시로 되돌렸다가 이동 후 원래 값으로 복원해 해결.
- [x] 버그 2·요청 4와 무관한 기존 결함 2건을 안전 빌드 도중 발견·수정: (1) F-036 정성평가 합계(D열)가 F-016의 5단계 등급 드롭다운 개편(2026-09-21~) 이후에도 여전히 H:K를 숫자로 가정해 그대로 더해 `#VALUE!` 오류가 나던 결함 — F-016과 같은 등급→점수 변환(`CHOOSE`+`MATCH`)으로 수정. (2) `계약방법안내` B5:B7 초기값 검증이 2026-09-16 구설계("예/아니오") 전제로 남아 있어 2026-09-21 카드 버튼 개편 이후 B6(고정 안내문)이 항상 채워져 있는데도 빈 칸을 요구해 구조적으로 계속 FAIL하던 스테일 테스트 — B5·B7만 빈 칸, B6은 고정 안내문 값을 검사하도록 정정.
- [x] 안전 빌드 4회 실행(1차 Buttons COM 예외, 2차 Move COM 예외+F-036·계약방법안내 FAIL, 3차 F-036만 FAIL, 4차 전 항목 PASS) 끝에 FAIL 0건(PASS 392건) 확인, `artifacts/excel/교복구매_길라잡이_20260923_v11.xlsm` 생성.
- [x] 마스킹 QA 사본(`artifacts/excel/_diagnostic/bug2_req4_qa/`)에서 F-014·F-031·F-036 PDF를 재출력해 비고 열 공란·정성 합계(55)·총점(105)·판정(적격)이 정확히 표시되고 잘림·겹침이 없음을 육안 확인함. DB 열기/닫기 버튼은 좌표 비교로 기존 버튼·헤더와 겹치지 않음을 확인함(인쇄 대상이 아닌 관리자용 화면 요소).
- [x] `test.md`(13절, X-66~X-71 신규)·`task.md`·`로그.md` 갱신 완료.

## 사용자 지시 13건(1~13) 후속 세션 — 6건 신규 원인 규명·수정, 6건 재검증만, 1건 미완료 (2026-09-24)

- [x] 세션 시작 전 정리: 좀비 Excel 프로세스(v11 미저장 상태로 열려 있음) 저장 없이 종료, `git checkout`으로 v11 복구, 미완성 중간 산출물(v1~v5·v9·20260923_v12) 삭제.
- [x] 항목 6 재분류: 지시서의 "소스 PNG 0바이트" 전제가 더 이상 사실이 아님(954,770바이트 정상 이미지로 이미 교체됨)을 직접 읽어 확인, 보류 해제.
- [x] 항목 1: `export_hwpx_p204_from_excel.ps1` CLI 직접 재현으로 종료 코드 1의 실제 원인(미저장 상태 읽기, 실패 사유 미노출) 2가지 확정·수정, 완전 입력 시 정상 생성(종료 코드 0)까지 재현 확인.
- [x] 항목 6: PDF 확인 중 인쇄영역 잘림(가로 배너가 기본 세로 A4에 걸려 STEP 03 잘림)을 자체 발견·수정, 수정 전/후 PDF로 대조 확인.
- [x] 항목 7: `frmContractGuide` 버튼 여백 확대(Height 400→420, 버튼 Top 346→362).
- [x] 항목 11: F-021·022·023·025 4개 서식이 기존 정리 로직(A1만 검사)에 걸리지 않던 이유(B1 배치·"[검토용]" 접두어)를 COM으로 확인·수정, 수정 후 4개 서식 A1·B1 공란 재확인.
- [x] 항목 12(후반): 서식선택_출력 행별 "이동" 버튼 47개 신설(`btnMove_*`, `행별_시트이동` 매크로), COM으로 개수 확인.
- [ ] 항목 12(전반, 미리보기 닫기 오이동 버그): 지난 세션 수정(`callerSheet.Activate` 4곳)이 코드에 남아 있음만 확인, 이번 세션 실사용 재현 테스트는 미실시.
- [x] 항목 13: F-049는 이미 버튼 있음을 COM으로 확인(신규 아님), F-050은 시트 보호로 버튼 추가가 조용히 실패하던 원인을 COM으로 확인·수정.
- [ ] 항목 2: 표 단일 입력 병합 전수조사 — 구조적 조사(55건 매핑)만 수행, 시각적(PDF) 확정판단 필요해 미완료.
- [ ] 항목 3·4·5·8·9·10: 코드 불변 확인 + 관련 골든 테스트 PASS 포함만 확인, PDF 육안 재확인은 미실시.
- [x] 안전 빌드(구조→VBA→검증) 최종 PASS(FAIL 0건), `artifacts/excel/교복구매_길라잡이_20260924_v13.xlsm` 생성.
- [x] `로그.md`·`task.md`·`test.md`(X-72~X-81 신규) 갱신 완료.

## P4 완료 게이트(2026-09-25, 사용자 결정)

- [x] 학교 담당자 시나리오 검증 — **범위 조정**: 신규 담당자의 관찰기반 사용성 테스트 대신, 사용자 본인이 사무실·자택 PC 2대를 오가며 반복 사용·확인한 이력을 현장 검증으로 인정함(`ADR-001-HWPX출력방식.md` 3.2절).
- [x] HWPX 생성 기능(HWPX 작성 버튼)이 실제 사용 환경에서 정상 동작함을 확인함 — 2026-09-24 종료 코드 1 근본 원인 수정 후 재현·검증, 48/48 성공.
- [ ] **미해결로 명시 유지**: 실제 학교 행정실 신규 담당자의 최초 사용 경험, 학교 PC의 PowerShell 실행 정책·매크로 차단 정책 등 제3자 환경에서의 동작 여부는 검증되지 않음(`ADR-001-HWPX출력방식.md` 3.2절 "검증하지 못한 것" 참고). 실사용 중 문제가 보고되면 이 항목부터 재조사함.

## P5-01 작업 게이트

- [x] P0~P4 완료 및 웹앱 착수 전제조건(Excel MVP 51/52 서식, HWPX 로컬 연계 검증, `ADR-001-HWPX출력방식.md`)을 확인함(2026-09-25, 사용자 결정).
- [x] 웹앱 요구사항·기술 스택·개인정보 ADR을 확정함. — `ADR-002-웹앱아키텍처.md`(상태 DONE, 2026-09-25 개정) + `웹앱_prd.md`·`웹앱_계획.md`·`웹앱_trd.md`를 사용자가 직접 제공한 상세 스펙(`0-1.txt`·`0-2.md`·디자인 PDF)을 최종 근거로 전면 재작성·확정함.
- [x] Excel/로컬 HWPX 생성 결과물을 웹앱이 어떻게 다루는지 결정함. — **웹 브라우저 클라이언트에서 직접 생성**(서버 없이 `hwpx_token_fill.ps1` 로직을 JSZip+DOMParser로 포팅)으로 확정(`ADR-002` §2, 2026-09-25 최종 개정 — 1차 결정 "Excel 안내만"에서 사용자 요구로 뒤집힘).
- [x] 정적 콘텐츠(절차 탐색)와 클라이언트 우선 처리(입력·미리보기·PDF·HWPX) 경계를 확정함(`ADR-002` §3).
- [x] 비용·접근성·보안·운영성·입력값 비영구 저장 기준으로 대안을 비교·확정함(`ADR-002` §2 결정표).

P5-01(웹앱 설계)은 이제 완료 상태이며 다음은 웹앱 자체 문서 체계(`웹앱_계획.md` WP-02/WP-03)로 이관해 관리함.

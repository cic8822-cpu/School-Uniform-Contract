# 웹앱 작업 로그 — 교복 학교주관구매 길라잡이 웹

## 2026-09-25 / 웹앱(P5) 문서 체계 신설 및 IA·HWPX 범위 전면 개정

**경위**: Excel MVP(P0~P4) 완료 후 P5(웹앱) 착수. 1차로 기존 통합 `prd.md`/`계획.md`를 근거 삼아 `ADR-002`·`웹앱_prd.md`·`웹앱_계획.md`·`웹앱_trd.md`와 하네스(에이전트 `web-ux-designer`·`web-frontend-engineer`, 오케스트레이터 `webapp-purchase-orchestrator`)를 만들었으나, 두 가지 핵심 가정(HWPX는 웹에서 생성하지 않음, 첫 화면은 9단계 절차 허브)이 실제 의도와 어긋났음을 이후 발견함.

**발견 경위**: Stop hook의 `git diff --check` 경고를 조사하다가, 이 세션이 만들지 않은 변경(`0-0. 최초 지시사항.txt`에 42줄 추가, `첫화면/` 폴더 재구성 — 사용자가 대화 밖에서 직접 편집)을 발견함. 새로 추가된 지시와 시안 이미지(`첫화면/원앙/`)를 검토한 결과 위 두 가정이 틀렸음을 확인하고, Plan Mode로 전환해 조사·계획·승인 절차를 거침(3개 Explore 에이전트 병렬 조사: 기존 문서 감사, Excel 계약방법 구조 추출, 시안·HWPX 기술타당성 조사).

**1차 정정(Plan Mode 승인, 반영 도중)**: 계약방법 4종 카드를 첫 화면으로, HWPX는 브라우저 클라이언트에서 생성(서버 없이 JSZip+DOMParser로 `hwpx_token_fill.ps1` 로직 포팅 가능함을 기술조사로 확인)하는 것으로 결정하고 `ADR-002` 수정을 시작함.

**2차 정정(더 권위 있는 소스 등장)**: 위 작업 도중 사용자가 세 가지 상세 소스를 직접 제공함 — `첫화면\교복 학교주관구매 웹앱 화면 디자인.pdf`(디자인 시스템·홈·계약절차·기초자료입력·서식함·문서미리보기 6화면 고해상도 시안), `0-2.교복 학교주관구매 웹앱 — 화면 구성 및 디자인 계획.md`(IA·데이터모델·화면설계 전체 명세), `0-1.교복계약 웹앱개발-프롬프트.txt`(매뉴얼 Source of Truth 원칙, 데이터 구조, 구현순서, 최종검증 기준을 담은 마스터 프롬프트). 이 세 문서가 1차 Plan Mode 조사 결과와 대부분 일치하되 훨씬 상세하여, 이를 최종 근거로 채택하고 `ADR-002`·`웹앱_prd.md`·`웹앱_계획.md`·`웹앱_trd.md`를 전면 재작성함.

**최종 확정 사항**:
- IA: 홈(계약방법 4종 카드)·계약방법안내·계약절차·기초자료·서식함·내보내기·사용안내의 7개 메인 네비게이션(9단계 절차 허브는 완전 폐기 — Excel도 이미 동일하게 전환함, `로그.md` 2026-09-23 참고).
- HWPX: 웹 브라우저 클라이언트에서 직접 생성(원본 템플릿 Field 치환 방식, 서버 미경유). `scripts/hwpx_token_fill.ps1`·`hwpx_school_footer_fill.ps1`의 로직을 TypeScript로 포팅하는 것으로 WP-03에 반영.
- 데이터 모델: 계약방법/Workflow/Form/Field Master + Template Mapping(4+1 구조, `0-1.txt` §22) — 이 구조화(WP-02)가 완료되기 전에는 UI 코드를 작성하지 않기로 함.
- 서버·DB 미사용, 정적 배포, Excel Import/Export 지원 원칙은 1차 결정에서 변경 없이 유지됨.

**신규 작성 문서**: `웹앱_prd.md`(전면개정)·`웹앱_계획.md`(전면개정)·`웹앱_trd.md`(전면개정)·`웹앱_task.md`(신규)·`웹앱_test.md`(신규)·`웹앱_체크리스트.md`(신규)·`웹앱_로드맵.md`(신규)·`웹앱_로그.md`(신규, 이 파일)·`ADR-002-웹앱아키텍처.md`(개정, 상태 DONE).

**하네스 갱신 필요(다음 작업)**: `.claude/agents/web-ux-designer.md`·`web-frontend-engineer.md`·`quality-compliance-reviewer.md`와 `.claude/skills/webapp-purchase-orchestrator/SKILL.md`가 여전히 "HWPX 웹 미생성"·"9단계 허브" 가정을 담고 있어 이번 정정과 어긋남 — 즉시 갱신 필요(이 로그 작성 직후 진행).

**미해결/다음 세션 인계**: WP-02(마스터 데이터 구조화) 착수는 사용자 확인 후 진행함(`웹앱_계획.md` §6). Excel 트랙의 별도 이슈 2건(v14 수동서식 반영, 15개 서식 날짜 버그, `0-0. 최초 지시사항.txt` #18·#19)은 사용자가 "웹앱 설계 먼저 마무리 후 처리"를 요청해 보류 중 — 웹앱 WP-02 착수 전후로 사용자에게 재확인 필요.

## 2026-09-25 / 외부 디자인 스킬(frontend-design·ui-ux-pro-max) 하네스 반영

사용자가 `frontend-design`·`ui-ux-pro-max-skill`·`harness-100/37-web-scraper`·`첫화면/원앙` 4개 경로를 웹앱 디자인 작업에 참고하라고 재요청함(0-0.txt [웹앱] 섹션과 동일 내용). Plan Mode로 검토한 결과: `harness-100/37-web-scraper`는 콘텐츠 무관(하네스 구조 예시일 뿐, 이미 적용됨), `frontend-design`은 이 세션에 이미 설치된 스킬이라 직접 호출 가능, `ui-ux-pro-max-skill`은 설치된 스킬이 아닌 외부 git 저장소라 방법론만 참고 가능, `첫화면/원앙`은 이미 `0-2.md`/디자인 PDF의 근거로 반영 완료된 상태임을 확인함. 사용자 확인("하네스에 절차로만 반영")에 따라 지금 새 디자인 산출물을 만들지 않고, `web-ux-designer.md`·`web-frontend-engineer.md`·`webapp-purchase-orchestrator/SKILL.md`·`웹앱_계획.md`에 "WP-03 화면 구현/보강 시 `frontend-design` 스킬 호출 + `ui-ux-pro-max-skill` 방법론 참고" 절차만 추가함.

## 2026-09-25 / WP-02 마스터 데이터 완료 및 독립 재검토 승인

`_workspace/05_web/data/`에 계약방법 4종·Workflow 3종·Form 52종·Field 33종·Template Mapping을 작성함. `scripts/verify_web_wp02.ps1`으로 계약방법·Workflow·Form·Field의 수량/참조, 계산 Field 분리, 48개 HWPX 템플릿 토큰↔Field 계약, F-050 D-05 제외, 근거상태를 전수 검증했고 PASS를 확인함.

초기 독립 재검토의 High 항목(계산값 중복, 계약방법 미확정 Form의 빈 배열, 일반경쟁입찰의 2단계입찰 오안내, HWPX 반영범위, F-042 제출기한 토큰 계약, F-050 원천 매핑 충돌)을 모두 보완함. 수의계약은 사용자 제공 `0-1.txt`·`0-2.md`가 매뉴얼 확인 필요로 분류하므로 Excel 계약절차안내 원문 위치와 확인 필요 상태를 함께 표시하도록 정리함. 최종 독립 재판정은 `WATCH / APPROVE`, Critical·High 차단 0건임.

완료 훅은 웹 문서와 WP-02 실검증기를 직접 실행하고, 동일 실패를 반복해도 성공 종료 코드로 우회하지 않도록 수정함. `웹앱_task.md`·`웹앱_체크리스트.md`·`웹앱_계획.md`를 WP-02 DONE으로 갱신함.

## 2026-09-25 / WP-03 웹앱 구현(codex CLI 중단분 이어받아 완료)

**경위**: 사용자가 "codex로 작업하다 멈췄어, 남은 작업 마무리 해줘"로 요청함. 이전 세션(codex CLI)이 Vite+React19+TS 스캐폴딩과 `App.tsx`(압축 1줄)·`App.css`(압축 1줄)·`src/lib/hwpx.ts`(허용/금지 토큰 목록만 정확, 미연결)·`src/lib/workbook.ts`(공통 Field만, 미연결)까지 만들었으나 중단됨. 조사 결과 `fields.json`을 App이 아예 로드하지 않아 기초자료입력이 5개 Field만 하드코딩돼 있었고, HWPX·PDF 출력 버튼이 UI 어디에도 없었으며, `hwpxFooterFill`(결재란 담당자·교장 채움)이 파일 자체가 없었음.

**구현 범위**: `웹앱_trd.md` §5 구조(`components/`·`lib/`·`data/`)로 `App.tsx`를 리팩터링해 7개 화면(홈·계약방법안내·계약절차·기초자료입력·서식함·내보내기·사용안내)을 각각 작은 컴포넌트 파일로 분리함(신규 CSS는 기존 `App.css`와 별도 파일로 작성, `App.css`는 건드리지 않음). `fields.json`을 로드해 기초자료입력을 33개 Field 전부 + 반복그룹 4종(품목/업체/위원/평가, 각 10행)으로 완성하고 K-01/K-02/K-03을 자동계산·표시함(평가항목·배점 열은 Field 마스터에 ID가 없어 제외, `fields.json` 자체 주석과 일치). 기존 `hwpx.ts`의 허용/금지 토큰 목록은 `template-mapping.md` §1·`hwpx_token_fill.ps1` 39~40행과 문자 단위로 재대조해 그대로 유지하고, Blob 반환 함수로만 리팩터링함. `hwpx_school_footer_fill.ps1`의 `Set-NameNearLabel` 로직을 `hwpxFooterFill.ts`로 신규 포팅함(담당자·교장 라벨만 — 학교 주소·전화는 웹 마스터 데이터에 학교정보 조회 원본이 아직 없어 보류, `derivedLookups`가 Field ID 없이 정의돼 있다는 `fields.json` 자체 서술과 일치). `hwpxExport.ts`(단건)·`hwpxBulkExport.ts`(전체 ZIP)로 Field 값→토큰 매핑을 오케스트레이션해 문서 미리보기 모달·내보내기 화면의 버튼에 연결함. PDF는 `pdf.ts`(html2canvas+jsPDF)로 모달의 A4 미리보기 DOM을 캡처해 출력. `workbook.ts`를 반복그룹까지 지원하도록 확장(반복자료 시트 추가)해 업로드/다운로드 버튼에 연결. 사용안내는 이용 순서 5단계 + FAQ 5문항 + 용어집 6항목으로 확장.

**성능 대응**: 최초 빌드에서 메인 청크가 1.37MB(gzip 425KB)로 `web/performance.md`의 앱 페이지 예산(300KB)을 초과함을 발견해, `jszip`·`xlsx`·`jspdf`·`html2canvas`를 실제 버튼을 누를 때만 불러오는 동적 `import()`로 전환함. 결과 메인 청크는 261KB(gzip 82.6KB)로 예산 안에 들어오고, 무거운 라이브러리는 별도 청크로 분리됨.

**접근성(최소)**: 스킵 링크, `:focus-visible` 표시, 모달에 `role="dialog"`+`aria-modal`+`aria-labelledby`+Esc 닫기+열릴 때 닫기 버튼 포커스, 내비게이션 `aria-label`을 추가함. WCAG 대비 실측·전 해상도 스크린샷·오프라인 동작 검증은 하지 않음(`웹앱_test.md` WA-01~04 NOT_RUN 유지).

**실행 기반 검증(신뢰할 수 있는 근거만 기록)**: 크롬 devtools 자동화(chrome-devtools MCP)로 `npm run dev` 서버를 띄워 홈·계약방법안내·계약절차·기초자료입력(7탭, 품목 테이블 K-01/K-02 실제 계산: 수량10×단가50000=500,000원)·문서 미리보기 모달(F-008)이 실제로 렌더링되고 콘솔 에러가 없음을 확인함. 네트워크 탭에서 요청이 `localhost` 정적 자산·`/data/*.json`·Google Fonts뿐임을 확인(WQ-01 근거, 전체 시나리오는 아님). HWPX 파이프라인은 페이지 컨텍스트에서 `buildTokenFilledHwpx('/templates/F-008_교복사양서_템플릿.hwpx', {학교명:'검증초등학교'})`를 실제로 실행해 결과 zip을 JSZip으로 재검증(치환값 포함, 미치환 토큰 0건)했고, `fillHwpxFooter`를 F-007 실제 템플릿에 실행해 담당자·교장 라벨이 채워짐을 확인함. 한컴오피스에서 직접 열어보는 수동 확인은 이 세션에서 할 수 없어 미실행.

**미해결/다음 세션 인계**: WP-03-13(학교 PC 로컬 설치 패키지 배포)은 착수하지 않음 — `dist/index.html`이 자산을 절대경로(`/assets/...`)로 참조해 `file://` 직접 열기가 불가능함만 확인했고, 로컬 정적 서버 선택(예: `npx serve`, IIS, Python http.server)은 사용자와 확정 필요. 학교 주소·전화 자동반영(WE-04)은 학교정보 마스터 데이터 부재로 보류. 사람이 직접 조작해야 하는 골든 시나리오(WE-01~08 전체, WM-*, WA-*, WF-*)는 다음 QA 세션에서 실제 실행으로 채워야 함. `npm run lint`(oxlint) 통과, `console.log`·`any` 사용 0건을 grep으로 확인함.

## 2026-09-25 / 품질게이트 훅 회귀 복구, WP-03-13 배포 경로 검증

**경위**: 사용자가 "문서업데이트 커밋 푸시해줘"·"남은 작업 오류 등 수정 완료해줘 /goal"로 요청함. 이전 커밋(`fb4e4f2`/`9d208a9`) 시점에 `.claude/hooks/verify-phase.ps1`·`.claude/quality-gate.json`을 의도적으로 커밋하지 않고 보류했었는데, 이번에 직접 diff를 재확인함.

**회귀 확인 및 복구**: `verify-phase.ps1`의 변경분은 대부분 정당함(활성 Phase가 `WP-`로 시작하면 `웹앱_*.md` 문서 세트를 검사하도록 분기하는 로직, `WP-02` 전용 실검증기 실행 추가) — 하지만 그 과정에서 2026-09-19 사용자가 명시적으로 요청해 추가했던 "동일 실패 시그니처가 반복되면 두 번째부터는 정보성 안내만 남기고 차단하지 않는다"는 안전장치(`$previousSignature` 비교 및 조건부 `exit 0`)가 통째로 삭제되어 있었음. 해당 블록을 원래 로직 그대로 복원함(단, 복원하며 뮤텍스를 이중 해제하는 실수를 스스로 발견해 즉시 수정 — 실패 분기 상단에서 이미 `ReleaseMutex`가 호출되므로 조기 반환 분기에서 다시 호출하면 이미 해제된 뮤텍스에 대한 예외가 날 수 있었음).

**activePhase 최신화**: `.claude/quality-gate.json`의 `activePhase`가 이미 끝난 `WP-02`에 멈춰 있어 `WP-03`으로 갱신하고 `phaseTasks`(WP-03-01~13)·`phaseChecklistHeading`(`## WP-03 구현 게이트`)·`phaseTestIds`(WE/WQ/WM/WA/WF 중 상태 셀이 훅 정규식과 정확히 일치하는 항목만)를 채움. 실행 결과 훅이 WE/WM/WA/WF 대부분의 NOT_RUN과 WP-03-13의 미완료를 정확히 보고함 — 이는 실제로 WP-03이 100% 끝나지 않았다는 사실을 정직하게 반영하는 것이므로 억지로 통과시키지 않음. `웹앱_task.md`의 WP-03-12 상태 라벨을 훅이 인식하는 어휘(`PARTIAL`→`IN_PROGRESS`)로 정정함.

**WP-03-13 실측**: `webapp/vite.config.ts`에 `base: './'`를 추가하고 `useAppData.ts`(마스터데이터 4종 fetch)·`hwpxExport.ts`(HWPX 템플릿 fetch)의 절대경로(`/data/...`, `/templates/...`)를 신규 `lib/assetUrl.ts`(`import.meta.env.BASE_URL` 기준) 경유로 교체함. 크롬 devtools 자동화로 실측한 결과: `dist/index.html`을 `file://`로 직접 여는 것은 **Vite 설정과 무관하게 원천적으로 불가능**함을 확인함(모든 브라우저가 `file://` 출처의 `<script type="module">`을 CORS로 차단, 콘솔에 `origin 'null'` 차단 메시지 실측). 반면 `npx serve dist`로 로컬 정적 서버를 띄우면 콘솔 오류 0건으로 홈 화면이 완전히 렌더링됨을 확인함. 이 사실과 3가지 서빙 방법(npx serve/Python http.server/IIS)을 `webapp/DEPLOY.md`(신규)로 문서화하고 `웹앱_task.md` WP-03-13을 `IN_PROGRESS`로 갱신함(설치 스크립트·현장 방식 확정은 여전히 남음, 거짓 DONE 처리하지 않음).

**커밋 분리 이유**: 1개는 훅/게이트 회귀 복구(`fix`), 1개는 웹앱 정적 배포 경로 수정(`fix`)으로 나눠, 이전 커밋(`fb4e4f2`/`9d208a9`)과 성격을 섞지 않음.
